# 7. Системная область 0…777

> **Источники главы**
> * **[З]** «Системная область БК-0010» и Приложение «Полный список системных переменных
>   монитора БК-0010» (файл `DMBK1-B`, сост. П. Б. Эльтерман) — **основной и практически
>   единственный источник**: карта ячеек 0…777 в РСП отсутствует.
>   Сведения получены Зальцманом из листинга драйверного модуля **DMBK** (М. И. Дябин,
>   1983–1985), дизассемблирования ПЗУ и собственных экспериментов.
> * **[РСП § 4]** — границы стека (776…300) и зона 300–352, занятая драйвером магнитофона.
> * **[ПЗУ]** — точный диапазон ячеек, очищаемых командой `EMT 14` (40…277).

Системной областью называется зона адресов **0…777**. В ней размещаются:

* векторы прерываний (0…276);
* системные переменные драйверов монитора;
* стек (372…777).

Источники сведений об этих ячейках: авторский листинг драйверного модуля **DMBK**
(М. И. Дябин, Москва, 1983–1985), дизассемблирование ПЗУ и эксперименты
(Ю. А. Зальцман); мнемонические имена и формат — по файлу `DMBK1-B`, составленному
П. Б. Эльтерманом.

Часть ячеек свободна и может использоваться программистом, но нужно учитывать, что многие
из них заняты языками высокого уровня или распространёнными программами (например,
ассемблер-системой МИКРО.10К).

> **Что очищает `EMT 14`.** По листингу ПЗУ [✓ ПЗУ] инициализация заполняет ячейки
> 0…36 значением 100000, затем задаёт векторы 4, 30 и слово 32, после чего **очищает
> ячейки 40…277**. Ячейки 300…316, 320…371 и стек она **не трогает**.

---

## 7.1. Карта ячеек

Формат: адрес — мнемоника DMBK — размер — назначение.

### Векторы прерываний

| Адрес | Мнемоника | Разм. | Назначение |
| --- | --- | --- | --- |
| 0, 2 | | сл. | **Не используется.** При включении сюда (и в другие неиспользуемые ячейки) заносится адрес перезапуска системы — 100000. Случайное косвенное обращение вызовет инициализацию системы |
| 4, 6 | | сл. | Вектор по клавише «СТОП» и команде `HALT`. Значения: ПМ — 100442, МСД — 160722, Фокал — 121110, Бейсик — 120234 |
| 10, 12 | | сл. | Вектор по резервному коду |
| 14, 16 | | сл. | Вектор по T-разряду и `BPT` |
| 20, 22 | | сл. | Вектор по команде `IOT` |
| 24, 26 | | сл. | Вектор по аварии сетевого питания (вход ACLO). Внеприоритетный, аппаратно не задействован |
| 30, 32 | | сл. | Вектор по команде `EMT`. Штатно: 30 := 100112, 32 := 200 |
| 34, 36 | | сл. | Вектор по команде `TRAP` |

### Системные признаки — слово состояния дисплея (SYSPR)

Каждая ячейка — **байт**. Исходное содержимое при перезапуске — 0; при включении режима
становится 377, **кроме ячейки 43** (регистр «РУС»), куда заносится 200.

| Адрес | Мнемоника | Режим |
| --- | --- | --- |
| 40 | `PRCW` | Цветной режим / «32 символа в строке» |
| 41 | `PRINF` | Инверсия фона (экрана) |
| 42 | `PRRP` | Расширенная память (РП) |
| 43 | `PRRUS` | Русский регистр (значение **200**) |
| 44 | `PRPODC` | Подчёркивание символа |
| 45 | `PRINWS` | Инверсия символа |
| 46 | `PRISU` | Индикация символов управления (ИСУ) |
| 47 | `PRBLR` | Блокировка редактирования (БЛР) |
| 50 | `PRGRAF` | Графический режим (ГРАФ) |
| 51 | `PRZAP` | Режим записи точек (ЗАП) |
| 52 | `PRSTIR` | Режим стирания точек (СТИР) |
| 53 | `PRCWSS` | Цветной режим / «32 символа» в служебной строке |
| 54 | `PRPSSS` | Подчёркивание символа в служебной строке |
| 55 | `PRISSS` | Инверсия символа в служебной строке |
| 56 | `PRGAHK` | Гашение курсора |
| 57 | | Не используется |

#### Прямая запись в эти ячейки

Переключение режима записью в ячейку происходит **на несколько порядков быстрее**,
чем через `EMT 16`. Но поведение отличается от штатного:

* **44–47, 54–56** — запись 377 прямо включает режим, иногда с нюансами: при включении
  «БЛОКРЕД» и «ИНДСУ» отсутствует индикация в служебной строке; при записи 377 в 56
  курсор со следующего символа исчезает, но на экране остаётся его «призрак».
* **40–43, 50–53** — режим включается частично и «ненормально», дальнейшая работа
  с дисплеем затруднена или невозможна. Конкретно:
  * `43` — переключает «РУС»/«ЛАТ», но одновременно меняет на обратную установку «СТР»/«ЗАГЛ»;
  * `41` — проявляется только в отдельных случаях, например после переключения цветов;
  * `40` — блокирует вывод каждого второго символа (получаются широкие символы);
  * `42` — по достижении края экрана блокирует рулонный сдвиг, как бы организуя «РП»
    на обычном экране;
  * `50–52` — переключают в текстовую графику, но на экране остаётся тень символьного
    и графического курсоров, резко замедляется отработка команд текстовой графики.

Записав в ячейку число, **отличное от 0 и 377**, можно сделать режим «неотменяемым»
с клавиатуры.

### Переменные драйвера клавиатуры (DKLAW)

| Адрес | Мнемоника | Разм. | Назначение |
| --- | --- | --- | --- |
| 60, 62 | | сл. | Вектор прерывания от клавиатуры (кроме «АР2»/«НР»). Приоритет 4. Штатный обработчик — 101136 |
| 64…76 | | | Не используются |
| 100 | `WPTM` | сл. | Вектор прерывания от внешнего таймера (ПРТ, IRQ2). Вход — контакт В1 разъёма УП. Приоритет 4 |
| 102 | `SSPTM` | сл. | ССП для прерывания по таймеру |
| 104 | `BUFKL` | байт | **Код последнего введённого символа** (уже обработанный, с учётом регистра). Заносится, только если разрешено прерывание от клавиатуры. Широко используется для организации автоповтора |
| 105 | `PRZKB` | байт | Признак записи кода в ячейку 104. Используется командой `EMT 6`. `CLRB @#105` «отменяет» предыдущее нажатие |
| 106 | `BKPOWT` | сл. | Буфер константы повтора — число «пустых» циклов `SOB` перед выводом следующего символа при удержании «ПОВТ». В символьном режиме 20000, в графическом 2000. Минимальная скорость — 0 (эквивалент 200000), максимальная — 1 |
| 110 | `PRPOWT` | байт | Признак повтора кода: если ≠ 0, последний символ выводится повторно, после чего ячейка сбрасывается |
| 111 | `SCTAB` | байт | Счётчик табуляции — количество выводимых пробелов при отработке «ТАБ» |
| 112…120 | `MTAB` | 4 сл. | **Позиционная маска табуляции** (64д бита). Единичный бит — установленная метка. Можно задать позиции табуляции программно; чтобы метки появились в служебной строке, после записи маски нужно подать команду «установка индекса» (код 236) |
| 122 | `SCKLC` | байт | Счётчик ключа — длина текста текущего ключа в символах |
| 123 | | байт | Не используется |
| 124 | `TAKLC` | сл. | Адрес очередного байта текста текущего ключа |
| 126…150 | `AKLC` | 10 сл. | **Адреса начала текстов программируемых ключей.** По DMBK порядок: 126 → К10 (клавиша «0»), 130 → К1, 132 → К2, … 150 → К9 |

> Источники расходятся: Зальцман пишет «адреса (по порядку) начала текстов ключей,
> соответствующих клавишам 0, 1, … 9», что даёт ту же раскладку (126 — клавиша «0»).
> Таблица из «Программирования на БК-0010-01» указывает явно: 126 → К10, 130 → К1, …,
> 150 → К9. Это согласуется с описанием `EMT 12`, где номер ключа для клавиши «0» равен 12
> (10д).

### Переменные драйвера ТВ-монитора (DTVMN)

| Адрес | Мнемоника | Разм. | Назначение |
| --- | --- | --- | --- |
| 152 | `PRNRUL` | байт | Признак нарушения рулона (для сдвига экрана вверх/вниз) |
| 153 | `PRZTCK` | байт | Признак записи точки (используется в `EMT 30`/`EMT 32`): 0 — стирание, 1 — запись |
| 154 | `MASPGT` | байт | **Маска позиции графической точки (ГТ)** внутри байта. Ч/б режим (512 точек): 1, 2, 4, 10, 20, 40, 100, 200. Цветной режим — пара соседних битов: 3, 14, 60, 300 |
| 155 | `NMPGT` | байт | Начальная маска ГТ: 1 (ч/б) или 3 (цветной) |
| 156 | `NOMSIM` | сл. | **Номер символа на экране.** 0 — левый верхний, 2777 — правый нижний (режим 64 символа). От рулона не зависит. Последний символ строки оканчивается на 77, первый — на 00. При переходе двух младших разрядов через 0 драйвер выполняет перевод строки. В цветном режиме содержимое кратно 2. Координаты: `X + 100·Y` (64 симв.) или `2X + 100·Y` (32 симв.) |
| 160 | `AS` | сл. | **Абсолютный адрес текущего символа** (с учётом рулона) — адрес левого верхнего байта символьного курсора. Запись сюда другого значения заставит следующие символы формироваться с этого адреса (в том числе вне экрана, например по адресу 30000 — для «бегущей строки» или увеличенных копий) |
| 162 | `DSIMB` | сл. | **Горизонтальный размер символа в байтах** — смещение курсора после вывода символа. Режим 64 — 1, режим 32 — 2. Изменяя, можно выводить текст «вразрядку», по вертикали, по диагонали, назад |
| 164 | `KSVP` | сл. | Количество знакомест экрана: 3000 (обычный режим), 400 (РП) |
| 166 | `AGTVP` | сл. | Адрес (номер) байта текущей ГТ на экране. **Не зависит от рулона**: левый верхний угол — 0, правый нижний — 35777 |
| 170 | `AGT` | сл. | **Абсолютный** адрес байта текущей ГТ. При начальном положении рулона меняется от 42000 до 77777 |
| 172 | `DGW` | сл. | Длина графического вектора |
| 174 | `SCTVS` | сл. | Счётчик телевизионных строк |
| 176 | `BUFX` | сл. | **Буфер координаты X** для `EMT 30`/`EMT 32` — координата последней точки/конца вектора |
| 200 | `BUFY` | сл. | **Буфер координаты Y** для `EMT 30`/`EMT 32` |
| 202 | `ANVP` | сл. | **Адрес начала видеопамяти**: всегда 40000 (в РП — 70000), независимо от рулона. Годится как константа конца ОЗУ пользователя и как признак режима |
| 204 | `BAZVP` | сл. | **База видеопамяти** — относительный адрес начала экрана (адрес конца служебной строки). В исходном положении рулона 2000, меняется со сдвигом |
| 206 | `DVPB` | сл. | Длина видеопамяти в байтах: 40000 (обычный), 10000 (РП) |
| 210 | `DGPB` | сл. | Длина рабочей области экрана: 36000 (обычный), 5000 (РП; ещё 1000 байт — нижняя черта) |
| 212 | `FON` | сл. | **Маска цвета фона экрана.** Красный — 177777, зелёный — 125252, синий — 52525, чёрный — 0 |
| 214 | `MASCW` | сл. | **Маска цвета символа** (и графических точек, и векторов) |
| 216 | `FONSS` | сл. | Маска цвета фона служебной строки |
| 220 | `MSWSS` | сл. | Маска цвета символа служебной строки |
| 222 | `SCKOD` | сл. | Счётчик кодов |
| 224 | `SCUIND` | сл. | Счётчик установки индикаторов |

Ячейки 222 и 224 используются при выдаче в служебную строку индикаторов режимов:
«РУС», «ЛАТ», «ГРАФ», «ЗАП», «СТИР», «БЛР», «ИСУ», «РЕД».

> **Про ячейку 212.** При записи нового значения фон **сразу не меняется**. Он изменится
> после сброса экрана (клавиша «СБР», с потерей информации). Если же просто выводить
> символы, не очищая экран, фон каждого нового символа будет соответствовать новому
> содержимому 212, а остальное изображение не изменится. Этот приём удобен для организации
> меню и оконного интерфейса в вильнюсском Бейсике через `POKE &O212,...` + `PRINT`.

> **Расширение палитры.** Комбинируя маски 212 и 214 (не только четырьмя «штатными»
> значениями) по вертикали и горизонтали, можно значительно расширить цветовую палитру.
> Специально подобранные значения (например, 100001 в цветном режиме) позволяют
> «маскировать» выводимые символы.

### Прочие системные переменные DMBK

| Адрес | Мнемоника | Разм. | Назначение |
| --- | --- | --- | --- |
| 226…246 | | | Не используются |
| 250 | `DLBIT0` | сл. | Длина «0» для драйвера магнитофона (в условных единицах) |
| 252 | `DLBIT1` | сл. | Длина «1» для драйвера магнитофона |
| 254 | `DLBIT` | сл. | Длина бита для драйвера ТЛГ |
| 256 | `KPORT` | сл. | **Копия порта вывода.** Зарезервирована под копию 177714; драйверами монитора не используется — программист может вести её сам |
| 260 | `APOPKL` | сл. | **Адрес дополнительной программы обработки прерывания от клавиатуры.** Если занести сюда адрес, управление будет передаваться на него при нажатии любой клавиши. При нормальной работе обнуляется драйвером |
| 262 | `PRWK` | сл. | **Признак кода «ВК».** 0 — при нажатии «ВВОД» в программу передаётся код 12 (большинство режимов), иначе — код 15 (например, в Фокале) |
| 264 | `BUFSTA` | сл. | **Буфер стартового адреса** — физический адрес последнего загруженного файла (независимо от адреса в оглавлении). На него передаётся управление по директиве ПМ `S` без явного адреса |
| 266 | `BUFDL` | сл. | **Буфер длины массива** — длина последнего считанного файла |
| 270, 272 | | сл. | Не используются. По [ВМ1 § 7.3] вектор **270** разделяют два источника: прерывание по ниспадающему фронту на входе **nIRQ3** и прерывание от **встроенного ВЕ-таймера** (последнее — только в К1801ВМ1Г). В БК аппаратно не задействован ни один из них |
| 274, 276 | | сл. | Вектор прерывания от клавиатуры по нижнему регистру («АР2»). Штатный обработчик — 101362 |

### Переменные драйвера магнитофона (DMAG)

| Адрес | Мнемоника | Разм. | Назначение |
| --- | --- | --- | --- |
| 300 | | байт | **Флаг фазы** читаемого с МЛ сигнала (прямой/инверсный). [✓ ПЗУ]: очищается на входе в драйвер (`116112 CLR 300`) и в начале чтения (`116656`), наращивается в подпрограмме измерения длительности импульса (`117042 INCB 300`), проверяется в `117264` и `117400`. Мнемонику `PROH` приложение [З] относит к этой ячейке — см. §1.5, Д7 |
| 301 | | байт | **Признак ошибки** драйвера магнитофона, он же код ответа. Первичен по отношению к ячейке 321 блока параметров. [✓ ПЗУ]: `117250 MOVB #2,301` при несовпадении контрольной суммы, `116220 MOVB #4,301` в собственном обработчике вектора 4, `116236 MOVB 301,1(R1)` — копирование в байт «Ответ» блока параметров |
| 302 | `PRFCT` | сл. | Признак фиктивного чтения (≠ 0 при фиктивном чтении) |
| 304 | `INCADR` | сл. | **Шаг смещения байтов при чтении** [Рад] / **инкремент адреса массива.** При чтении всегда 1 (при фиктивном — 0). Изменение приводит к характерному дефекту загрузки: косая «сетка» на экране и порча байтов. Копировщик HELP7 устанавливает 177777 (−1) для загрузки в экранное ОЗУ «в обратном порядке» |
| 306 | `ABP` | сл. | Буфер адреса блока параметров `EMT 36` (дублирует R1) |
| 310 | `BUFSP` | сл. | Буфер указателя стека: сюда заносится и отсюда восстанавливается вершина стека при работе драйвера |
| 312 | `BUFKS` | сл. | **Буфер контрольной суммы** файла (при чтении и при записи). Также используется как буфер загрузки при фиктивном чтении |
| 314 | `GRDL0` | сл. | **Пороговое значение**, измеренное по входной настроечной последовательности [Рад]. Импульс короче порога — нуль, длиннее — единица. Порог берётся **в полтора раза бо́льшим длительности логического нуля**. Годится для приблизительной оценки скорости записи |
| 316 | | сл. | **Не используется.** Единственная свободная ячейка, содержимое которой не меняется при `EMT 14` (кроме ячеек стека). Автор DMBK-описания просил оставить её для системных драйверов |
| 320…371 | `BPDMAG` | | **Блок параметров драйвера магнитофона** — см. [10-магнитофон.md](10-магнитофон.md) |
| 372…777 | | | **Стек** |

> В ANDOS v3.1 специально предусмотрены ещё три пары ячеек с тем же свойством
> «переживать `EMT 14`» (в каждой паре первая хранит значение, вторая — адрес, по которому
> оно должно быть занесено): `@#120244`/`@#120246`, `@#120250`/`@#120252`,
> `@#120254`/`@#120256`.

> **Что лежит сразу за системной областью.** Адреса от `1000` формально свободны, но
> у каждой системной программы там своя рабочая область, и её нельзя занимать, не
> зная последствий. У **Бейсика** это `2000`…`3051`: переменные компилятора, таблица
> адресов `USR`, тексты функциональных клавиш и буферы ввода-вывода; текст программы
> пользователя начинается только с `3052`. Полная карта — в
> [16-бейсик.md](16-бейсик.md), §16.4.

---

## 7.2. Стек

* Отсчёт ведётся **«сверху вниз»**: начало — адрес 777 (в чётных адресах 776).
* **Заводское руководство** (разд. 4): «стек может нарастать от ячейки с адресом 776
  до ячейки с адресом **300**. Однако при работе с драйвером магнитофона следует иметь
  в виду, что на время работы драйвер использует ячейки с адресами **300–352**».
  Указатель стека устанавливается на адрес **1000** при запуске микро-ЭВМ пусковым
  монитором; если объёма стековой области недостаточно, программа может установить SP
  на другое значение.
* Ю. А. Зальцман называет «дном» адрес 372 (с возможностью продления до 320) — это более
  осторожная оценка, учитывающая весь блок параметров 320…371. Практический вывод один
  и тот же: **ниже 372 стек безопасен, только если магнитофон не используется**.
* Глубина стека, требуемая самому драйверу магнитофона, не превышает 16 байт — это
  позволяет размещать блок параметров в зоне 320…371, не мешая драйверу.
* Регистр SP отличается от остальных РОН тем, что его автоинкремент/автодекремент **всегда
  на 2**, независимо от того, работает команда со словом или с байтом.
* Обмен — по принципу «первым вошёл — последним вышел».

**Правило.** Адрес вершины стека должен восстанавливаться при работе отдельных ветвей
программы. Если занести что-то в стек до ветвления, надо либо восстановить вершину до него,
либо предусмотреть одинаковое восстановление в каждой ветви. Нарушение приводит к тому,
что стек постепенно засоряется и затирает системную область. Особенно опасно нарушение
вершины **внутри подпрограммы или обработчика прерывания**: адрес возврата хранится в стеке,
и возврат станет невозможен.

### Использование «дна» стека под переменные

Большинству программ такая глубина стека не нужна — достаточно дна по адресу 600, иногда
даже 700. Всю область ниже можно рассматривать как свободные системные ячейки.

Так поступает ассемблер-система **МИКРО.10К**, размещая переменные в 400…600:

| Адрес | Что хранится |
| --- | --- |
| 402 | Адрес начала текста редактора |
| 404 | Адрес текущего конца текста редактора |
| 426 | Текущий адрес метки |
| 432 | Текущий адрес курсора |
| 446 | Адрес начала текущей страницы текста |
| 470 и далее | Буфер маски поиска |

Благодаря такой организации МИКРО.10К, не имея внутри себя буфера переменных, одинаково
работает и в ОЗУ пользователя, и в дополнительном ОЗУ, и в ПЗУ.

Аргументы против такого приёма: если в памяти работают две программы, использующие одни и
те же ячейки, они помешают друг другу. Аргументы за: одновременная работа нескольких
программ на БК редка, а программу, рассчитанную на работу в ПЗУ, переменные больше девать
некуда.

#### Настройка буферов МИКРО.10К

Если обозначить адрес загрузки ассемблер-системы за 0, то:

| Смещение | Что задаёт |
| --- | --- |
| 6 | Адрес начала текста редактора |
| 32 | Адрес начала загрузочного модуля |
| 36 | Тот же адрес минус 1000 (для вычисления адресов меток) |

Исходные значения в МИКРО.10К-РП / МИКРО.10-01К: текст с 17001, загрузочный модуль
с 13000 (то есть 20000 байт под текст, из них последние 1000 — таблица меток, и 4000 под
загрузочный модуль).

При размещении МИКРО.10К в ПЗУ (например, с 140000) целесообразно: текст — 10001,
загрузочный модуль — 1000, третья константа — 0. Если МИКРО используется только как
текстовый редактор — текст с 1001, две другие константы не менять.

Единица в адресе начала текста не обязательна: это опознавательный признак текстового файла
МИКРО.10К, и перед буфером текста желательно иметь свободный байт, куда при запуске
записывается код 12. **После изменения констант МИКРО.10К нужно перезапустить с адреса
загрузки.**

### Перенос стека

Иногда стек в 372…777 мешает. Пример: скопировать файл длиной 77400 с адресом загрузки 400
(такие игры существуют — это своеобразный слабый способ защиты от копирования). Обычный
копировщик в ОЗУ не поместится, копировщик в дополнительном ОЗУ или ПЗУ упрётся в стек.
Решение — копировщик, размещаемый, например, с 140000 и **переносящий туда же стек**:
достаточно изменить содержимое SP.

## 7.3. Автозапуск программ

При обращении к драйверу магнитофона по `EMT 36` в стек заносится адрес команды, следующей
за вызвавшей прерывание. Если загружаемая программа имеет адрес загрузки **меньше 1000**
и содержит в начале блок слов с адресом запуска, этот адрес в стеке будет подменён.
После возврата из прерывания (`RTI`) он попадает в PC — программа стартует сама.

| Режим | Исходная вершина стека | Куда обычно заносят адрес автозапуска |
| --- | --- | --- |
| МСД | 732 | 732 |
| ПМ | 1000 | 776 или 766 |

Автозапуск по ячейке 766 можно задать прямо в МСД, записав затем файл с адреса 760.
Для других ячеек нужны специальные средства — например, копировщики HELP, HELP3, КОПИРАК.
Обычно такие средства заполняют адресом запуска всю область от 732 (или ниже) до 1000,
что обеспечивает автозапуск из любого режима.

### Недостатки автозапуска

1. Программа запускается **без проверки на ошибки чтения**.
2. `EMT 36` завершается «нештатным» образом, и **дистанционное управление магнитофоном
   не отключается**, если в запущенной программе нет команды, делающей это (например,
   `EMT 14`).

Существуют способы автозапуска, свободные от этих недостатков: в начало программы
помещается блок, размещаемый при загрузке ещё «глубже» в стеке; автозапуск передаёт
управление ему, а блок доделывает всё необходимое, в том числе перемещает загруженную
программу по рабочему адресу.

> Автозапуск — коварный приём, снабжать им все программы подряд не рекомендуется.
> Особенно это касается системных и инструментальных программ, которым может требоваться
> обязательный «штатный» выход из режима загрузки: запись переменных, байта ответа
> `EMT 36`, восстановление стека.
