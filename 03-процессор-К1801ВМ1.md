# 3. Процессор К1801ВМ1

> **Источники главы**
> * **[ВМ1]** «Однокристальный микропроцессор К1801ВМ1. Техническое описание», версия 1.2
>   от 24.10.2020, 151 с. → `docs/cpu11/vm1/doc/1801vm1.pdf`.
>   Составлено на основе **реверс-инжиниринга кристалла** (проект `cpu11`): вскрытие
>   корпуса, панорамная съёмка слоёв металла и диффузии, векторизация топологии,
>   восстановление схемы на уровне вентилей, Verilog-модели, прогон заводских тестов.
>   Официального технического описания К1801ВМ1 в открытом доступе не существует —
>   это единственный полный документ.
> * `docs/cpu11/readme.md`, `docs/cpu11/vm1/readme.md` — обзор проекта, сравнение
>   производительности процессоров.
> * **[З]**, **[ЭМ]** — для сопоставления с наблюдаемым поведением БК.

Эта глава описывает **сам кристалл**. Всё, что касается того, как БК-0010-01 использует
процессор, — в главах [02-архитектура.md](02-архитектура.md),
[04-система-команд.md](04-система-команд.md),
[05-системные-регистры.md](05-системные-регистры.md).

---

## 3.1. Общая характеристика

К1801ВМ1 — однокристальный 16-разрядный микропроцессор, разработанный в начале 1980-х.
**Это не клон какого-либо процессора DEC**: архитектура оригинальная, управляющий автомат
построен на программируемой логической матрице (ПЛМ). Реализован базовый набор команд
PDP-11 плюс команды **SOB и XOR** из набора расширенной арифметики (EIS).
Диспетчера памяти (MMU) нет.

### Технические характеристики [ВМ1 § 3]

| Параметр | Значение |
| --- | --- |
| Представление чисел | Дополнительный код с фиксированной запятой |
| Система команд | Безадресная, одноадресная, двухадресная |
| Виды адресации | Регистровая, косвенно-регистровая, автоинкрементная, косвенно-автоинкрементная, автодекрементная, косвенно-автодекрементная, индексная, косвенно-индексная |
| Число РОН | 8 |
| Число уровней прерываний | **4** |
| Системная магистраль | Типа МПИ (Q-bus) |
| Адресное пространство | 64 Кбайт |
| Тактовая частота, минимум | **100 кГц** для всех вариантов (ограничение снизу — часть защёлок построена на ёмкости затворов n-МОП транзисторов) |
| Тактовая частота, максимум | 4,7 МГц (ВМ1**А**), 3,5 МГц (ВМ1**Б**), 2,5 МГц (ВМ1**В**), 4,7 МГц (ВМ1**Г**) |
| Максимальное быстродействие | **до 500 тыс. регистровых операций в секунду** |
| Напряжение питания | Однополярное +5 В ±10 % |
| Многопроцессорность | **до 4 процессоров на единой магистрали** |
| Технология | n-МОП, **проектная норма 5 мкм**, ширина затвора ~3 мкм, **нагрузки обеднённого типа**, один слой металла и один слой поликремния, **самосовмещённые затворы** |
| Размер кристалла | 5 × 5 мм |
| Число транзисторов | ~16 650 |
| Потребляемая мощность | до 1,2 Вт |
| Корпус | Металлокерамический 429.42 либо пластиковый 2204.42-3 |

Внутреннего ODT (пультового отладчика в кристалле) нет; внутрисхемная отладка
поддержана специальным **пультовым режимом** и внешним ПЗУ.

> ⚠ **Две цифры быстродействия у одного проекта.** Техническое описание [ВМ1 § 3]
> даёт **500 тыс. регистровых операций/с** и предельную частоту 4,7 МГц, а сводка
> самого проекта `cpu11` — **625 тыс. регистр-регистровых команд/с при 5 МГц/5 В**.
> Это соответствует 9,4 и 8,0 тактам на операцию, то есть расхождение не сводится
> к пересчёту частоты. В справочнике принята цифра подробного технического описания
> (см. §1.5, Д9). На разбор расхождения **Г2** это не влияет: измеренные в БК
> ≈250 тыс. оп./с заметно ниже любой из двух оценок.

> **Быстродействие: чип против машины.** Потолок кристалла — 500 тыс. регистровых
> операций/с. В БК-0010 при 3 МГц измеренное [З] значение — **≈250 тыс. оп./с**
> (основное время команды 4,0 мкс). Разница объясняется тем, что контроллер ОЗУ БК
> совмещён с контроллером телевизора: независимая схема регенерации читает слова
> из того же ОЗУ для вывода на экран, а арбитр синхронизирует её с обращениями
> процессора [МПСС]. Процессор простаивает в ожидании шины.
> Это снимает вопрос по расхождению **Г2** (см.
> [01-источники-и-соглашения.md](01-источники-и-соглашения.md), §1.5).

## 3.2. Варианты К1801ВМ1 и их различия

Первоначально предполагалось, что буквенные индексы — результат сортировки по частоте
и работоспособности блоков после заводских тестов. Это оказалось верно лишь отчасти:

> **К1801ВМ1Г выпускалась по другим фотошаблонам слоя диффузии и содержит
> микропрограмму, отличную от микропрограммы К1801ВМ1А** [ВМ1 § 1].

| Признак | ВМ1А / ВМ1Б / ВМ1В | **ВМ1Г** |
| --- | --- | --- |
| Команда `MUL` | Нет | **Есть** [ВМ1 § 8.6] |
| Блок встроенного таймера | Работоспособен **не на всех экземплярах** | **Гарантированно работоспособен** |
| Прерывание от таймера (вектор 270) | Заблокировано | **Поддерживается** |
| Число вентилей | 16 632 (ВМ1А) | 16 646 |
| Входов логической матрицы прерываний | 16 | 17 |

Именно ради добавления `MUL` и была переработана микропрограмма.

### Маркировка [ВМ1 § 11]

Вариант чипа определяется **точками на корпусе**, но их значение менялось трижды:

| Редакция ТУ | Период | Маркировка |
| --- | --- | --- |
| Начальное ТУ бК0.348.570-02ТУ-81 | 1.12.81 — 31.12.83 | ВМ1А — одна точка; ВМ1Б — две |
| Изменение ТУ № 3 от 1.09.83 | 1.01.84 — 31.12.89 | ВМ1А — одна; ВМ1Б — две; ВМ1В — три; **ВМ1Г — четыре** |
| Изменение ТУ № 8 от 21.04.89 | с 1.01.90 | ВМ1А — без точек; ВМ1Б — одна; **ВМ1Г — две** |

Практический вывод: **по числу точек нельзя судить о варианте, не зная года выпуска**.
Две точки на чипе 1985 года — это ВМ1Б, а на чипе 1991 года — ВМ1Г.

## 3.3. Периферийный блок процессора

Внутри микросхемы имеется **периферийный блок**, подключённый к внешним выводам МПИ
и играющий роль встроенного ведомого устройства магистрали. Его регистры доступны
по шине как внутреннему процессорному блоку, так и **внешнему агенту** — например,
другому процессору в многопроцессорной системе.

**Базовый адрес зависит от физического номера процессора**, задаваемого уровнями
на входах nPA0/nPA1:

| nPA1, nPA0 | Номер процессора | Диапазон адресов |
| --- | --- | --- |
| 1, 1 | 0 | **177700…177716** |
| 1, 0 | 1 | 177720…177736 |
| 0, 1 | 2 | 177740…177756 |
| 0, 0 | 3 | 177760…177776 |

В БК процессор один и имеет номер 0, поэтому база — **177700**.

### Карта смещений

| Смещение | Адрес в БК | Регистр | Обслуживается |
| --- | --- | --- | --- |
| 000 | 177700 | `CPU_MODE` — регистр управления режимом | внутренними схемами |
| 002 | 177702 | `CPU_IVEC` — регистр вектора прерывания рестарта | внутренними схемами |
| 004 | 177704 | `CPU_ERROR` — регистр флагов ошибок | внутренними схемами |
| 006 | 177706 | `TVE_LIMIT` — регистр перезагрузки/захвата таймера | внутренними схемами |
| 010 | 177710 | `TVE_COUNT` — счётчик таймера | внутренними схемами |
| 012 | 177712 | `TVE_CSR` — регистр управления таймера | внутренними схемами |
| **014** | **177714** | **внешний регистр, сигнал nSEL2** | **внешними схемами** |
| **016** | **177716** | **внешний регистр, сигнал nSEL1** | **внешними схемами** |

> ### Почему 177714 и 177716 ведут себя «странно»
>
> Это **не регистры БК** и не регистры процессора. При обращении по смещениям 014 и 016
> процессор лишь вырабатывает активный низкий сигнал **nSEL2** / **nSEL1**, а данные
> должны выдать или принять **внешние схемы машины**. Сигналы фиксируются по
> ниспадающему фронту nSYNC и держатся весь цикл обмена.
>
> Отсюда и наблюдаемое поведение, которое [З] и [РСП] описывают как «два раздельных
> регистра — входной по чтению и выходной по записи»: в БК на эти сигналы повешены
> **физически разные** регистры ввода и вывода. Прочитать записанное невозможно
> не из-за прихоти, а потому что читается совсем другая микросхема.
>
> Отсюда же и **адрес начального пуска**: при аппаратном сбросе процессор читает
> стартовый адрес из **старшего байта регистра 177716** (см. §3.5), то есть из внешнего
> регистра nSEL1. В БК он зашит равным 100000.

При обращении к любому адресу диапазона периферийного блока процессор **всегда сам
генерирует nRPLY** — внешние схемы формировать ответ не должны.

### `CPU_MODE` — регистр управления режимом (177700)

Начальное значение 177740 (зависит от номера процессора).
Разряды 15…5 — только чтение (единицы). Доступны по чтению и записи:

| Разряд | Имя | Назначение |
| --- | --- | --- |
| 4 | PA1 | Физический номер процессора (чтение) |
| 3 | PA0 | То же |
| 2 | WT | Управление пуском |
| 1 | ST | подчинённых процессоров |
| 0 | HLT | в многопроцессорной системе |

Разряды 0–2 используются для синхронизации пуска ведомых процессоров главным:
блок регистров каждого процессора доступен любому другому вне зависимости от состояния
процессора-владельца.

### `CPU_IVEC` — регистр вектора прерывания рестарта (177702)

**Рудимент от микроконтроллера К1801ВЕ1.** Только запись; при чтении даёт 177777.

Замысел был такой: запись в регистр сохраняет значение и взводит запрос на прерывание;
пока запрос не обработан, регистр «пропадает» с шины (на обращение к нему не выдаётся
nRPLY), и внешний агент может так узнать, что процессор-владелец ещё не обработал
прерывание.

**Ни ВМ1А, ни ВМ1Г полный функционал не реализуют** — запрос просто не подключён к блоку
обработки прерываний. При записи произвольных данных регистр действительно исчезает
из адресного пространства, и вернуть его штатно нельзя.

> **Аномалия:** процессоры К1801ВМ1 **ошибочно сбрасывают этот запрос при обработке
> инструкции `EMT`**, после чего регистр снова становится доступен на шине.

### `CPU_ERROR` — регистр флагов ошибок (177704)

Начальное значение 177440 (все флаги очищены). Единица во флаге означает, что
соответствующая ошибочная ситуация имела место.

| Разряд | Имя | Значение |
| --- | --- | --- |
| 0 | `DBL` | Двойная ошибка — повторная установка UOP, QTO, OAT или ER7 |
| 1 | `UOP` | Недопустимый код команды; вызывает сброс микропрограммного автомата |
| 2 | `ER2` | Резервный, исключений не вызывает, микрокодом не устанавливается |
| 3 | `ER3` | Резервный, исключений не вызывает |
| 4 | `QTO` | Тайм-аут обращения к внешнему устройству («зависание шины») |
| 6 | `OAT` | Словное обращение по нечётному адресу. **Детектор нечётного адреса в К1801ВМ1 заблокирован аппаратно, поэтому бит никогда не устанавливается** |
| 7 | `ER7` | Резервный; исключений не вызывает, но может вызвать двойную ошибку |

**Все биты безусловно сбрасываются при загрузке нового кода команды** во внутренний
регистр инструкций. Поэтому регистр отражает ошибки **текущей** команды, и сам процессор
не может прочитать большинство флагов: успешное чтение регистра — это команда, которая
выполнилась без ошибок, и флаги уже сброшены. Но регистр может прочитать **внешний агент
на шине МПИ** — на этом строится внешняя диагностика.

## 3.4. Встроенный таймер

> Встроенный таймер К1801ВМ1 **унаследован от однокристального микроконтроллера
> К1801ВЕ1. Таймер является недокументированным, и некоторый процент микросхем
> К1801ВМ1А/Б/В имеют неработоспособный блок встроенного таймера.**
> — [ВМ1 § 6.4]

Это исчерпывающе объясняет наблюдение [З]: «поскольку таймер по техническим условиям
в состав БК-0010 не входит, то на некоторых экземплярах его регистры могут быть
неисправны, что не служит основанием для предъявления претензий заводу-изготовителю».
Таймер не входит в ТУ **на процессор**, а не только на машину.

### Состав блока

* 16-разрядный **декрементируемый счётчик** (`TVE_COUNT`);
* 16-разрядный **регистр значения перезагрузки и/или захвата** (`TVE_LIMIT`);
* **регистр управления** (`TVE_CSR`);
* предделитель входной процессорной частоты **CLC на 4**;
* дополнительный делитель CLC на **128** (образуется делением CLC/4 ещё на 32);
* **детектор ниспадающего фронта на входе nSP**.

Все внутренние управляющие схемы таймера тактируются частотой CLC/4. Предделитель
и делители **недоступны из программы, работают постоянно** и не сбрасываются ни при
обращении к регистрам таймера, ни при сбросе процессора по входу nDCLO.

Для внешней частоты nSP счётный такт формируется по ниспадающему фронту сигнала,
синхронизированному с CLC/4; поэтому входная частота не может превышать **CLC/8**,
а длительность любого полупериода должна быть больше периода CLC/4.

### `TVE_LIMIT` — регистр перезагрузки и захвата (177706)

16 разрядов, чтение и запись. Начальное значение **после включения питания не определено**
(зависит от того, в какое псевдослучайное состояние встанут триггеры).
**Сброс по входам nINIT и nDCLO состояние регистра не меняет.**

> **Любая запись процессора в `TVE_CSR` копирует `TVE_LIMIT` в `TVE_COUNT`** —
> независимо от режима.

### `TVE_COUNT` — счётчик (177710)

16 разрядов, только чтение; запись игнорируется.

### `TVE_CSR` — регистр управления (177712)

**Начальное значение 177400.** Разряды 8…15 — только чтение, читаются как единицы.

| Разряд | Имя | Назначение |
| --- | --- | --- |
| 0 | **`SP`** | Тактирование счётчика по ниспадающему фронту на входе **nSP**. Делители на 4 и 16 в этом случае на частоту не влияют |
| 1 | **`CAP`** | Режим **захвата**: по ниспадающему фронту nSP содержимое `TVE_COUNT` копируется в `TVE_LIMIT`, при этом генерируется событие таймера. Бит `RUN` тоже должен быть установлен. При `CAP` = 0 событие генерируется при переходе счётчика через ноль и выполняется загрузка счётчика из регистра предела |
| 2 | **`MON`** | Разрешить **мониторинг** события таймера — устанавливать бит `FL`. Для ВМ1Г этот же бит разрешает запрос прерывания от таймера |
| 3 | **`OS`** | **Однократный режим**: при достижении нуля бит `RUN` сбрасывается, таймер перезагружается и останавливается. Требует `CAP` = 0 |
| 4 | **`RUN`** | Запись 1 разрешает счёт (с копированием `TVE_LIMIT` → `TVE_COUNT`). Запись 0 останавливает счётчик (копирование происходит тоже). Сбрасывается аппаратно только событием перехода через ноль при сброшенном `CAP` |
| 5 | **`D16`** | Разрешить **делитель на 16** |
| 6 | **`D4`** | Разрешить **делитель на 4** |
| 7 | **`FL`** | Флаг события таймера (см. ниже) |

Делители 1/4 и 1/16 включаются **последовательно**.

> ⚠ **Это окончательно закрывает расхождение А2.** Разряд **05 — делитель на 16**,
> разряд **06 — делитель на 4**. Прозаическое описание у [З] («05 → ×4, 06 → ×16»)
> ошибочно; его собственная таблица кодов и реализация [ЭМ] верны.
>
> Кроме того, уточняются два имени: бит 0 — **не «STOP»**, а выбор тактирования
> от внешнего входа nSP (в БК вход не задействован, поэтому счёт прекращается —
> отсюда и наблюдаемый «останов»); бит 1 — **не «WRAPAROUND»**, а режим захвата
> (в нём счётчик не перезагружается и после нуля переходит к 177777 — отсюда
> наблюдаемый «непрерывный счёт»).

### Частота тактирования счётчика

| `D4` | `D16` | `SP` | Частота |
| --- | --- | --- | --- |
| × | × | 1 | вход nSP (максимум CLC/8) |
| 0 | 0 | 0 | **CLC/128** |
| 0 | 1 | 0 | CLC/2048 |
| 1 | 0 | 0 | CLC/512 |
| 1 | 1 | 0 | CLC/8192 |

> **Сходимость с измерениями [З].** При тактовой частоте БК 3 МГц базовый период
> счёта равен 128 / 3 000 000 = **42,67 мкс**. Зальцман, измерявший период на нескольких
> машинах, получил **≈42,9 мкс** с разбросом «в пределах долей процента, реже
> нескольких процентов». Совпадение независимо подтверждает и тактовую частоту БК,
> и корректность его методики.

### Как те же разряды выглядят «со стороны БК» [Мор]

Сводка А. Морозова описывает `TVE_CSR` не через назначение выводов кристалла,
а через **наблюдаемое поведение на БК**, где внешних сигналов таймера просто нет.
Полезно как перевод нормативных имён на язык практики:

| Разряд | [ВМ1] — назначение | [Мор] — наблюдаемое поведение на БК |
| :-: | --- | --- |
| 0 | `SP` — тактирование от внешнего входа nSP | «При записи 1 таймер **останавливается**» |
| 1 | `CAP` — режим захвата | «Если 1, то вслед за 0 всегда идёт `177777`, независимо от `177706` и режима одновибратора; разряд 7 не устанавливается» |
| 2 | `MON` — мониторинг события | «Если 1, устанавливать разряд 7 в конце периода» |
| 3 | `OS` — однократный режим | «Если 1, в конце периода остановить таймер» |
| 4 | `RUN` — счёт разрешён | «При записи 1 таймер запускается, счёт начинается со значения из `177706`» |
| 6, 5 | `D4`, `D16` — делители | «00 = F, 10 = F/4, **01 = F/16**, 11 = F/64» |
| 7 | `FL` — флаг события | «Если разряд 2 установлен, в конце периода устанавливается в 1; сбрасывается программно» |

Расхождений по существу нет — оба описания сходятся, если учесть, что **в БК нет
источника внешнего сигнала для входа nSP и нет сигнала захвата**:

* разряд 0 переводит счётчик на несуществующий внешний вход, и счёт прекращается —
  внешне это и есть «таймер остановился»;
* разряд 1 включает режим захвата, в котором содержимое `TVE_LIMIT` в счётчик
  не перезагружается, отчего после нуля счётчик уходит в `177777`.

Два независимых подтверждения из этого источника:

1. **Делители.** «01 = F/16, 10 = F/4» — то есть **разряд 5 делит на 16, разряд 6 на 4**.
   Это ровно то, чем [З] ошибается в прозаическом описании
   (см. [05-системные-регистры.md](05-системные-регистры.md), §5.4).
2. **«Старшие 8 бит читаются как 1»** — то есть `TVE_CSR` при сбросе читается как
   **177400**, что совпадает и с [ВМ1], и с реализацией [ЭМ] (`CPU.cpp`).

> Оговорка: под «F» здесь понимается **собственная базовая частота таймера**
> (тактовая частота ЦП, делённая на 128), а не тактовая частота процессора.

### Режимы работы

| `RUN` | `OS` | `CAP` | Режим |
| --- | --- | --- | --- |
| 0 | × | × | Остановлен |
| 1 | 0 | 0 | Непрерывный счёт с перезагрузкой счётчика |
| 1 | 1 | 0 | Однократный счёт с предзагрузкой счётчика |
| 1 | × | 1 | Непрерывный счёт **без** перезагрузки, захват значения счётчика по входу nSP |

Если установлены **оба** бита `OS` и `CAP`, таймер при переходе через ноль
не останавливается: установленный `CAP` выбирает генерацию события по фронту nSP.

### Флаг `FL` и его аномалия

`FL` устанавливается схемой таймера при наступлении события — перехода счётчика через
ноль либо захвата значения по nSP. Для установки требуется `MON` = 1.

Фактически при наступлении события **генерируется запрос на прерывание**, и при запуске
обработки прерывания запрос сбрасывается. **`FL` устанавливается только если возник новый
запрос, а старый ещё не был обработан.**

> Отсюда аномалия, которую [З] описал как эмпирический факт без объяснения:
>
> «Поскольку функция обслуживания прерывания от таймера в К1801ВМ1А заблокирована,
> бит `FL` будет реально установлен только **после второго срабатывания** таймера.
> После включения питания или сброса nDCLO при самом первом событии таймера данный бит
> не устанавливается. При втором и последующих событиях этот бит работает правильно,
> поскольку внутренний флаг запроса на прерывание уже установлен.» — [ВМ1 § 6.7]
>
> Сравните с формулировкой [З]: «при первом (после включения ЭВМ или системного сброса)
> запуске таймера в данном режиме индикация срабатывает только после второго перехода
> счётчика через 0, причём независимо от того, работал ли таймер до этого в других
> режимах». Наблюдение верное; причина — в кремнии.

В К1801ВМ1**Г** прерывание от таймера обрабатывается, и `FL` играет роль **флага
переполнения**: устанавливается, если событие возникло, а прерывание от предыдущего
ещё не обработано.

**Прикладные приёмы работы с таймером на БК** (измерение интервалов, генератор случайных
чисел, максимальные времена цикла, проверка исправности) —
в [05-системные-регистры.md](05-системные-регистры.md), §5.4.

## 3.5. Прерывания и исключения на уровне кристалла

Блок обработки прерываний построен на логической матрице: 16 входов у ВМ1А,
**17 у ВМ1Г** (добавлен вход от ВЕ-таймера), 11 выходов; результат — 3-разрядный
регистр `MCIR` и 4-разрядный `VSEL`.

Таблица исключений и прерываний **в порядке приоритета** [ВМ1 § 7.3]:

| № | Источник | Вектор | Сохранение PSW/PC | Маска |
| --- | --- | --- | --- | --- |
| 1 | Начальный пуск после аппаратного сброса (читается только стартовый адрес в **старшем байте** регистра `177716 + Ncpu×20`; PSW всегда 340) | 177716 + Ncpu×20 | — | — |
| 2 | Зависание шины при получении вектора прерывания (в цикле nIAKO) | 160012 | 177674 | — |
| 3 | Двойное зависание шины | 160006 | 177674 | — |
| 4 | Зависание шины: если PSW11 = 0 и PSW10 = 0 | 000004 | SP−4 | PSW11 |
| | если хотя бы один из PSW10, PSW11 ненулевой | 160002 | 177674 | PSW10 |
| 5 | Недопустимый код инструкции | 000010 | SP−4 | — |
| 6 | T-бит (разряд 4 PSW) | 000014 | SP−4 | WCPU |
| 7 | Пропадание питания (ниспадающий фронт nACLO) | 000024 | SP−4 | PSW10 |
| 8 | Аппаратный останов, переход в пультовой режим (низкий уровень на nIRQ1) | 160002 | 177674 | PSW10, PSW11 |
| 9 | **От ВЕ-таймера — только в К1801ВМ1Г** | **000270** | SP−4 | PSW10, PSW7, `MON` |
| 10 | Ниспадающий фронт на nIRQ2 | 000100 | SP−4 | PSW10, PSW7 |
| 11 | Ниспадающий фронт на nIRQ3 | **000270** | SP−4 | PSW10, PSW7 |
| 12 | Векторное прерывание от ВУ (низкий уровень на nVIRQ) | внешний | SP−4 | PSW10, PSW7 |
| 13 | Инструкция `HALT` | 160002 | 177674 | — |
| 14 | Инструкция `BPT` | 000014 | SP−4 | — |
| 15 | Инструкция `IOT` | 000020 | SP−4 | — |
| 16 | Инструкция `EMT` | 000030 | SP−4 | — |
| 17 | Инструкция `TRAP` | 000034 | SP−4 | — |

Существенное для программиста БК:

* **Пультовый режим.** Векторы `160002`, `160006`, `160012` и сохранение PSW/PC
  по адресу `177674` — механизм внутрисхемной отладки. В БК по адресам 160000+
  находится ПЗУ МСТД, поэтому переход в пультовой режим попадает в тест-систему.
  `HALT` — **единственное программное исключение, вызывающее переход в пультовой режим**;
  остальные пультовые исключения аппаратные.
* **Вектор 270 разделяют два источника** — прерывание от ВЕ-таймера (ВМ1Г) и
  ниспадающий фронт на nIRQ3. В БК аппаратно не задействован ни тот, ни другой, поэтому
  ячейки 270/272 системной области свободны (см.
  [07-системная-область.md](07-системная-область.md)).
* **Маскирование.** Прерывания от устройств (nIRQ2, nIRQ3, ВЕ-таймер, nVIRQ)
  запрещаются разрядом **PSW7** либо **PSW10**. Разряды **PSW10 и PSW11** участвуют
  в управлении обработкой зависания шины и пультового режима. Таким образом, вопреки
  распространённому «в БК используется только младший байт PSW», старший байт PSW
  на уровне кристалла значение имеет — просто монитор БК его не задействует.
* **`WAIT` и T-бит.** В режиме ожидания по команде `WAIT` T-бит не вызывает немедленного
  исключения: процессор ждёт прерывания, и любое незамаскированное (ACLO, IRQ1, TVE,
  IRQ2, IRQ3, VIRQ) выводит из режима с переходом по своему вектору.
* Прерывания по nIRQ1 и nVIRQ **чувствительны к уровню, а не к фронту**: если уровень
  не снят после обработки, немедленно возникает новый запрос.

Если прерывание использует вектор вида `1600xx`, то перед сохранением PSW и PC
устанавливается разряд 3 по адресу `177716` (**этот адрес не зависит** от физического
номера процессора).

## 3.6. Микропрограммное управление

Процессорный блок состоит из блока микропрограммного управления (ПЛМ + автомат),
операционного блока и блока обработки прерываний. Каждая инструкция реализуется
цепочкой микроадресов; недопустимые коды приводят к сбросу автомата и исключению
по вектору 10.

Подробности — таблица микроадресов, цикл автомата, процедуры начального старта и входа
в исключения, реализация `MUL` в ВМ1Г — в **[ВМ1] § 7, § 8**. Покомандные цепочки
микроадресов приведены в **[ВМ1] § 9** для каждой инструкции.

## 3.7. Обнаруженные аппаратные ошибки [ВМ1 § 10]

### Ошибка флага C после `MOVB` и `MFPS`

**Флаг переноса `C` трактуется командами условного перехода как сброшенный после команд
`MOVB xx,Rd` и `MFPS Rd`** (только с регистровым режимом адресации приёмника), хотя
в реальном PSW флаг устанавливается корректно.

Ошибка локализована по микроадресу `0x51` (для ВМ1А): на нём выполняется знаковое
расширение байта в регистр результата, и временное значение `C` в промежуточном регистре
флагов оказывается нулевым — а именно его использует следующая команда условного перехода.

```asm
        SEC                     ; установить флаг C
        MOVB    R1,R0           ; флаг C измениться не должен
        BCC     1$              ; переход БУДЕТ выполнен — неверно
```

Значение в самом PSW корректно, что легко проверить:

```asm
        SEC
        MOVB    R1,R0
        MFPS    R2              ; бит 0 в R2 будет единичным — верно
        BCC     1$              ; переход БУДЕТ выполнен — неверно
```

**Обходной приём:** при выполнении любой другой команды промежуточный регистр флагов
перезаписывается из реального PSW, и переходы начинают работать правильно:

```asm
        SEC
        MOVB    R1,R0
        NOP                     ; ← достаточно любой команды
        BCC     1$              ; переход не будет выполнен — верно
```

Микроадрес `0x51` используется также командой `SWAB Rd` (для выставления N и Z
по младшему байту), но `SWAB` и так предполагает сброс `C`, поэтому там ошибка
не проявляется.

### Прочие аномалии, отмеченные в [ВМ1]

* Детектор обращения по нечётному адресу **заблокирован аппаратно**: флаг `OAT`
  в `CPU_ERROR` никогда не устанавливается (§3.3).
* Запрос от регистра вектора рестарта ошибочно сбрасывается при обработке `EMT` (§3.3).

## 3.8. Материалы проекта cpu11

Каталог `docs/cpu11/` — репозиторий результатов реверс-инжиниринга советских
PDP-11-совместимых процессоров. Помимо документации на ВМ1 содержит:

| Каталог | Содержание |
| --- | --- |
| `vm1/doc/` | **Техническое описание** (`1801vm1.pdf`, `1801vm1.docx`) и диаграммы циклов шины |
| `vm1/img/` | Фотография кристалла; ссылки на панорамные снимки: ВМ1**А** — металл 19K×19K (524 МБ) и диффузия 12K×12K (202 МБ), ВМ1**Г** — металл 19K×19K (525 МБ) и диффузия 18K×18K (472 МБ) |
| `vm1/hdl/org/` | Асинхронная Verilog-модель, максимально близкая к схеме на уровне вентилей (только для моделирования — содержит защёлки) |
| `vm1/hdl/syn/` | Синхронная модель с шиной Q-Bus; тайминги совпадают с оригиналом с точностью до полупериода. Пригодна для построения замены реального чипа |
| `vm1/hdl/wbc/` | Синхронная модель с интерфейсом Wishbone для SoC |
| `vm1/tst/` | **Заводские тесты**: `t401.mac`, `t404.mac`, `test.mac`, **`tve1.mac` — тест встроенного таймера**, `pitest.mac` (вычисление 1000 знаков π) |
| `vm2/`, `vm3/`, `lsi/`, `f11/`, `t11/`, `am4/` | То же для 1801ВМ2, 1801ВМ3, LSI-11 (581-я серия, клон DEC LSI-11 на наборе Western Digital MCP-1600), F-11 (1811-я серия, клон DEC DC302/303/304 «Fonz»), **T-11 (1807-я серия, клон DEC DC310, кодовое имя «Tiny»)**, M4 (советская реплика военной LSI-11M на разрядно-модульных секциях AMD Am2900) |
| `xen/` | Проекты для FPGA-плат (Quartus, ISE, Gowin) |

**Топология и схемы вынесены в отдельный репозиторий** `1801BM1/cad11`: топология
в формате Sprint Layout 6.0 (экспортируется в Gerber) и в формате PCAD, восстановленные
схемы на уровне вентилей в PCAD и PDF.

### Как запустить модель

* **Моделирование в ModelSim:** `tst\build.bat t401` — собрать образ тестового ПО,
  затем в ModelSim сделать рабочим каталогом `vm1/hdl/org/sim/de0` и выполнить
  `do run.do`.
* **Icarus Verilog:** в каждом каталоге процессора есть `run_iverilog.sh`; список
  файлов проекта — в `iverilog.cf`. Рекомендуется `iverilog` версии 12,
  но достаточно и версии 10 из Ubuntu 20.04.

### Ресурсы ПЛИС для Wishbone-ядра ВМ1А

Оценки средств синтеза (Quartus/XST/Vivado), сбалансированная оптимизация
площадь/скорость, медленная модель, худший угол:

| Плата | ПЛИС | Fmax | LUT | Триггеров |
| --- | --- | --- | --- | --- |
| DE1 | Cyclone II | 94 МГц | 1410 | 464 |
| DE0 | Cyclone III | 111 МГц | 1420 | 427 |
| DE2-115 | Cyclone IV | 111 МГц | 1370 | 425 |
| DE10-Lite | Max 10 | 82 МГц | 1373 | 469 |
| QC5 | Cyclone V | **131 МГц** | **693** | 384 |
| QC10 | Cyclone 10 | 108 МГц | 1349 | 694 |
| AX309 | Spartan 6 | 72 МГц | — | 387 |
| QA7 | Artix 7 | 124 МГц | 1298 | 669 |
| QK7 | Kintex 7 | **181 МГц** | 1301 | 633 |
| EG4 | Eagle EG4S20 | 95 МГц | 1810 | 621 |

Показательно: **весь процессор укладывается примерно в 1400 LUT и один блок
памяти**, а на современной ПЛИС работает на частоте в 15–35 раз выше исходных
3 МГц БК.

### Демонстрационная система в примерах проектов

Состав отладочной системы, собираемой вокруг ядра, любопытен тем, насколько он
совпадает с решениями БК:

| Ресурс | Адрес / вектор |
| --- | --- |
| ОЗУ 8К × 16 | с `000000`, содержимое из `test.mif` |
| Терминал (ТТY) | `177560`, векторы `60`/`64`, 115200/8/N/1 |
| Системный таймер 50 Гц | **IRQ2, вектор `100`**, по фронту; включается движком платы |
| Семисегментный индикатор | выходные регистры `177714`/`177715` |
| Движки и кнопки | входной регистр `177714` |

> Совпадение не случайно: **вектор `100` для прерывания 50 Гц и адрес `177714`
> для параллельного порта — конвенция 1801-й серии**, а не изобретение БК
> (ср. [05-системные-регистры.md](05-системные-регистры.md), §5.5, и
> [12-разъёмы-и-периферия.md](12-разъёмы-и-периферия.md), §12.4). Векторы `60`/`64`
> у ТТY соответствуют тем же векторам клавиатуры в БК.

### Сравнительная производительность (тест π, 1000 знаков)

Тест — вычисление 1000 знаков π по алгоритму spigot. Значения — время в тиках 50 Гц,
меньше — быстрее. Столбец **т/мк** — тактов ядра на одну микрокоманду:

| Модель | Без EIS | Только MUL | MUL/DIV | т/мк |
| --- | --- | --- | --- | :-: |
| LSI-11 | 746 | 422 | 284 | ×1 |
| F-11 | 693 | 429 | 323 | ×2 |
| **1801ВМ1А** | **586** | — | — | ×2 |
| **1801ВМ1Г** | **588** | **458** | — | ×2 |
| M4 | 532 | 275 | 154 | ×1 |
| 1801ВМ3 | 388 | 205 | 137 | ×1 |
| 1801ВМ2 | 340 | 190 | 123 | ×2 |

**Условия испытания** (важны, иначе таблица читается неверно): все модели —
Wishbone-совместимые, одна и та же плата DE0 и одинаковая конфигурация системы,
ПО в статической памяти без тактов ожидания, **4 минимальных такта на цикл шины**.
Последнее — узкое место для 1801ВМ3, который способен выбирать и исполнять
регистр-регистровую команду каждые 3 цикла. Частоты **не у всех 100 МГц**:
LSI-11 работал на 80 МГц (результат умножен на 0,8), M4 — на 50 МГц (умножен на 0,5).

Показательно: ВМ1 — не самый медленный из ряда; он опережает и LSI-11, и F-11,
хотя и заметно уступает более поздним ВМ2 и ВМ3, которые выполняют предвыборку команд.
Причём опережает, тратя **вдвое больше тактов на микрокоманду**, чем LSI-11.
Колонка «только MUL» для ВМ1Г подтверждает наличие у него аппаратного умножения.

> Столбец «т/мк» отражает устройство **синхронных моделей**, а не исходных кристаллов:
> у LSI-11 четыре фазы `c1`–`c4` сведены к одному такту ядра, у F-11 — к двум.
