# 6. Системные регистры

> **Источники главы**
> * **[РСП § 4.1]** «Системные регистры» (с. 13–16) — нормативное побитовое описание
>   регистров 177660, 177662, 177664, 177714, 177716.
> * **[З]** «Системные регистры» — **единственный источник по системному таймеру**
>   (177706/177710/177712), которого нет ни в одном заводском руководстве;
>   а также разбор ошибки РСП по разряду 09 регистра 177664.
> * **[ЭМ]** `bkemu-QT/devemu/Board.cpp`, `CPU.cpp` — побитовые комментарии, делители таймера.

> **Важное уточнение [ВМ1 § 6].** Адреса **177700…177716** — это не устройства БК,
> а **периферийный блок самого процессора К1801ВМ1** (база = `177700 + <номер
> процессора> × 20`; в БК процессор один, номер 0). Смещения 000…012 обслуживаются
> внутренними схемами кристалла, а **014 (177714) и 016 (177716) — внешние регистры**,
> выбираемые сигналами **nSEL2** и **nSEL1**, которые процессор лишь вырабатывает;
> данные выдают и принимают схемы машины.
>
> Отсюда всё «странное» поведение этих двух адресов — см. §6.5 и §6.6, а также
> [04-процессор-К1801ВМ1.md](04-процессор-К1801ВМ1.md), §4.3.
>
> В БК не используются, но физически существуют: `177700` — регистр управления режимом,
> `177702` — регистр вектора прерывания рестарта, `177704` — регистр флагов ошибок.

Область системных регистров занимает адреса **177600…177777** (100 слов = 64д регистра),
но в БК-0010 задействована лишь малая часть. Каждый регистр — 16 разрядов; отдельные
разряды могут быть доступны только по чтению, только по записи, по чтению и записи,
либо не использоваться вовсе.

Принципиальное отличие от ячеек ОЗУ: разряды системных регистров связаны с внешним миром
напрямую — с клавиатурой, магнитофоном, ТЛГ-линией, портом УВВ, физическим временем.

Сводка:

| Адрес | Регистр | Доступ |
| --- | --- | --- |
| 177660 | Состояние клавиатуры | разряд 06 — Ч/З, разряд 07 — Ч |
| 177662 | Данные клавиатуры | разряды 00–06 — Ч |
| 177664 | Регистр смещения (скроллинг) | Ч/З |
| 177706 | Таймер: регистр начального значения | Ч/З |
| 177710 | Таймер: реверсивный счётчик | Ч |
| 177712 | Таймер: регистр управления | Ч/З (мл. байт) |
| 177714 | Параллельный порт УВВ | два раздельных регистра: ввод (Ч) и вывод (З) |
| 177716 | Системный регистр (ВР1/SEL1) | два раздельных регистра: ввод (Ч) и вывод (З) |

---

## 6.1. Регистр состояния клавиатуры — 177660

| Разряд | Доступ | Назначение |
| --- | --- | --- |
| 06 (0100) | Ч/З | Маска прерываний от клавиатуры. **0 — прерывание разрешено, 1 — запрещено.** Начальное состояние 0 |
| 07 (0200) | Ч | Флаг готовности. Устанавливается в 1 при поступлении нового кода в регистр данных (177662), сбрасывается в 0 при чтении 177662 |
| прочие | Ч | 0 |

Разряд 07 удобно проверять командой `TSTB @#177660` + `BPL`/`BMI`, так как для байта
седьмой разряд является знаковым.

При обычной работе разряд 07 практически всегда читается как 0: системный драйвер
клавиатуры успевает прочитать код раньше пользовательской программы. Опрос имеет смысл
только при запрещённом прерывании.

Пример работы в режиме опроса, без прерываний:

```asm
        MOV     #100,@#177660   ; запретить прерывания от клавиатуры
        ...
        TST     @#177662        ; снять готовность (очистить буфер от дребезга)
M:      TSTB    @#177660        ; ждать
        BPL     M               ; поступления кода
        MOVB    @#177662,R0     ; принять код
```

## 6.2. Регистр данных клавиатуры — 177662

| Разряд | Доступ | Назначение |
| --- | --- | --- |
| 00…06 | Ч | 7-разрядный «сырой» код нажатой клавиши. На него **не влияют** регистровые клавиши «АР2»/«НР» и «РУС» |
| 07…15 | — | Не используются |

Новый код не записывается в регистр, пока не прочитан предыдущий.

Полные коды клавиатуры (с учётом регистров РУС/ЛАТ/СТР/ЗАГЛ/СУ/АР2) формируются
**программно** драйвером клавиатуры; уже обработанный код помещается в ячейку `104`.

Этот регистр широко используется для чтения клавиатуры независимо от текущего регистра
клавиатуры и для ввода команд «на ходу», без остановки работающей программы.

> **Замечание для БК-0011М.** В БК-0011(М) разряд 14 этого регистра при записи управляет
> разрешением прерывания от внешнего таймера 50 Гц. На БК-0010 **запись в 177662
> запрещена** — это был один из способов программно отличить БК-0010 от БК-0011
> (см. комментарий в `bkemu-QT/devemu/Board.cpp`, обработчик `case 0177662`).

## 6.3. Регистр смещения (рулон) — 177664

| Разряд | Доступ | Назначение |
| --- | --- | --- |
| 00…07 | Ч/З | Смещение начала экранного ОЗУ в телевизионных строках (по 100 = 64д байт каждая) |
| 09 (01000) | Ч/З | **1 — обычный режим (полный экран 256 строк), 0 — режим РП** (сокращённый экран, 1/4) |
| 08, 10…15 | — | Не используются |

Начальное значение регистра после включения — **1330** (разряд 9 = 1, смещение = 330).
Изменение младшего байта на 1 сдвигает изображение по вертикали на одну телевизионную
строку — так реализуется плавный вертикальный скроллинг. Одна символьная строка занимает
12 (10д) телевизионных строк, поэтому «штатный» сдвиг на строку текста меняет регистр на 12.

Служебная строка занимает 2000 байт видеопамяти (обычная строка текста — 1200 байт),
из-за чего число сдвигов до возврата начала экрана к адресу 40000 не кратно 24д, и младшие
разряды регистра при работе почти всегда разные. Служебная строка при рулонном сдвиге
смещается вместе со всем экраном, поэтому прямое использование регистра для скроллинга
требует специальных приёмов формирования изображения.

В режиме РП экран организован иначе: служебная строка (начало экрана) всегда имеет
адрес 70000, рулонное смещение не используется, регистр содержит константу **230**
(разряд 9 сброшен).

> **Байтовая запись в этот регистр не работает.** Чтение байта работает, но при записи
> всегда записывается слово, а старший байт при байтовой операции теряется
> (проверено в `bkemu-QT/devemu/Board.cpp`).

Записать 0 в разряд 09 недостаточно, чтобы **включить** режим РП; разряд пригоден
в первую очередь для **обнаружения** текущего режима.

> ⚠ **Ошибка [РСП] подтверждена дословно.** **[РСП § 4.1.3]** пишет: «Разряд 9 используется для управления режимом
> расширенной памяти. **"1"** в разряде задаёт режим отображения на экране 1/4 части
> экранного ОЗУ с адресами 70000–77777». Это неверно, на что и указывал Ю. А. Зальцман:
> в действительности **0 — режим РП, 1 — обычный режим**. Подтверждается начальным
> значением регистра 1330 (разряд 9 установлен при полном экране) и реализацией
> в `bkemu-QT/devemu/Board.cpp`.

Первоисточник даёт и наглядное обоснование единицы измерения: изменение младшего
разряда на 1 смещает адрес экранного ОЗУ на 100 байт — ровно на длину информационного
поля одной телевизионной строки. Восемь разрядов дают 256д комбинаций, что соответствует
256д телевизионным строкам экрана.

### Откуда взялось именно `330` [k1801]

Почему «нейтральное» значение равно `330`, а не нулю, из документации на БК не следует —
ответ даёт только устройство БИС **1801ВП1-037** (см.
[15-схемотехника-и-доработки.md](15-схемотехника-и-доработки.md), §15.1а).

Регистр смещения не управляет выводом напрямую: его содержимое **переписывается
в счётчик видеоадреса**. Строб этой перезаписи формируется во время кадрового
синхроимпульса, когда изображение бланкировано, и он достаточно длинный — процессор
успевает поменять регистр несколько раз, и на картинке это никак не скажется.
Окончательная фиксация (ниспадающий фронт строба) происходит за
**40 × 256 × 8 = 81920д тактов CLC до начала вывода изображения**.

За это время счётчик успевает «дотикать» до переполнения. Значение `330`
подобрано так, чтобы к моменту начала вывода счётчик пришёл ровно к условному нулю —
то есть к началу экранного ОЗУ. Никакого смысла «сдвиг на 330 строк» в этом числе нет:
это **компенсация опережения загрузки счётчика относительно начала кадра**.

> Про константу `230` для режима РП источник ничего не говорит. Логично
> предположить, что там та же компенсация, только при вчетверо более коротком
> экране, но **проверкой это не подтверждено** — привожу как догадку.

## 6.4. Системный таймер — 177706, 177710, 177712

> **Это не устройство БК, а периферийный блок самого процессора.**
> Нормативное описание — устройство блока, побитовая карта `TVE_CSR`, таблицы частот
> и режимов, аномалия флага `FL`, различия ВМ1А/ВМ1Г — в
> [04-процессор-К1801ВМ1.md](04-процессор-К1801ВМ1.md), §4.4.
> Здесь — только то, что относится к применению таймера **на БК-0010-01**.

| Адрес | Регистр [ВМ1] | Доступ |
| --- | --- | --- |
| 177706 | `TVE_LIMIT` — регистр начального значения (перезагрузки/захвата) | Ч/З |
| 177710 | `TVE_COUNT` — реверсивный счётчик | Ч (запись игнорируется) |
| 177712 | `TVE_CSR` — регистр управления, младший байт | Ч/З |

Краткая сводка разрядов `TVE_CSR`: 0 — `SP` (тактирование от внешнего входа nSP),
1 — `CAP` (режим захвата), 2 — `MON` (мониторинг события), 3 — `OS` (однократный режим),
4 — `RUN` (счёт разрешён), 5 — `D16` (делитель 16), 6 — `D4` (делитель 4),
7 — `FL` (флаг события). **Любая запись в `TVE_CSR` копирует `TVE_LIMIT` в `TVE_COUNT`.**

### Почему таймера нет в документации на БК

Таймер **не входит в технические условия на процессор** и в заводских руководствах на БК
не описан. По [ВМ1 § 6.4]: блок унаследован от микроконтроллера К1801ВЕ1, он
недокументирован, и «некоторый процент микросхем К1801ВМ1А/Б/В имеют неработоспособный
блок встроенного таймера». Гарантированно рабочим он стал только в **К1801ВМ1Г**.

Отсюда и предупреждение [З]: неисправность таймера «не служит основанием для предъявления
претензий заводу-изготовителю»; на БК 3–5 лет более ранних выпусков она встречалась
заметно чаще.

### Единичный период и максимальные интервалы [З]

Единичный период счёта, измеренный на нескольких БК, — **≈42,9 мкс**, разброс от долей
процента до нескольких процентов. Это в точности базовая частота CLC/128 при тактовой
частоте 3 МГц: 128 / 3 000 000 = **42,67 мкс** (см.
[04-процессор-К1801ВМ1.md](04-процессор-К1801ВМ1.md), §4.4).

| Код в 177712 | Множитель | Макс. время цикла, с |
| --- | --- | --- |
| 20 | ×1д | 2,812 |
| 120 | ×4д | 11,25 |
| 60 | ×16д | 45,00 |
| 160 | ×64д | 180,0 |

Отсчитанный промежуток = (разность показаний счётчика) × (множители) × 42,9 мкс.

*Пример.* Код 160 (разряды 04, 05, 06), разность показаний 154432 (55578д):
55578 × 4 × 16 × 42,9 мкс ≈ 152,6 с.

Максимальный цикл задаётся числом 177777 в 177706 — или нулём: после первого вычитания
произойдёт заём и число станет 177777.

*Расчёт цикла ровно в 1 минуту:* 60 000 000 мкс / 64 / 42,9 ≈ 21853д = **52535** в 177706,
код **160** в 177712.

### Свойства и применение на БК

* Таймер работает **автономно от процессора**: отсчитывает время независимо от того,
  стоит процессор или работает, есть ли активная программа, и даже во время работы
  с магнитофоном. Обратно — на скорость исполнения программы он не влияет.
* **При выходе в Бейсик или Фокал таймер останавливается**, поэтому запускать его надо
  в той среде, в которой он будет работать.
* Для интервалов дольше 3 минут нужно программно считать переходы через ноль, опрашивая
  177710 **не реже двух раз за цикл**, либо используя флаг `FL`, либо перезапуская таймер
  в однократном режиме.
* Таймер — **генератор истинно случайных чисел**, если момент обращения задаётся
  человеком (нажатием клавиши). При обращении из программы последовательность далека
  от случайной: таймер и процессор тактируются от одной частоты и коррелируют.
  (Приём использован в игре STREAP SHOW.)
* Проверка исправности в МСД: `177706А0,,160-Ц` — цикл должен быть ровно 3 мин.
* Ещё одно применение — организация многопрограммного режима на БК
  («Информатика и образование», № 2 за 1992 г.).

> ⚠ **Ошибка в описании делителей у [З].** В тексте раздела «Системные регистры»
> сказано, что разряд 05 даёт множитель ×4, а разряд 06 — ×16. Верно обратное:
> **разряд 05 — делитель на 16, разряд 06 — делитель на 4**. Подтверждено таблицей
> частот в [ВМ1 § 6.7], собственной таблицей кодов [З] и реализацией [ЭМ]
> (`TVE_D16 = 040`, `TVE_D4 = 0100`). Таблицей кодов пользоваться можно, прозаическим
> описанием — нет.

### Независимое измерение периода [ИиО-90]

Статья «Отсчёт времени на БК-0010.01» («Информатика и образование», 1990 № 1) —
редкий случай, когда таймер БК измерен экспериментально. Автор работает с регистрами
через Бейсик, в шестнадцатеричной записи:

| Бейсик | Восьмеричный адрес | Регистр |
| --- | --- | --- |
| `&HFFC6` | 177706 | `TVE_LIMIT` — начальное значение α (в дополнительном коде) |
| `&HFFC8` | 177710 | `TVE_COUNT` — счётчик, читается `PEEK` |
| `&HFFCA` | 177712 | `TVE_CSR` — запуск: `POKE &HFFCA,&H70` |

`&H70` = **0160** — это в точности `RUN` + `D16` + `D4`, то есть множитель **×64**
(см. таблицу кодов выше).

**Результат измерения:** при α = 32767 (`&H7FFF`) полный период работы счётчика равен
**89,657 с**, откуда коэффициент пересчёта в секунды **K = 32767 / 89,657 = 365,47**
отсчёта в секунду.

> **Сходимость.** Моя таблица даёт для кода 160 полный цикл (65536 отсчётов) **180,0 с**,
> то есть для половины диапазона — 90,0 с против измеренных 89,657 с. Расчётная частота
> 1 / (64 × 42,9 мкс) = **364,2 Гц** против измеренных 365,47 Гц. Расхождение — **0,4 %**,
> что укладывается в разброс тактовой частоты конкретных машин. Экспериментальные данные
> 1990 года и побитовая карта из [ВМ1] подтверждают друг друга.

**Практические наблюдения автора:**

* После запуска счётчик **работает непрерывно**, не реагируя ни на какие команды
  и операции Бейсика — **даже на `STOP`, `END` и `NEW`**. Изменить его состояние
  может только `POKE` по соответствующим адресам. Регистр начального значения
  тоже не воспринимает `NEW` — в нём сохраняется прежнее α.
* После включения БК регистр начального значения установлен в **α = 0**, поэтому
  при α = 0 оператор его загрузки можно опустить.
* Счётчик **знаковый**: при α > 0 показания меняются по треугольнику
  32767 ↔ 0 ↔ −32768; отсюда в программах строка вида `IF T>0 THEN T=T-65536`.
* Если подсчитывать и запоминать **номер периода** работы счётчика, а не только
  его показание, точность отсчёта достигает **0,01 %** — погрешность не превышает
  **2 с за сутки**. Этого достаточно для программы электронных часов.
* Таймер годится для измерения времени исполнения отдельных операторов Бейсика
  (программа `TRIAL`: цикл `FOR I%=0 TO 999` вокруг испытуемого фрагмента).

> В статье есть оговорка: сказано, что «регистр FFCA, задающий начальное состояние
> счётчика, автоматически устанавливается на α = 0». Начальное значение задаёт
> **`FFC6` (177706)**, а `FFCA` (177712) — регистр управления; по смыслу изложения
> имеется в виду именно регистр начального значения.

## 6.5. Регистр порта ввода-вывода — 177714

Используются все 16 разрядов. Регистр **состоит из двух независимых регистров**:
запись идёт в регистр вывода, чтение — из регистра ввода. **Прочитать записанное в порт
вывода невозможно.**

> **Причина** [ВМ1 § 6]: по этому адресу процессор лишь вырабатывает сигнал **nSEL2**,
> а данные выдают и принимают внешние схемы БК — физически **разные** микросхемы
> К589ИР12 на ввод и на вывод. Это не особенность регистра, а особенность подключения. Если нужно сохранить доступ к записанным данным, их надо продублировать
в ОЗУ — для этого зарезервирована ячейка `256`.

**Порт инвертирующий.** При записи нулей на выходе будут единицы и наоборот; при подаче
на вход логических единиц программно читаются нули. Уровни сигналов:

```
логическая «1»:  0 В  < U < 0,5  В   (низкий уровень)
логический «0»:  2,4 В < U < 5,25 В   (высокий уровень)
```

Подача других напряжений может вывести компьютер из строя. Аппаратно каждый порт
реализован на двух микросхемах К589ИР12; нагрузочная способность выхода ограничена
возможностями этих микросхем.

Соответствие разрядов контактам разъёма «УП» — см. [14-разъёмы-и-периферия.md](14-разъёмы-и-периферия.md).

Прочитать записанное в 177714 всё-таки можно, если подключить к порту **«блок нагрузок»**,
входящий в комплект ЭВМ: он замыкает выход на вход.

> **Работа с портом системным обеспечением не поддержана** [РСП § 11]:
> EMT-функций для порта нет, обращаться нужно непосредственно по физическому адресу 177714.
> Ячейка 256 предусмотрена в системе именно для того, чтобы программа вела в ней копию
> выходного регистра порта.
>
> Статья разработчиков (МПСС № 1, 1985) добавляет важное эксплуатационное требование:
> **питание устройств пользователя должно осуществляться от отдельного источника**,
> а не от контактов +5 В разъёма.

Типовое применение — подключение принтера (разряд 08 порта ввода — сигнал готовности,
разряд 08 порта вывода — строб, разряды 00–07 — код символа), джойстика, программатора ПЗУ,
самодельных АЦП, а также звуковых расширений (Covox, AY-8910) в поздних доработках.

## 6.5а. Регистры блока ИРПС — 176560…176566 (только БК-0010Ш)

> Раздел справедлив **только для «Электроника БК-0010Ш»** (школьный вариант) —
> **[РСП § 4.1.6]**. В БК-0010 и БК-0010-01 этих регистров нет: там последовательный
> обмен реализован программно через разряды регистра 177716
> (см. [13-последовательный-канал.md](13-последовательный-канал.md)).

Блок ИРПС обеспечивает обмен по интерфейсу радиального подключения устройств
с последовательной передачей информации. БК-Ш работает по протоколу ИРПС на скорости
**9600 бод**. Блок содержит четыре регистра:

| Адрес | Регистр |
| --- | --- |
| 176560 | Регистр состояния приёмника |
| 176562 | Буферный регистр приёмника |
| 176564 | Регистр состояния передатчика |
| 176566 | Буферный регистр передатчика |

### Регистр состояния приёмника — 176560

| Разряд | Доступ | Назначение |
| --- | --- | --- |
| 06 | Ч/З | Разрешение работы приёмника по прерыванию: 1 — разрешено, 0 — запрещено |
| 07 | Ч | Флаг состояния приёмника: 1 при поступлении посылки в буферный регистр |
| 12 | Ч | **Ошибка переполнения**: поступило более одной посылки без чтения предыдущей. В буфере при этом сохраняется **первая** посылка |
| 15 | Ч | Ошибка в принятой посылке |
| прочие | Ч | Не используются, читаются как 0 |

Посылка — порция информации в **семь или восемь бит**.

### Буферный регистр приёмника — 176562 (только чтение)

Разряды 00…07 — принятая с линии посылка: в разряде 00 — первый бит, в 07 — восьмой.
Разряды 08…15 не используются.

### Регистр состояния передатчика — 176564

| Разряд | Доступ | Назначение |
| --- | --- | --- |
| 00 | Ч/З | Разрыв линии |
| 02 | Ч/З | Проверка работы (шлейф) |
| 06 | Ч/З | Разрешение работы передатчика по прерыванию: 1 — разрешено |
| 07 | Ч | Флаг состояния передатчика. Устанавливается в 1 в момент выдачи посылки на линию, сбрасывается записью в буферный регистр. **1 означает, что буферный регистр пуст** |
| прочие | Ч | Не используются, читаются как 0 |

Если запись в буферный регистр произошла во время посылки, следующая посылка начинается
сразу же по окончании предыдущей.

### Буферный регистр передатчика — 176566 (только запись)

Разряды 00…07 — данные для передачи: в разряде 00 — первый бит посылки, в 07 — восьмой.

Векторы прерываний блока: **360** (приёмник) и **364** (передатчик) — см.
[07-прерывания.md](07-прерывания.md).

## 6.6. Регистр управления системными внешними устройствами — 177716

Внешний регистр 1 (ВР1, **nSEL1**) процессора ВМ1 — регистр начального пуска. Как и 177714,
состоит из **двух раздельных регистров** для чтения и записи.

> **Откуда берётся адрес начального пуска.** По [ВМ1 § 7.3] при аппаратном сбросе
> процессор читает стартовый адрес из **старшего байта регистра `177716 + Ncpu×20`**,
> а PSW всегда устанавливается в `340`. В БК процессор имеет номер 0, а на линиях
> старшего байта внешнего регистра nSEL1 аппаратно задано значение, дающее **100000**.
> Именно поэтому разряды 08…15 доступны только по чтению — это не регистр,
> а жёстко заданное значение. **Кто именно его выставляет**, видно из восстановленной
> модели контроллера видео: разряд 15 при чтении `177716` выдаёт **сама БИС
> 1801ВП1-037**, остальные разряды старшего байта никем не удерживаются и читаются
> нулями (см. [15-схемотехника-и-доработки.md](15-схемотехника-и-доработки.md), §15.1а).
>
> Ещё одна деталь [ВМ1]: если прерывание использует вектор вида `1600xx` (пультовый
> режим), процессор перед сохранением PSW и PC устанавливает **разряд 3 по адресу
> 177716**, причём этот адрес не зависит от номера процессора.

### По чтению (входной системный порт)

| Разряд | Назначение |
| --- | --- |
| 00, 01, 03 | Не используются, 0 |
| 02 (004) | **Признак записи в системный регистр.** Устанавливается в 1 при любой записи в 177716, сбрасывается в 0 после чтения регистра. Используется для различения прерывания по клавише «СТОП» / команде `HALT` от прерывания по зависанию |
| 04 (020) | Данные с ТЛГ-линии |
| 05 (040) | Данные с магнитофона |
| 06 (100) | **Индикатор нажатия клавиши: 0 — нажата любая клавиша** (кроме «СТОП» и регистровых «НР»/«АР2», «ПР», «СУ», «СТР», «ЗАГЛ»), 1 — все клавиши отжаты |
| 07 (200) | Сигнал готовности с ТЛГ-линии. По заводской документации на БК-0010 этот разряд — константа 1, указывающая на отсутствие в системе команд расширенной арифметики |
| 08…15 | Адрес начального пуска — **100000** (младший байт принимается нулевым). Только чтение |

Разряд 06 крайне широко используется в играх и в языках высокого уровня: он позволяет
узнать факт нажатия клавиши без остановки программы и без чтения кода. В самой ЭВМ
он используется для режима «ПОВТ».

### По записи (выходной системный порт)

| Разряд | Назначение | Исходное состояние |
| --- | --- | --- |
| 00, 01, 03 | Не используются | 0 |
| 04 (020) | Передача данных на ТЛГ-линию | 1 |
| 05 (040) | Данные на магнитофон **либо** сигнал готовности на ТЛГ-линию (одновременный обмен с МЛ и ТЛГ невозможен) | 0 |
| 06 (100) | Данные на магнитофон **и на пьезодинамик** | 0 |
| 07 (200) | Управление двигателем магнитофона: **0 — пуск, 1 — стоп** (через электромагнитное реле на плате) | 1 |

**Звук.** Разряд 06 — единственный штатный источник звука в БК-0010. Если периодически
записывать в него чередующиеся 0 и 1, звук воспроизводится встроенным пьезодинамиком
и одновременно поступает на выход «МАГНИТОФОН», откуда его можно подать на УНЧ.

**Двухуровневая запись на МЛ.** Изначально для записи на магнитофон использовался
двухуровневый сигнал: при записи «нуля» сигнал шёл с обоих разрядов 05 и 06 (большая
амплитуда), при записи «единицы» — только с разряда 06 (меньшая). Это корректировало
частотную характеристику тракта записи. В БК-0010 поздних выпусков от этого отказались:
разряд 05 на магнитофон уже не передаётся, но **программа драйвера осталась прежней
и по-прежнему обслуживает оба разряда**. Проверить, задействован ли разряд 05 на
конкретной машине, можно осциллографом по форме импульсов на выходе «МГ»: два разных
уровня амплитуды — задействован, один — нет.

### Определение причины прерывания по вектору 4

Прерывание по клавише «СТОП» и по зависанию имеют один и тот же вектор 4. Различить их
позволяет разряд 02 регистра 177716, выставляемый в 1 при нажатии «СТОП» или отработке
`HALT`:

```asm
PRST:   ...                     ; п/п обработки прерывания по вектору @#4
        BIT     #4,@#177716     ; проверка разряда 02
        BEQ     ZAVIS           ; 0 — обработка зависания
        ...                     ; иначе — обработка нажатия «СТОП»
```

Механизм: при нажатии «СТОП» возникает радиальное прерывание IRQ1, микропрограммная
обработка которого содержит обращение по записи к регистру `@#177676`. В БК ячейки
с таким адресом нет, поэтому и возникает прерывание по зависанию.

> **Подтверждение.** Ю. А. Зальцман указывал, что способ описан только во внутризаводской
> ремонтной документации. На деле он документирован и в заводских руководствах для
> родственных машин на том же ВМ1: «Команда `HALT` (код 000000) вызывает прерывание
> выполнения программы и переход по вектору прерывания 4, причём **при этом
> устанавливается бит — признак записи в системный регистр**»
> ([РСП БК-0011] 00008-01.32.01 § 1.5; то же в [РСП БК-0011М] 00015-01.32.01 § 1.3).
> Там же оговорено, что **из-за особенностей аппаратуры может выдаваться неправильное
> значение счётчика команд** и что корректная обработка «СТОП» возможна только при
> корректном значении указателя стека — это ровно та причина, по которой перехват «СТОП»
> требует восстановления SP (см. [07-прерывания.md](07-прерывания.md), §7.4).
