# 17. Контроллер СМК и семейство «АльтПро»

> **Источники главы**
> * **[АльтПро]** — **первоисточник главы.** Документация разработчиков с фирменного
>   диска `GID/Release_x64/Img/AltPRO_ReLoad.IMG` (формат ANDOS/FAT12, метка тома
>   `ALTpro`, тексты в KOI8-R): `ALTBIOS1.EDP`…`ALTBIOS3.EDP` — «Техническое описание
>   контроллеров „АльтПро“ с доп. памятью 64–512 Кб и версией ПЗУ не ниже 2.00»
>   (В. Е. Новак, А. И. Глускер, 1997–98); `HDD_DOC1.EDP`, `HDD_DOC2.EDP` —
>   «Контроллер IDE винчестера для БК» (Н. Подкопаев, В. Новак, 1994–95, известна
>   также как «версия 1.21 от 27.01.95»); `A16TECH.EDP` — режимы A16/A16M и нумерация
>   версий; `PROG1.EDP`, `PROG2.EDP`, `HDD_HELP.EDP` — эксплуатация.
>   То же описание в HTML — `GID-src/BKemu/Docs/ALTBIOS.html`.
> * **[РТ5]** — `БК-0010/files.bk-fpga.ru/SMK/SMK64.MAC` (в UTF-8: `SMK64-utf8.MAC`) —
>   **исходный текст прошивки дешифратора 556РТ5** контроллера СМК-64. Аппаратный
>   первоисточник таблицы режимов: 8 режимов × 64 строки на уровне сигналов.
> * **[CPLD]** — исходники прошивки ПЛИС реплики СМК-512,
>   `SMK-512/smk512-cpld_firm-src v1.3.zip` (`smk64.vhd`, `RT5.vhd`, `fdd.vhd`);
>   трассировка и перечень элементов — там же и в `SMK-512/spec.txt`. В объявлении
>   портов `smk64.vhd` цоколёвка разъёма МПИ проставлена прямо в комментариях.
> * **[Сх512]** — **принципиальная схема реплики СМК-512 v1.0**, две страницы формата
>   A3 → `СМК/smk_import.pdf` (лист 2) и `СМК/smk_import2.pdf` (лист 1). Распечатка
>   из Altium (09.07.2020) проекта, импортированного из P-CAD-файла
>   `SMK-512/smk512-scheme-v1.0.sch` (2012); текстовый слой сохранён, поэтому
>   позиционные обозначения и имена цепей читаются без распознавания.
> * **[СМК]** — `docs/SMK.pdf`, «СМК. Режимы работы и распределение памяти»
>   (вёрстка 2012 г.) — перепечатка Приложения 1 из [АльтПро] с той же Табл. 1.
> * **[ДОЗУ]** — `docs/Работа с ДОЗУ контроллеров СМК.pdf` (2023) — та же таблица
>   плюс развёрнутые таблицы «объём ↔ слово ПЗУ», «страница ↔ код», «режим ↔ код».
> * **[ПЗУ-СМК]** — комментированный дизассемблер ПЗУ v2.05,
>   `sources/good-apple/docs/SMK64_v2.05.ASM`.
> * **[ЭМ]** — `bkemu-QT/devemu/FDDController.cpp` (`SMK512_MemManager_10`,
>   `SMK512_MemManager_11M`, `Change_AltPro_Mode`), `Board_10_FDD.cpp`,
>   `Board_11M_FDD.cpp`, `Config.cpp`; те же файлы в `GID-src/BKemu/BK/devemu/`.
> * **[Каз]** — требования к доработке БК под СМК (см. также §14.2в).
> * **[ВП128]** — описание БМК 1801ВП1-128: штатное назначение разрядов `177130`
>   (§14.2г).
> * **[Мнв]** — Manwe, «Управление памятью»,
>   https://manwe.pdp-11.net/articles/programming/memory.md — практические примеры
>   переключения страниц.
> * **[MAXIOL]** — форум https://forum.maxiol.com, темы 5563 (консолидация
>   документации по СМК), 5617 и 5593 (реализация СМК в AZ-БК, включая исходный
>   текст маппера на Verilog).

**СМК** — совмещённый контроллер для разъёма МПИ, дающий БК сразу четыре вещи:
дополнительное ОЗУ 64…512 Кбайт, собственное ПЗУ с драйверами и BIOS, контроллер
НГМД и контроллер жёсткого диска IDE.

Главное, что нужно знать сразу: **«СМК» и «АльтПро» — это одно и то же семейство**,
одного разработчика, с общим регистром управления `177130`, общими восемью режимами
памяти и общими кодами включения. В литературе (и в этом справочнике до сих пор)
они разнесены, потому что расширение памяти обычно описывают по [АльтПро], а
дисковую часть — по [Каз] и по документации на винчестер. Здесь они сведены вместе.

---

## 17.1. Линейка контроллеров

Фирма **«АльтПро»** (ранее «Альтек») выпускала контроллеры с дополнительным ОЗУ
**64, 128, 256 и 512 Кбайт** с **одинаковой организацией** [АльтПро]. Внутри
семейства две ветви:

| Обозначение | Что это |
| --- | --- |
| **A16** | Ранний контроллер НГМД с 16 Кбайт доп. ОЗУ |
| **A16 + винт** | То же плюс контроллер НЖМД и 4 Кбайт доп. ОЗУ (`170000–177000`) |
| **A16M** | Развитие A16 |
| **СМК-64, СМК-128, СМК-256, СМК-512** | «Винтовые» контроллеры: НГМД + НЖМД + ДОЗУ соответствующего объёма |
| **A64M … A512M** | «Дисководные»: то же ДОЗУ, но **без** контроллера винчестера |

Схема у всех «винтовых» одна, различается только объём ОЗУ (а значит — число
страниц). Из прайс-листа на том же диске [АльтПро]: «SMK64 с 64 Кб ОЗУ,
HALT-расширителем и встроенным кабелем», далее SMK128, SMK256, SMK512.

Единственное различие «винтовых» и «дисководных» в карте памяти [АльтПро]:

> В контроллерах "SMK" ("винтовых") во всех режимах, кроме SYS, в адресах
> 170000-177000 подключено ОЗУ, в дисководных — нет, выше 177000 — во всех
> одинаково.

**Модели «СМК-16» не существует** — под этим именем в архивах ходит скан Табл. 1
(файл `sources/good-apple/docs/SMK16.jpg`).

### Как программа узнаёт модель и объём ДОЗУ

Модель и версия прошивки закодированы в **последнем слове ПЗУ контроллера,
`167776`**: старший байт — модель и объём, младший — номер версии ПЗУ [АльтПро],
[СМК], [ДОЗУ].

| Ст. байт (слово `& 177400`) | Модель |
| --- | --- |
| `174000` | A16 |
| `174400` | A16 + контроллер винчестера с доп. ОЗУ 4 Кбайт (`170000–177000`) |
| `175000` | A16M |
| `175400` | не используется |
| `176000` | **64 Кбайт** |
| `176400` | **128 Кбайт** |
| `177000` | **256 Кбайт** |
| `177400` | **512 Кбайт** |

Младший байт:

* `0` — актуальная прошивка «дисководного» или «винтового» контроллера с ROM-BIOS;
* `> 0` и `< 200` — **старая** версия ПЗУ совмещённого контроллера SMK64,
  **ROM-BIOS в ней отсутствует**, её рекомендуется заменить (ПЗУ на панельках);
* `200…300` — номер версии ПЗУ контроллера **SMK** с указанным объёмом ОЗУ;
* `> 300` — номер версии ПЗУ **дисководного** контроллера A64M–A512M.

Проверка «есть контроллер, не меньше 64 Кбайт, современная прошивка» — одной
командой [АльтПро]:

```
        CMP     @#167776,#176200
        BHIS    Ok
```

> **Внимание** [АльтПро]: программы, загружающие в ОЗУ с адреса `160000` свою
> версию драйверов, обязаны копировать область **`167460–170000`** (точки входа
> BIOS и номер версии контроллера) **1:1 из ПЗУ**.

**[✓ ПЗУ]** Проверено по реальным образам прошивок из `bkemu/ROM/` (все ровно
4096 байт):

| Файл | Слово `167776` | Модель | Версия |
| --- | :-: | --- | :-: |
| `DISK_A16M_v2.41.rom` | `175041` | A16M | 2.41 |
| `DISK_SMK00_v1.41.rom` | `175541` | код `175400` («не используется»), мл. байт `141 < 200` — старая прошивка без ROM-BIOS | 1.41 |
| `DISK_SMK64_v2.05.rom` | `176205` | СМК-64 | 2.05 |
| `DISK_SMK512_v2.05.rom` | `177605` | СМК-512 | 2.05 |

Файл `smk512.rom` из java-эмулятора `BK2010` побайтно совпадает с
`DISK_SMK512_v2.05.rom`.

---

## 17.2. Состав контроллера и подключение

Контроллер вставляется в **разъём МПИ** (розетка СНП58-64) на задней стенке БК.
На плате [АльтПро], [CPLD]:

* **дополнительное ОЗУ** 64…512 Кбайт со страничным подключением (§17.3–17.7);
* **собственное ПЗУ** объёмом `10000` байт по адресам `160000–167777` — слегка
  изменённая прошивка 326 (драйвер НГМД, см. §14.2д) плюс базовый драйвер НЖМД,
  укороченный эмулятор EIS и ROM-BIOS (§17.9, §17.11);
* **статическое ОЗУ** `170000–176777` для служебных данных драйвера НЖМД и
  резидентов (§17.13);
* **контроллер НГМД** — штатный БМК 1801ВП1-128, регистры `177130`/`177132`
  (§14.2г); его регистр управления по совместительству управляет памятью;
* **контроллер НЖМД IDE**, регистры `177740–177756` (§17.12);
* у современной реплики СМК-512 [CPLD] — ПЛИС EPM3256ATC144, SRAM CY7C1041D
  (256K × 16 = 512 Кбайт), ПЗУ AT27C1024 (64K × 16), разъёмы IDC-34 (НГМД),
  IDC-40 (IDE), CF-50 (карта CompactFlash вместо винчестера).

> Подключение и отключение контроллера — **только при выключенном питании**.

Полный состав платы реплики виден по её принципиальной схеме [Сх512] (две страницы,
проект `smk512-scheme-v1.0`):

| Лист | Что на нём |
| :-: | --- |
| 1 | ПЛИС **EPM3256ATC144-10** (символ разбит на сигнальную и питающую части), тактовый генератор на кварце **4 МГц** и двух инверторах 74HC04 с резисторами 10 кОм, стабилизатор **LM1117-3.3** (+5 В → +3,3 В) с блокировочными конденсаторами, разъём **JTAG IDC-10** с подтяжками 1 кОм, формирователь `SYN` → `SNC` |
| 2 | Разъём МПИ **CON64**, ОЗУ **CY7C1041D**, ПЗУ **AT27C1024**, два буфера данных **74HC245**, два **74HC244** и **74HC14** на линии НГМД, разъёмы **HDD_CONN** (IDE, 40 конт.), **FDD_CONN** (34 конт.), **COMPACT_FLASH3** (50 конт.), сборка 1 кОм, светодиод активности |

Формирователь `SNC` заслуживает отдельного слова: сигнал `SYN` (СИА) пропускается
через инвертор 74HC04, конденсатор 33 пФ и триггер Шмитта 74HC14 — получается
**задержанный СИА**, по которому и разрешается работа дешифратора адреса. Это та
самая «задержка совместимости с БК-0010», которая в оригинальном СМК-64 делалась
конденсатором; в поздних ревизиях реплики её убрали внутрь ПЛИС
(`SMK-512/spec.txt`, примечание к C15).

---

## 17.3. Требования к БК и доработки

Чтобы БК-0010 работала с контроллером, **заменять ПЗУ не обязательно** — достаточно
доработать машину [Каз]; подробности по контактам разъёма — в §14.2в:

| Контакт МПИ | Что должен делать |
| --- | --- |
| **Б1** | Отключать ПЗУ монитора |
| **А14** | Отключать ПЗУ Фокала |
| **А29** | Блокировать тестовое ПЗУ |
| **А22**, **Б6** | Должны быть свободны |

Контроллер вырабатывает три сигнала отключения штатной памяти (строки
«выраб. сигналы» в Табл. 1, §17.6); в терминах [CPLD] это `mon10`, `mon11` и `p4o`:

| Сигнал | Что отключает | В каких режимах |
| --- | --- | --- |
| `MON10` | ПЗУ монитора БК-0010 | ОЗУ10, All, Hlt10, Hlt11 |
| `MON11` | ПЗУ монитора БК-0011М | Std10, All, ОЗУ11, Hlt11 |
| `ВЭД` (`P4`) | верхнее ОЗУ БК-0011М | SYS, Std10, ОЗУ10, All |

### Какой сигнал контроллера на каком контакте МПИ

Требования [Каз] сформулированы через контакты, но не говорят, что именно
контроллер туда выдаёт. Недостающую половину дают схема реплики [Сх512] и
объявление портов ПЛИС [CPLD] — там цоколёвка проставлена прямо в комментариях.
Слева имена цепей контроллера, справа — принятые в справочнике имена сигналов МПИ
(§14.1, §14.2б):

| Контакт | Цепь платы | Сигнал МПИ по §14.2б | Что делает |
| :-: | :-: | --- | --- |
| **Б1** | `MON10` | **БЛК МОН** — отключение ПЗУ монитора | блокировка ПЗУ монитора БК-0010 |
| **А14** | `BAS` | **БЛК** — блокировка ПЗУ | блокировка ПЗУ в окне `120000–137777` |
| **А29** | `BAS2` | **Е** — блокировка | блокировка ПЗУ по `160000` |
| **А22** | `P4O` («ROM 4 exit») | в таблице [Каз] **отсутствует** | ВЭД — отключение верхнего ОЗУ БК-0011М |
| **Б6** | `M11` | в таблице [Каз] **отсутствует** | блокировка ПЗУ монитора БК-0011М |
| А1 | `RES` | ОСТ | останов/перезапуск |
| Б19 | `RST` | СБРОС (INIT) | первоначальная установка канала |
| Б20 | `RPL` | СИП (RPLY) | ответ пассивного устройства — выход контроллера |
| Б21 | `DOU` | ВЫВОД (DOUT) | вход |
| А23 | `DIN` | ВВОД (DIN) | вход |
| Б22 | `SYN` | СИА (SYNC) | вход; из него формируется задержанный СИА (§17.2) |
| Б11 | `WBT` | БАЙТ (WTBT) | вход: признак байтового обмена |

Все пять «особых» контактов из требований [Каз] этим объясняются. По Б1, А14 и А29
контроллер **сам выдаёт** сигналы блокировки штатных ПЗУ — потому машину и надо
доработать так, чтобы она их слушалась. А22 и Б6 обязаны быть свободны потому, что
контроллер **занимает их своими выходами** `P4O` и `M11`; в заводской таблице
сигналов МПИ этих двух контактов нет вовсе. Отсюда же и «хак» с А22, описанный
ниже.

Названия окон у [Каз] и у разработчиков разные, но окна одни и те же:
`120000–137777` — ПЗУ Бейсика, оно же ПЗУ Фокала в комплектации с блоком МСТД;
`160000–167777` — окно, которое в БК занимает тестовое ПЗУ, а в машине с
контроллером — его собственное ПЗУ (§8, §9).

**[✓] Сверка шины данных.** Шестнадцать линий адреса/данных на схеме контроллера
(`D0`…`D15`) разведены по контактам А31, Б31, Б29, Б30, Б28, А28, Б27, Б32, Б26,
А27, Б25, А26, Б24, А25, Б23, Б7 — это **в точности** ряд ДА 1…ДА 16 из таблицы
§14.2б, вплоть до порядка. Схема реально работающей платы — источник, полностью
независимый от [Каз], так что заодно подтверждается и та таблица.

Питание `+5 В` подведено к А3, А4, А11, А12 и Б12, общий провод — А2, Б2, Б3.

Оговорки самих разработчиков [АльтПро]:

> ОЗУ10 — вместо монитора БК10 и ПЗУ-160000 подключается ОЗУ (**на многих БК10
> требуется простая доработка**, иначе доп. ОЗУ подключается параллельно ПЗУ
> монитора и приходится записывать в это ОЗУ копию монитора)…
> ОЗУ11 — подключение ОЗУ вместо ПЗУ на БК11М, для отключения ПЗУ монитора БК11М
> **простая доработка требуется всегда**.

### Что происходит без доработки

Ключевое правило [АльтПро]:

> При подключении ОЗУ параллельно чему-либо (ПЗУ, регистры) в случае конфликта
> данных (в одном — 0, в другом — 1) в подавляющем большинстве случаев
> **читается 1**… Поэтому при параллельном подключении ОЗУ и ПЗУ в ОЗУ надо
> предварительно записывать копию ПЗУ, а при параллельном подключении ОЗУ и
> регистров в ОЗУ должны быть нули.

Отсюда два практических следствия. Первое: без доработки в «оперативные» режимы
можно входить, только предварительно скопировав туда содержимое ПЗУ. Второе:
именно поэтому в ПЗУ контроллера зарезервированы нулевые ячейки-двойники системных
регистров (§17.9). У современной реплики СМК-512 память быстрая и «перебивает» ПЗУ
электрически, так что монтажного «ИЛИ» фактически не получается — Бейсиком из ПЗУ
воспользоваться уже нельзя ⚠ (§16.5).

### Доработка БК-0011М: отключение ПЗУ монитора

Дословно [АльтПро] / [СМК]:

> Соответствующий сигнал высокого уровня вырабатывается контроллерами и выдаётся
> на контакт **B6 разъёма МПИ**. Доработка обеспечивает автоматическое отключение
> ПЗУ монитора БК0011М при подключении на его место доп. ОЗУ (адреса
> 140000-160000) и делает эти 8 Кбайт доступными для программ как и на БК0010(-01).
> В частности, можно запускать точную копию Бейсика-БК0010. Для этого нужно
> отделить от схемы **23 вывод микросхемы DS17**, перерезав идущую к нему жирную
> дорожку (1801РЕ2-324, крайняя левая нижняя микросхема). В разрыв нужно подключить
> резистор 1 кОм, а также соединить проводом отрезанный вывод DS17.23 и контакт B6
> разъёма МПИ.

Отдельный «схемотехнический хак» СМК касается **окна 1 БК-0011М**: выход
собственного регистра контроллера заведён прямо на **контакт А22 МПИ**, который в
норме разрешает работу ПЗУ ЛВС. Подавая туда физический ноль, контроллер
«перетягивает» линию на себя, и схема отключения ОЗУ считает, что ПЗУ включено
через `177716`. Программы этим пользуются, отключая страницу 1 БК-0011М средствами
СМК в обход API. Правильный порядок — сначала записать код отключения ПЗУ в
`177716`, и только потом переключать страницы контроллером [MAXIOL].

---

## 17.4. Организация дополнительной памяти

Дословно [АльтПро] (Приложение 1):

> Память в контроллерах "АльтПро" разбита на **страницы по 32 Кб**, части
> (сегменты) одной такой страницы подключаются в области адресов
> **100000-177777**, сегменты разных страниц одновременно подключить нельзя.
> Сегментами ниже называются фрагменты памяти по **10000 байт**. Сегмент с номером
> X=0...7 может быть подключен либо с адреса **1X0000**, либо с адреса
> **1X0000 ± 40000** в зависимости от номера режима.

| Объём ДОЗУ | Число страниц по 32 Кбайт |
| :-: | :-: |
| 64 Кбайт | 2 |
| 128 Кбайт | 4 |
| 256 Кбайт | 8 |
| 512 Кбайт | 16 |

Итак, три уровня деления:

* **страница** — 32 Кбайт, единица переключения; одновременно активна ровно одна;
* **сегмент** — `10000` байт (4 Кбайт), 8 штук в странице, нумеруются 0…7;
* **окно** — вся верхняя половина адресного пространства, `100000–177777`;
  раскладка сегментов внутри окна задаётся **номером режима** и жёстко зашита в
  дешифратор, отдельно выбрать «этот сегмент в это окно» нельзя.

Обратите внимание: у BIOS есть **своё**, укрупнённое деление — «логический сегмент»
длиной `20000` со сквозной нумерацией по всей доп. памяти (§17.11). Это разные
вещи, совпадающие только по названию.

---

## 17.5. Регистр управления `177130`

Всё управление памятью идёт **через регистр управления НГМД `177130`** — контроллер
навешивает на его разряды дополнительный смысл. Штатное назначение разрядов (шаг
головки, выбор накопителя, включение двигателя) описано в §14.2г по [ВП128];
здесь — только «памятная» трактовка.

⚠ В [ДОЗУ] на первой странице указан адрес «`177730`» — это опечатка: в примере
кода в том же документе стоит `@#177130`, и все прочие источники дают `177130`
(§16.5).

### Трёхтактный протокол переключения

Одной записи мало: контроллер защёлкивает конфигурацию по **стробу** [ДОЗУ],
[АльтПро]:

```
        MOV     #6,@#177130                     ; стробирующий код
        MOV     <код режима + код страницы>,@#177130
        MOV     #0,@#177130                     ; снятие строба
```

Та же последовательность — в самом ПЗУ v2.05 [ПЗУ-СМК], с показательным
комментарием автора:

```
MOD$:   MOV     #6,@#177130     ;-> включение режима R4:
        MOV     (SP)+,@#177130  ;ВЫЗОВ - JSR R4,@#MOD$
MOD$0:  MOV     #0,@#177130     ;CLR низя
```

**Пользоваться `CLR`, `BIC`, `BIS` для записи в порт нельзя.** Это команды
«чтение-модификация-запись»: лишний цикл чтения срывает стробирование. Дословно
[АльтПро] (`A16TECH.EDP`, про более раннюю модель):

> Для очистки в данной модели контроллера можно использовать и команду CLR,
> команда "MOV #0,..." приведена для совместимости с контроллерами с доп. ОЗУ
> 128 Кб и выше — там CLR использовать нельзя (MOV #0 и CLR — не одно и то же).

Складывать код режима с кодом страницы нужно **в регистре**, а в порт писать одним
`MOV`; для сложения [СМК] рекомендует `BIS`:

```
        MOV     #6,@#177130
        CLR     R0              ; тут соберём режим + страницу
        BIS     #120,R0         ; режим ОЗУ10
        BIS     #4,R0           ; страница 2
        MOV     R0,@#177130     ; установили режим и страницу
        MOV     #0,@#177130     ; остановили дисковод
```

Аппаратно [CPLD]: код `6` (разряды 1 и 2 установлены, 0 и 3 сброшены) взводит
внутренний триггер, следующая запись защёлкивается в регистр режима, третья снимает
блокировку регистров НГМД. ⚠ Оригинальные СМК и A16M опознают строб по маске
(достаточно разрядов 1 и 2), реплика СМК-512 требует **точного** значения `6`
в младшей тетраде (§16.5).

### Разряды слова, записываемого в `177130`

| Разряд | Вес | Значение для памяти | Штатная функция НГМД |
| :-: | :-: | --- | --- |
| 00 | `1` | номер страницы, вес **8** | `DS0` |
| 01 | `2` | участвует только в стробе | `DS1` |
| 02 | `4` | номер страницы, вес **2**; пока установлен — **регистры `177130` и `177132` не читаются** | `DS2` |
| 03 | `10` | номер страницы, вес **4**; на контроллерах < 256 Кбайт — традиционное подключение ПЗУ Бейсика на БК-0010 | `DS3` |
| 04 | `20` | код режима, сигнал `ВЭД` (`P4` = /разряд 04) | включение двигателя |
| 05 | `40` | код режима, сигнал `M10` (= /разряд 05) | выбор головки |
| 06 | `100` | код режима, сигнал `M11` (= /разряд 06) | направление шага |
| 07…09 | | памятью не используются | шаг, начало чтения, запись маркера |
| 10 | `2000` | номер страницы, вес **1** | предкоррекция |

Маска режима — `0160` (разряды 04–06), маска кода страницы — `02015`
(разряды 00, 02, 03, 10). Это подтверждается тремя независимыми источниками:
[CPLD] (`extended_reg`), [ЭМ] (`ALTPRO_MODE_MASK = 0160`,
`ALTPRO_CODE_MASK = 02015`) и маппером AZ-БК на Verilog [MAXIOL]:

```verilog
wire [3:0] SMK_PAGE = {smk_data[10], smk_data[3], smk_data[2], smk_data[0]};
wire [2:0] SMK_MODE = {smk_data[6], smk_data[5], smk_data[4]};
```

Номер страницы собирается так: `N = разр.10·1 + разр.02·2 + разр.03·4 + разр.00·8`.

Это подтверждается и «железно» — разводкой адресных входов ОЗУ на схеме [Сх512]
вместе с описанием ПЛИС [CPLD]. Микросхема CY7C1041D (256K × 16) получает:

| Входы ОЗУ | Откуда |
| :-: | --- |
| `A0`…`A12` | разряды `01`…`13` адреса БК — 8K слов = 16 Кбайт |
| `A13` | **`A14′` от дешифратора РТ5** — тот самый «± 40000» (§17.8) |
| `A14` | разряд **10** регистра `177130` |
| `A15`, `A16`, `A17` | разряды **02**, **03**, **00** регистра `177130` |

То есть четыре старших адресных разряда ОЗУ — это и есть номер страницы, причём
их порядок `A17 A16 A15 A14` = разряды `00 03 02 10` даёт ровно веса 8, 4, 2, 1
из таблицы кодов ниже. Окно в 32 Кбайт складывается из 13 разрядов адреса БК плюс
разряда `A14′`.

### Коды страниц

| Стр. | Код | Стр. | Код | Стр. | Код | Стр. | Код |
| :-: | :-: | :-: | :-: | :-: | :-: | :-: | :-: |
| 0 | `0` | 4 | `10` | 8 | `1` | 12 | `11` |
| 1 | `2000` | 5 | `2010` | 9 | `2001` | 13 | `2011` |
| 2 | `4` | 6 | `14` | 10 | `5` | 14 | `15` |
| 3 | `2004` | 7 | `2014` | 11 | `2005` | 15 | `2015` |

Какие коды допустимы, зависит от объёма ДОЗУ [АльтПро]:

```
  64 Кб - 0,2000;
 128 Кб - 0,2000,4,2004;
 256 Кб - 0,2000,4,2004,10,2010,14,2014;
 512 Кб - 0,2000,4,2004,10,2010,14,2014,
          1,2001,5,2005,11,2011,15,2015.
```

### Две ловушки разрядов 02 и 03

Обе унаследованы от контроллеров A16 и объясняют расхожее мнение, будто «не все
страницы СМК работают одинаково»:

* **Разряд 02.** Пока он установлен, `177130` и `177132` **не читаются**. Это было
  нужно для работы Бейсика на БК-0010 и так и осталось. Практический вывод: для
  записи в `177130` пользоваться **только `MOV`** — любая команда с чтением порта
  получит мусор.
* **Разряд 03.** На БК-0010 он традиционно подключает ПЗУ Бейсика записью `#10` в
  `177130`; «правда, на режимы ОЗУ это никак не влияет» [АльтПро]. Работает на
  контроллерах с ДОЗУ **менее 256 Кбайт** (на бо́льших этот разряд занят под вес 4
  номера страницы). На оригинальных СМК-64 разряд работает, на прототипе СМК-256 и
  на репликах СМК-512 эта функция уже не реализована; эмулятор BKemu для
  конфигурации СМК её игнорирует ⚠ (§16.5, уточняет запись Д8).

---

## 17.6. Таблица 1. Режимы работы и распределение памяти

Дословно [АльтПро] (`ALTBIOS3.EDP`, Приложение 1); та же таблица — в [СМК] и [ДОЗУ]:

```
Название режима │ SYS │Std10│ОЗУ10│ All │Std11│ОЗУ11│Hlt10│Hlt11
Код включения   │ 160 │ 60  │ 120 │ 20  │ 140 │ 40  │ 100 │20000
  выраб.сигналы
Откл.мон. БК10  │     │     │  +  │  +  │     │     │  +  │  +
-/- мон.БК11М   │     │  +  │     │  +  │     │  +  │     │  +
-/- верх.ОЗУ-11М│  +  │  +  │  +  │  +  │     │     │     │
  АДРЕСА                    номера подключаемых сегментов
HDD: 170-177000 │ ПЗУ │  7  │  7  │  3  │  7  │  7  │  7  │  7
170(177000hdd)-0│ ПЗУ │  -  │ 7W/-│ 3R  │  -  │  -  │ 7W  │ 7W
160000-170000   │ ПЗУ │ ПЗУ │  6  │  2  │ ПЗУ │  6  │ 6R/6│  6
150000-160000   │  1  │  5  │  5  │  1  │  -  │  5  │  5  │  5
140000-150000   │  0  │  4  │  4  │  0  │  -  │  4  │  4  │  4
130000-140000   │  7  │  3  │  3  │  7  │  -  │  -  │  3  │  -
120000-130000   │  6  │  2  │  2  │  6  │  -  │  -  │  2  │  -
110000-120000   │  -  │  -  │  1  │  5  │  -  │  -  │  1  │  -
100000-110000   │  -  │  -  │  0  │  4  │  -  │  -  │ 0R  │  -
```

Легенда [АльтПро]:

> Буква 'W' после номера сегмента означает, что ОЗУ доступно **только по записи**,
> 'R' — **только по чтению**, через дробную черту — **старая версия контроллеров /
> новая** (по номеру версии ПЗУ не определяется), '-' — не подключается ничего.

Две первые строки таблицы относятся к области `170000–177777`: верхняя — к
`170000–176777`, нижняя — к странице `177000–177777` за вычетом регистров НЖМД
`177740–177757`. У «дисководных» контроллеров ОЗУ там нет вовсе.

Строка «Код включения» — то, что складывается с кодом страницы и пишется в
`177130`. ⚠ Код Hlt11 записан как `20000`, хотя разряды 04–06 в нём нулевые:
аппаратно код режима равен нулю, а разряд 13 никуда не подключён. Лишний разряд
нужен, чтобы записываемое слово не совпало с «снятием строба» `#0`
(§16.5). В [РТ5] этот режим так и назван «код 0», а все эмуляторы держат
`HLT11 = 0`.

Внутри таблицы в оригинале рамками выделены **окна перекачки** — области, где
одновременно доступны все 8 сегментов страницы и которые не перекрываются с ПЗУ:

| Машина | Окно | Режимы |
| --- | :-: | --- |
| БК-0010 с неотключаемым ПЗУ монитора | `120000–157777` | **SYS** и **Std10** |
| БК-0010 с отключаемым ПЗУ монитора, а также БК-0011М | `100000–137777` | **ОЗУ10** и **All** |

> Однако при размещении и вызовах подпрограмм на БК11М RAM-BIOS использует два
> первых режима (SYS и Std10) в окне 120000-140000, т.к. для работы с
> подпрограммами необходимо наличие ROM-BIOS в адресном пространстве. [АльтПро]

---

## 17.7. Восемь режимов по одному

Расшифровка назначения — дословно [АльтПро]:

> **SYS** — используется для запуска, пересылки данных на БК10 и RAM-BIOS'ом
> (он — всегда в 0-й странице);
> **Std10, Std11** — стандартные, традиционные режимы работы для БК10 и 11М
> соответственно, Std10 используется также для пересылки данных на БК10;
> **ОЗУ10** — вместо монитора БК10 и ПЗУ-160000 подключается ОЗУ…; используется
> также для доступа к доп. памяти на БК11М;
> **All** — позволяет на БК10 подключить ОЗУ ко всему адресному пространству, что
> используется для загрузки копии Вильнюсского Бейсика; применяется также для
> доступа к доп. памяти на БК11М;
> **ОЗУ11** — подключение ОЗУ вместо ПЗУ на БК11М…;
> **Hlt10, Hlt11** — открывают доступ к HALT-режиму процессора на БК10 и БК11М
> соответственно. Максимально "продвинутые" режимы: везде, где можно, подключается
> ОЗУ вместо ПЗУ и на БК10 нижняя часть ОЗУ монитора защищена от записи
> (квази-ПЗУ) для повышения глюкоустойчивости.

Разбор по режимам (адреса — начала блоков по `10000`; «—» = контроллер молчит,
отвечает сама БК):

* **SYS (`160`), он же «Start» в [РТ5] и в маппере AZ-БК.** Стартовый режим.
  `100000`, `110000` — БК; `120000`→6, `130000`→7, `140000`→0, `150000`→1;
  **вся область `160000–177777` — ПЗУ контроллера**, причём один и тот же образец
  виден и с `160000`, и с `170000` (§17.9). Доступны только сегменты 0, 1, 6, 7.
* **Std10 (`60`).** Штатный режим БК-0010. `120000`→2, `130000`→3, `140000`→4,
  `150000`→5, `160000`— ПЗУ контроллера, `170000`→7. Вместе с SYS даёт окно
  перекачки `120000–157777`.
* **ОЗУ10 (`120`).** Линейная раскладка: `100000`→0, `110000`→1, … `170000`→7 —
  вся страница подряд, ПЗУ контроллера отключается. Требует отключаемого ПЗУ
  монитора БК-0010. Используется и на БК-0011М.
* **All (`20`).** Та же линейка, но со сдвигом на 4: `100000`→4, `110000`→5,
  `120000`→6, `130000`→7, `140000`→0, `150000`→1, `160000`→2, `170000`→3.
  ОЗУ подключено ко всему адресному пространству — отсюда и имя «All*Bas» в [РТ5]
  («Bas» — загрузка копии вильнюсского Бейсика).
* **Std11 (`140`).** Штатный режим БК-0011М: только `160000` — ПЗУ контроллера и
  `170000`→7; всё ниже — штатная память БК-0011М.
* **ОЗУ11 (`40`).** `140000`→4, `150000`→5, `160000`→6, `170000`→7 — верхние четыре
  сегмента вместо ПЗУ БК-0011М. Ниже `140000` — БК.
* **Hlt10 (`100`).** Как ОЗУ10, но с защитой: `100000–107777` доступно **только по
  чтению** (квази-ПЗУ), `160000` в старых контроллерах — тоже только по чтению,
  в новых — полноценное ОЗУ; `177000–177737` — сегмент 7 только по записи.
* **Hlt11 (`20000`, аппаратно `0`).** Как ОЗУ11 плюс `177000–177737` только по
  записи. Отключает и монитор БК-0010, и монитор БК-0011М, и верхнее ОЗУ 11М.

### Зачем нужны режимы Hlt

Для работы HALT-режима процессора **необходимо ОЗУ по записи по адресу `177674`**,
где в двух последовательных ячейках сохраняются PC и PS при HALT-прерывании
[АльтПро]. Именно это и дают строки `7W` в Табл. 1: сегмент 7 подключается на
страницу регистров **только по записи**, так что чтение по-прежнему идёт из
настоящих регистров БК, а запись «оседает» в теневом ОЗУ (§17.10).

Кнопка «СТОП» всегда вызывает именно HALT-прерывание, но без этого ОЗУ вслед за
ним происходит прерывание по вектору 4, причём PC в стеке оказывается на 2 больше,
чем надо (см. также §7 и §14.4). HALT-прерывание идёт по вектору `160002`;
совместить его с драйвером дисковода можно, подключив с `160000` ОЗУ вместо ПЗУ
[АльтПро]. В ячейке `160004` (new PS) должен быть установлен разряд `2000` —
«высокий приоритет процессора для HALT-режима», чтобы заблокировать прерывания от
дребезга кнопки.

---

## 17.8. Аппаратный первоисточник: прошивка дешифратора 556РТ5

Таблицу режимов задаёт прошивка ПЗУ 556РТ5 [РТ5] — 8 режимов по 64 строки. Это
самый нижний уровень описания и одновременно самая надёжная проверка Табл. 1.

Адресные входы РТ5 (в порядке возрастания веса) и его выходы:

```
;адресные входы  РТ5: ~A0,A74,A7T,~A12,~A13,~A14, M14(нпш), M10(вп0), P4(вэд)
;выходы (в порядке убывания): ~DOE,~DIE, ~CS0,~CS1,~CRF, ~CW0,~CW1,~SHD
N=0377          ;                        ничего нет
P=0267          ;   DI,      CRF         ПЗУ
I=0037          ;DO,DI,CS0                ОЗУ-инверс. (верх)
O=0017          ;DO,DI,CS0,A14'           ОЗУ-0 (нижн.40000)
IR=237          ;   DI,CS0                ОЗУ-инв., тк чтение
OR=217          ;   DI,CS0,A14'           ОЗУ-0, тк чтение
IH=137          ;DO,   CS0                ОЗУ-инв-Halt (тк зап.)
W0=072          ;DO,DI, CW0,    SHD       рег.винта с CW0 (CS0)
W1=074          ;DO,DI,     CW1,SHD       рег.винта с CW1
```

Дешифратор включается только при `A15 = 1`, то есть **контроллер вообще не
отзывается на адреса `0–77777`** [CPLD]. Вход `A7T` выделяет область
`177000–177777`, вход `A74` — регистры НЖМД `177740–177757`.

Ключ к чтению таблицы: выход РТ5 подставляет **собственный разряд A14′** вместо
процессорного A14, а A13 и A12 берутся от процессора. Отсюда простое правило:

> **номер сегмента = A14′ · 4 + A13 · 2 + A12**

`I` — это A14′ = 1 (верхняя половина страницы, сегменты 4…7), `O` — A14′ = 0
(нижняя, сегменты 0…3). Суффиксы: `R` — снят выход данных (только чтение),
`H`/`W` — снят вход данных (только запись). Это и объясняет фразу [АльтПро] про
«`1X0000` либо `1X0000 ± 40000`».

Индекс режима внутри прошивки — тройка инвертированных сигналов `(P4, M10, M11)`.
Соответствие полное и однозначное:

| Индекс [РТ5] | Имя в [РТ5] | `P4 M10 M11` | Разряды 06,05,04 | Код | Имя в Табл. 1 |
| :-: | --- | :-: | :-: | :-: | --- |
| 0 | РЕЖИМ 0 (Start) | 000 | 111 | `160` | SYS |
| 1 | РЕЖИМ 1 (Work) | 001 | 011 | `60` | Std10 |
| 2 | РЕЖИМ 2 (ОЗУ,h) | 010 | 101 | `120` | ОЗУ10 |
| 3 | РЕЖИМ 3 (All*Bas) | 011 | 001 | `20` | All |
| 4 | РЕЖИМ 4 (11 Std) | 100 | 110 | `140` | Std11 |
| 5 | РЕЖИМ 5 (11 ОЗУ) | 101 | 010 | `40` | ОЗУ11 |
| 6 | РЕЖИМ 6 (ОЗУ*ЗЗ,h) | 110 | 100 | `100` | Hlt10 |
| 7 | РЕЖИМ 7 (11 ОЗУ,h) | 111 | 000 | `0` | Hlt11 |

Буква «h» в именах [РТ5] — это HALT (теневая запись в страницу регистров),
«ЗЗ» — защита записи (квази-ПЗУ в `100000–107777`).

Сигналы отключения штатной памяти читаются прямо из индекса: `M10` (отключение
монитора БК-0010) равен 1 в режимах 2, 3, 6, 7 — то есть ОЗУ10, All, Hlt10, Hlt11;
`M11` (монитор БК-0011М) — в режимах 1, 3, 5, 7; `P4`/ВЭД активен при `P4 = 0`,
то есть в режимах 0…3. Всё сходится со строками «выраб. сигналы» Табл. 1.

### Сверка трёх источников

Табл. 1 восстанавливается из прошивки механически: 8 режимов × 64 строки [РТ5]
разворачиваются по правилу выше **в точности в Табл. 1** [АльтПро] — все девять
строк адресов и все восемь столбцов, включая ограничения `W` и `R`.

То же самое проделано для реплики: содержимое `RT5.vhd` [CPLD] (512 ячеек,
адрес = `P4 M10 M11 | ¬A14 ¬A13 ¬A12 | A7T A74 ¬A0`) сопоставлено с [РТ5] ячейка в
ячейку. **Из 512 ячеек различаются ровно две** — и это в точности те две, для
которых сама Табл. 1 даёт двойное значение «старая версия / новая»:

| Ячейка Табл. 1 | Оригинал СМК-64 [РТ5] | Реплика СМК-512 [CPLD] | Что это по Табл. 1 |
| --- | :-: | :-: | --- |
| ОЗУ10, `177000–177737` | `7W` | `—` | реплика ведёт себя как **новая** версия |
| Hlt10, `160000–167777` | `6R` | `6` | реплика ведёт себя как **новая** версия |

Практический вывод: **оригинальный СМК-64 — это «старая версия» Табл. 1, реплика
СМК-512 — «новая»**, и других отличий в раскладке памяти между ними нет.

Реализация `SMK512_MemManager_10`/`_11M` в [ЭМ] с этой картиной согласуется;
прямое сравнение затруднено тем, что эмулятор оперирует банками по 4 Кбайт и
обрабатывает область `170000–177777` отдельным кодом, а не через общую таблицу.

---

## 17.9. ПЗУ контроллера, захват старта и сброс

**ПЗУ контроллера — `10000` байт (4 Кбайт) по адресам `160000–167777`.** Дословно
[АльтПро]:

> ПЗУ в контроллерах имеет объем 10000 байт, в режиме SYS одно и то же ПЗУ
> подключается и со 160000, и со 170000 (**благодаря этому изменяется адрес запуска
> компьютера**).

Как это сделано, видно по реплике. Микросхема ПЗУ там — AT27C1024 объёмом
64K × 16 (128 Кбайт), то есть заведомо больше нужного, и адресные входы `A0`…`A15`
разведены на схеме [Сх512] так же, как у ОЗУ: младшие — от разрядов адреса БК,
старшие — от разрядов номера страницы. Но при обращении к ПЗУ ПЛИС **обнуляет
всё лишнее** [CPLD]: разряды `12` и `13` адреса принудительно сбрасываются, а
номер страницы подставляется нулевой. В итоге до ПЗУ доходят только 11 разрядов
адреса — 2048 слов, то есть ровно **`10000` байт**, а разряды `12` и `13`
игнорируются. Отсюда сразу два следствия: объём ПЗУ равен 4 Кбайт, и в режиме SYS
оно **зеркалится** по всей области `160000–177777`, где бы к нему ни обратились.
Остальные 124 Кбайт микросхемы не используются — в исходнике ПЛИС на этом месте
стоит пометка, что страницы ПЗУ оставлены на будущее.

Механизм захвата старта такой. После включения питания и после сброса контроллер
находится в режиме **SYS, страница 0** [CPLD], [ЭМ]. В этом режиме зеркало ПЗУ
накрывает всю область `170000–177777`, в том числе ячейку `177716`, из которой
К1801ВМ1 берёт адрес начального пуска (§4, §8). На соответствующем смещении ПЗУ
лежит адрес входа в прошивку контроллера — и машина стартует не с `100000`, а с
контроллера [ПЗУ-СМК]:

```
;Адрес перезапуска =@167716=167400 = #ZAG-2  нельзя смещать !!!
A716:   .WORD   STARTJ  ;177716 - адрес запуска
```

По той же причине в ПЗУ зарезервированы нулевые двойники системных регистров —
иначе программы, читающие клавиатуру и скролл на старте, получили бы из зеркала
ПЗУ мусор:

```
;Яч. 167664 (жел.и 167662) должна содерж. 0; 167716 - адр. ПЗП.
A662:   .WORD   0,0     ;чтоб читались 177662,664
```

Во всех остальных режимах ПЗУ контроллера видно только с `160000` (а в ОЗУ10, All,
ОЗУ11, Hlt10, Hlt11 — не видно вовсе, там сегменты ДОЗУ).

Тип машины (БК-0010 или БК-0011М) контроллер **распознаёт автоматически при
включении питания** и сразу устанавливает подходящий рабочий режим [АльтПро].

Из-за того, что контроллер сам подставляет адрес запуска, известная доработка
«СУ + Reset» с СМК-512 не работает: контроллер помещает в `177716` своё стартовое
значение [ЭМ].

### Клавиши при рестарте

Прошивки СМК-64…СМК-512 позволяют выйти из программы, минуя загрузку [АльтПро].
Клавишу нужно удерживать **не менее секунды** с момента отпускания кнопки
перезапуска:

| Клавиша | Действие |
| :-: | --- |
| `!` | Выход в ДОС без перезагрузки («перезапуск на 100000») |
| `КТ` | Выход в монитор |
| `"` `#` `$` `%` | Передача управления на `120000`, `130000`, `140000`, `150000` |
| `&` `'` | Передача управления на `137000`, `157000` |

---

## 17.10. Теневое ОЗУ на странице регистров

Строка Табл. 1 «`170(177000hdd)-0`» описывает область `177000–177777` **за вычетом**
регистров НЖМД `177740–177757` (в [РТ5] она помечена «177 кр.740-760»). Туда, в
зависимости от режима, подключается:

| Режим | Что подключено | Константа [РТ5] |
| --- | --- | :-: |
| SYS | ПЗУ контроллера | `P` |
| All | сегмент 3, **только чтение** | `OR` |
| Hlt10, Hlt11 (и ОЗУ10 у старых контроллеров) | сегмент 7, **только запись** | `IH` |
| Std10, Std11, ОЗУ11 (и ОЗУ10 у новых) | ничего | `N` |

Смысл «только записи» разобран в §17.7: чтение продолжает идти из настоящих
регистров БК, а запись дублируется в ОЗУ, что и даёт нужное HALT-режиму ОЗУ по
адресу `177674`. Отсюда буква «h» в именах режимов [РТ5].

Побочный эффект теневой записи, о котором предупреждает [АльтПро]:

> При включении режима "All" из режимов Hlt10, Hlt11 (а на "старых" контроллерах
> ещё и из режима ОЗУ10) в ОЗУ режима "All" по адресу 177130 вписывается
> `<20 + код страницы>`; при использовании запросов BIOS таких переключений не
> бывает.

В реплике СМК-512, начиная с версии прошивки ПЛИС 1.3, в теневой области сделана
«дыра» по чтению для адресов `177660`, `177662`, `177664` — чтобы не возникало
конфликтов при чтении регистров клавиатуры и скролла [CPLD].

---

## 17.11. BIOS: ROM-BIOS и RAM-BIOS

Работа с дополнительной памятью «по-хорошему» ведётся не прямой записью в
`177130`, а через диспетчер, названный по аналогии с IBM **BIOS** [АльтПро]. Он
состоит из двух частей:

* **ROM-BIOS** — точки входа в ПЗУ контроллера **выше `167000`**; эта часть
  объявлена неизменяемой;
* **RAM-BIOS** — собственно обработка запросов, загружается в ОЗУ (в системную
  страницу доп. ОЗУ). Если RAM-BIOS отсутствует, большинство запросов вызывает
  характерный сигнал ошибки — затухающее подёргивание экрана с прерывистым
  звуковым сигналом.

BIOS обеспечивает бесконфликтное выделение и освобождение памяти, пересылку и обмен
массивов, чтение/запись слова, вызов подпрограммы из доп. памяти, безусловную
передачу управления, смену режима.

### Три уровня запросов

| Уровень | Адресация | Скорость |
| --- | --- | --- |
| **Высокий** | по **имени модуля** — поиск во внутреннем каталоге BIOS с подстановкой физического адреса | медленнее всего |
| **Средний** | по **логическому номеру сегмента** (сегменты по `20000` со сквозной нумерацией; несколько особых — короче) | заметно быстрее: нет поиска в каталоге |
| **Низкий** | по **коду режима и физическому адресу** | максимально быстро в рамках BIOS |

Выделять память можно **только на высоком уровне**; выделенный фрагмент называется
**модулем**. Имя модуля — одно слово из двух символов: старший байт — идентификатор
автора, младший — сам модуль. Если старший байт нулевой, это запрос среднего уровня
и аргумент — логический номер сегмента; номера `376` и `377` означают текущий режим
и DOS-режим. Чтобы работать с модулем на среднем уровне, его надо создать с
признаком неперемещаемости и отдельным запросом получить логический номер сегмента.

Даже низкий уровень стоит около 40 подготовительных операций (сохранение и
восстановление регистров и вектора 4, блокировка «СТОП», распознавание запроса,
переключение режимов) плюс до 20 операций на разбор аргументов. Поэтому там, где
действительно нужна скорость, рекомендуется подключить модуль запросом, поработать
с ним напрямую и вернуться. Для самого быстрого переключения есть запрос
«сверхнизкого» уровня `CH$WRK` (19 операций) — он почти ничего не сохраняет и не
проверяет, но обязательно вписывает код нового рабочего режима в ячейку `@WRK$`,
без чего сломаются возвраты и внешние прерывания.

При всех запросах, кроме особо оговорённых, **стек не должен находиться выше
`100000`**; для корректного создания нового стека служит запрос `NEW$SP`.

При вызове подпрограмм BIOS можно использовать **только абсолютную адресацию**.

### Стандартные адреса ROM-BIOS

Приложение 8 [АльтПро], дословно:

```
BEGIN$=167342   ;начало ROM-BIOS; здесь подпрограммы пересылки массивов,
                ;при переносе ROM-BIOS в ОЗУ эту часть можно не копировать
BEG$R5=167460   ;начало точек входа запросов BIOS - эту часть и всё ниже
                ;при переносе ROM-BIOS в ОЗУ копировать обязательно
STP$=167712     ;подпрограмма блокировки кнопки СТОП
ARTI$=167714    ;в этой ячейке находится команда RTI
DIR1$=167720    ;3 "прямых" точки входа (не по JSR R5,...) - например,
DIR2$=167724    ;для обработки прерываний в доп. ОЗУ
DIR3$=167730
MOD$S=167744    ;3 точки входа переключения режимов на низком уровне
MOD$J=167752    ;(используются при заблокированных прерываниях)
MOD$=167754
VERS$=167776    ;номер версии ПЗУ и код контроллера

                ;ТОЧКИ ВХОДА ЗАПРОСОВ по  JSR R5,@#_имя_
JMP$G=167515    CAL$G=167516    CHW$G=167517    MOV$G=167520
CAT$=167521     CAL$N=167522    JMP$N=167523    RDW$N=167524
CHW$N=167525    MOV$N=167526    EXIT$=167527    SAVE$=167530
RESTO$=167531   NEW$SP=167630   CH$WRK=167734
```

Суффикс `$G` — запрос высокого/среднего уровня (аргумент `NASEG` — имя модуля либо
логический номер сегмента), `$N` — низкого (код режима и физический адрес).

### Ячейки RAM-BIOS

RAM-BIOS живёт в системной странице доп. ОЗУ:

```
R$VERS=120002   ;номер версии RAM-BIOS
ERR$=120004     ;мл. байт - код ошибки, ст. байт = 111
PZP$=120006     ;адрес подпрограммы обработки нестандартного перезапуска
CTRL0$=120010   ;содержит #120012 - проверка целостности начала RAM-BIOS
MAIN$=120012    ;адрес диспетчера запросов
WRK$=120016     ;указатель стека режимов
DOS$=120020     ;указатель на ячейку с кодом DOS-режима
```

⚠ [АльтПро] утверждает, что RAM-BIOS «всегда в 0-й странице», тогда как ПЗУ v2.05
переключается в «системную страницу» кодом `2160` — SYS плюс код страницы `2000`,
то есть страница 1 [ПЗУ-СМК]. Расхождение оставлено неразрешённым (§16.5).

---

## 17.12. Контроллер жёсткого диска

Дисковая часть у «винтовых» контроллеров состоит из двух половин: НГМД — это
штатный БМК 1801ВП1-128 с регистрами `177130`/`177132`, полностью описанный в
§14.2г; НЖМД — собственный узел контроллера.

> Плата контроллера выполняет функции формирователя интерфейсных сигналов,
> дешифратора адресов для регистров НЖМД, имеет своё ПЗУ (160000-167777) со слегка
> модифицированной 326-прошивкой и базовым драйвером НЖМД и статическое ОЗУ
> (170000-176777). [АльтПро]

Ограничения: накопитель до **2 Гбайт** (63 сектора, 16 головок, 4095 дорожек),
разбиение не более чем на **124 логических диска** произвольного объёма (от 5 Кбайт
до 32 Мбайт каждый).

### Регистры НЖМД

| Адрес | Чтение | Запись |
| :-: | --- | --- |
| `177740` | статус | команда |
| `177742` | выбор НЖМД и головки | то же |
| `177744` | ст. байт номера дорожки | то же |
| `177746` | мл. байт номера дорожки | то же |
| `177750` | номер сектора | то же |
| `177752` | счётчик секторов | то же |
| `177754` | код ошибки | не используется |
| `177756` | данные чтения | данные записи |
| `177741` | адрес накопителя | не используется |
| `177743` | 2-й статус | сброс |

Три замечания [АльтПро], без которых с этими регистрами работать нельзя:

1. `177741` — **не старший байт** `177740`, а самостоятельный регистр по нечётному
   адресу; то же и для `177743`.
2. У НЖМД и БК разные понятия о 0 и 1 на шине (единице на шине БК соответствует
   низкий уровень напряжения, а на шине НЖМД — высокий), поэтому под «бит
   установлен» следует понимать его отсутствие: **все данные представлены в
   инверсном коде**, и все коды команд, ошибок и констант ниже даны уже
   проинвертированными, «готовыми к употреблению».
3. **Байтовые команды записи в ОЗУ платы контроллера не работают.**

Обмен данными через `177756` идёт **словами**; байты ECC при длинных операциях
передаются байтами.

### Регистр состояния `177740` (чтение)

| Разряд | Имя | Значение |
| :-: | :-: | --- |
| 0 | `ERR` | предыдущая команда закончилась с ошибкой; сбрасывается записью новой команды |
| 1 | `INX` | индекс (оборот диска); в современных моделях не используется |
| 2 | `CORR` | ошибка при чтении скорректирована аппаратурой ECC; передачу не прекращает |
| 3 | `DRQ` | запрос на обмен данными с буфером сектора |
| 4 | `DCS` | поиск завершён |
| 5 | `WFT` | неисправность накопителя или некорректные параметры записи |
| 6 | `DRDY` | накопитель готов принять команду |
| 7 | `BSY` | выполняется команда или диагностика; **остальные разряды недействительны** |

### Регистр ошибок `177754`

Значения действительны после команды, если установлен `ERR`, а также после
внутренней диагностики при включении питания. Коды диагностики: `376` — ошибок нет,
`375` — ошибка микроконтроллера, `374` — буферного ОЗУ, `373` — аппаратуры ECC,
`372` — микропроцессора НЖМД; прочие коды — полная неисправность.

| Разряд | Имя | Значение |
| :-: | :-: | --- |
| 0 | `AMNF` | не найден адресный маркер данных при верном идентификаторе |
| 1 | `T0NF` | не найдена дорожка 0 (только в «Рекалибровке») |
| 2 | `ABRT` | команда прервана: write fault, not seek complete, drive not ready либо недопустимый код команды |
| 4 | `IDNF` | не найден цилиндр/головка/сектор либо ошибка ECC в поле идентификатора |
| 6 | `UNC` | некорректируемая ошибка ECC в поле данных |
| 7 | `BBK` | в идентификаторе найдена метка дефектного сектора |

Разряды 3 и 5 не используются.

### Регистр `177742` — выбор НЖМД и головки

Разряды 0–3 — двоичный код головки, разряд 4 — выбор накопителя (`0` — НЖМД 0,
`1` — НЖМД 1). С учётом инверсии: `377` — головка 0, привод 0; `376` — головка 1,
привод 0 и т. д. **Базовый драйвер в ПЗУ поддерживает только один привод**; для
двух нужен драйвер пользователя в ОЗУ платы (§17.13).

### Регистр сброса `177743` (запись)

Разряд 1 — разрешение прерывания от НЖМД (в БК не используется); разряд 2 —
программный сброс (сначала установить, затем сбросить); разряд 3 — разрешение
выбора головок 8–15. Разряды 0 и 4–7 не используются. Чтение `177743` даёт ту же
информацию, что и регистр состояния, но не сбрасывает установленное прерывание
IRQ14.

### Регистр `177752` — счётчик секторов

Содержит количество секторов для операции **в инверсном виде**: `376` — один
сектор, `375` — два и т. д. После ошибки в регистре остаётся число непереданных
секторов, после успешного завершения — `377`.

### Регистр `177750` — номер сектора

Нумерация секторов **с единицы**: `376` — 1-й сектор, `375` — 2-й. После выполнения
команды содержит номер последнего обработанного сектора либо сектора с ошибкой.

### Коды команд (восьмеричные, уже инвертированные)

| Команда | Код |
| --- | :-: |
| Рекалибровка | `357` |
| Чтение секторов | `33X` |
| Запись секторов | `31X` |
| Верификация | `27X` |
| Форматирование дорожки | `257` |
| Позиционирование | `217` |
| Внутренняя диагностика | `157` |
| Инициализация параметров НЖМД | `156` |
| Чтение буфера сектора | `033` |
| Запись буфера сектора | `027` |
| Идентификация НЖМД | `023` |

Младшая цифра `X`: `7` — с повторами при ошибках, `6` — без повторов, `5` — длинное
с повторами, `4` — длинное без повторов (для верификации только `7` и `6`).
«Длинная» операция добавляет к 256 словам данных ещё 4 байта ECC.

Перед записью кода команды нужно поместить данные в регистры `177742`–`177752` при
сброшенном `BSY`. Команда начинает выполняться сразу после записи кода. По
установленному `DRQ` программа принимает или передаёт **256 слов** через регистр
данных.

Команда «Инициализация параметров» (`156`) выполняется после сброса: регистры
`177742` и `177752` должны содержать максимальный номер головки (счёт с 0) и
максимальное число секторов на дорожке (счёт с 1).

### Паспорт НЖМД (команда `023`)

Занимает один сектор (256 слов):

| Смещение | Содержимое |
| :-: | --- |
| `0` | главное слово конфигурации |
| `2` | количество дорожек |
| `6` | количество головок |
| `10` | количество байт на дорожке |
| `12` | количество байт в секторе |
| `14` | количество секторов на дорожке |
| `16–23` | спецификация продавца |
| `24–47` | серийный номер (ASCII) |
| `50` | тип контроллера |
| `52` | ёмкость буфера в секторах |
| `54` | количество байт ECC |
| `56–65` | версия рабочих программ (ASCII) |
| `66–135` | модель (ASCII) |

Смещение `4` зарезервировано. При обработке полей `24–47`, `56–65` и `66–135` нужно
делать `SWAB` — и не забывать про инверсию.

---

## 17.13. Драйвер НЖМД и статическое ОЗУ платы

### Точки входа

Блок параметров драйвера НЖМД **полностью идентичен** блоку параметров драйвера
дисковода (§14.2д), точки входа для чтения и записи те же. Под номером привода
теперь понимается: `0`–`1` — дисководы A:, B:, остальное — логические диски НЖМД
(C:, D:, …), нумерация логических дисков с нуля.

| Адрес | Запрос | Параметры |
| :-: | --- | --- |
| `160000` | автоматическая загрузка | нет |
| `160002` | загрузка с выбранного привода | `R0` — номер привода |
| `160004` | чтение/запись по номеру блока | `R0` — начальный блок; `R1` — длина в словах (положительная — чтение, отрицательная — запись); `R2` — адрес массива; `R3` — блок параметров; `34(R3)` — номер привода (0–124) |
| `160006` | чтение/запись по номеру сектора, головки и дорожки | `R3` — блок параметров; `26(R3)` — адрес массива; `30(R3)` — длина в словах; `32(R3)` — мл. 4 разряда номер головки, ст. 4 разряда — разряды 0–3 номера дорожки; `33(R3)` — разряды 4–11 номера дорожки; `34(R3)` — номер привода; `35(R3)` — начальный сектор |
| `160010` | инициализация блока параметров | `R3` — блок параметров |
| `160012` | форматирование | `R3` — блок параметров; координаты как у `160006` |
| `160016` | точка входа эмулятора расширенной арифметики | — |

Особенности `160006`: при чтении номер привода роли не играет, лишь бы был ≥ 2;
**запись возможна только при номере привода `377`**, при значениях от 2 до 376
выдаётся ошибка 1. Разбиение на логические диски не анализируется — доступен весь
накопитель.

После работы подпрограмм, связанных с передачей информации (`160000`–`160006`,
`160012`), в `R4` помещается адрес регистра ошибок — «с целью нейтрализации действий
программ, которые заносят 0 (и не только) в `@R4`».

Коды ошибок стандартные для драйвера дисковода (§14.2д). Специфичные для НЖМД:
ошибка 5 — выход за пределы логического диска, ошибка 3 — логический диск не
определён, ошибка 6 — накопитель не подключён или не отвечает,
**ошибка 13** — не сошлась CRC таблицы разделов и области конфигурации; данные при
этом не передаются, таблица перечитывается, накопитель переинициализируется, а
следующие обращения проходят нормально. Ошибка 13 — сигнал порчи таблицы разделов,
позволяющий выявить программу, которая это сделала.

### Эмулятор расширенной арифметики

В 326-й прошивке драйвер расширенной арифметики оставлен, но ради экономии места
заменён на более короткий: **было 2400 байт, стало 776**, ценой отказа от поддержки
FIS. Эмулируются команды EIS `MUL`, `DIV`, `ASH`, `ASHC`. Чтобы подключить эмулятор,
нужно занести адрес его начала в вектор `10`. Если код команды, вызвавшей
прерывание по резервной команде, не относится к перечисленным, происходит выход по
вектору 4 с установленным разрядом C (стандарт RT-11).

### Служебные ячейки статического ОЗУ

Статическое ОЗУ платы — `170000–176777`. Базовый драйвер использует:

| Ячейка | Имя | Назначение |
| :-: | :-: | --- |
| `176776` | `HD$TRK` | число дорожек НЖМД |
| `176774` | `HD$HEA` | число головок |
| `176772` | `HD$SEC` | число секторов на дорожке |
| `176770` | `HD$LOG` | всего логических дисков (1–124); в старшем байте — номер привода загрузки по умолчанию |
| `176000` | `HD$SPD` | признак повышенной скорости обмена: установлен, если ячейка равна `125252` |
| `175776` | `HD$USR` | указатель на начало драйвера пользователя |
| `175774` | `HD$EIS` | указатель на пользовательский драйвер расширенной арифметики (первая команда — `NOP`, вход по `JMP @175774`) |

`HD$SPD` рекомендуется для современных накопителей — ускоряет обмен вдвое и более
за счёт отказа от ожидания готовности при чтении сектора; на старых накопителях
может привести к сбоям и потере информации.

Полями `HD$TRK`, `HD$HEA`, `HD$SEC` и `HD$LOG` и определяется «область
конфигурации», CRC которой проверяется при каждом обращении к диску.

### Распределение доп. ОЗУ `170000–175770`

Программы занимают и освобождают участки в произвольном порядке, поэтому принята
дисциплина:

* резиденты в доп. ОЗУ должны быть **перемещаемыми**;
* захватывать надо свободную область начиная с первой же свободной ячейки; первое
  слово свободного участка равно 0;
* в первое слово занимаемого участка вписывается его длина в байтах, в ячейку над
  участком заносится 0; со второго слова идёт список адресов ячеек-указателей, через
  которые адресуется этот участок, список оканчивается нулём; далее — тело
  резидента.

Программа, освобождающая участок ниже чужого резидента, обязана сдвинуть все
вышележащие резиденты вниз, предварительно вычтя величину смещения из содержимого
ячеек-указателей из их списков.

### Подключаемый драйвер пользователя

Драйвер может лежать где угодно в ОЗУ; подключается записью адреса его начала в
`175776`. Первая команда — `NOP`, далее подряд точки входа для `160004`, `160006`,
`160012`. Управление передаётся сразу после вызова базового драйвера — без разбора
параметров, без анализа номера привода и без подсчёта контрольной суммы. Признак
ошибки драйвер помещает в `57(R3)`. Способы возврата:

| Возврат | Что происходит |
| --- | --- |
| `RTS PC` | обычный выход из базового драйвера с установкой разряда C и номера ошибки, если `57(R3)` ненулевое |
| `ADD #2,@SP` + `RTS PC` | управление передаётся драйверу **дисковода** напрямую |
| `ADD #6,@SP` + `RTS PC` | управление передаётся базовому драйверу, как будто драйвера пользователя нет |

Если заменить `RTS PC` на `JSR PC,@(SP)+`, то по окончании работы драйверов
дисковода или НЖМД управление вернётся в драйвер пользователя.

---

## 17.14. Разметка жёсткого диска и загрузка

Под служебные нужды отводится **нулевая сторона нулевой дорожки**.

* Первые **7 блоков** занимает основной загрузчик (**MBR**), который при каждой
  загрузке с винчестера считывается в ОЗУ **с адреса `10000`** и запускается, если
  его первая команда — `NOP` [АльтПро]. ⚠ Сторонняя заметка `АльтПро.txt` говорит
  про 6 блоков и адрес `170000`; приоритет отдан описанию разработчиков (§16.5).
* В **седьмом блоке (восьмом секторе)** хранится копия области ОЗУ
  `176000–177000` — то есть конфигурация и таблица разделов.

### Таблица разделов

Начинается с адреса `176766` и имеет **стековую структуру, растущую вниз**. Каждая
запись о логическом диске — два слова:

* **слово с бо́льшим адресом** — номер дорожки и головки начала логического диска:
  младшие 4 разряда — номер головки, остальные 12 — номер дорожки. Если это слово
  отрицательно (инверсия по `COM`), считается установленной **защита записи**, и при
  попытке записи выдаётся ошибка 1;
* **слово с меньшим адресом** — длина логического диска в блоках (максимум
  65535 блоков = 32 Мбайт).

В слове, следующем за последней записью, лежит CRC таблицы разделов и области
конфигурации — простое сложение без учёта переноса.

Обращаться к таблице напрямую не рекомендуется, для этого есть штатный механизм:
если при вызове `160004` (при `160006` не работает) указать в `R1` нулевое число
слов, драйвер вернёт в `R0` размер логического диска в блоках, в `R1` — номер
дорожки/головки его начала, а по адресу из `R2` (если `R2` ≠ 0) положит четыре
слова: число дорожек, число головок, число секторов на дорожке, число логических
дисков. Заодно вычисляются координаты блока, указанного в `R0` на входе, и
помещаются в блок параметров в формате `160006`. `R2` при этом портится. При
подключённом драйвере пользователя эта функция не работает, если он сам её не
выполняет.

### Алгоритм загрузки

1. Если НЖМД подключён, то при **любой** загрузке (в том числе с дисководов)
   с адреса `176000` считывается 8-й сектор.
2. Проверяется CRC. Если нужно, конфигурация считывается из паспорта НЖМД, а
   таблица разделов инициализируется.
3. Выполняется программный сброс накопителя и команда «Инициализация параметров».
4. Если номер привода не был указан явно, берётся номер по умолчанию (из
   `HD$LOG+1`). Если в итоге номер привода меньше 2 — переход к стандартной загрузке
   с дисковода.
5. С адреса `10000` считывается MBR (первичная загрузка). Если его первая команда —
   `NOP`, управление передаётся на него (`CALL @#10000`); MBR, сделав что нужно
   (переконфигурация служебной области, установка драйверов пользователя и т. д.),
   возвращает управление драйверу по `RTS PC`.
6. В основную память с адреса `1000` считывается нулевой блок устройства с номером
   из `34(R3)`, и загрузка продолжается по стандартному алгоритму драйвера дисковода
   (вторичная загрузка).

При ошибке загрузка продолжается с дисководов A и B — если загрузка автоматическая;
в противном случае прекращается.

Для контроллеров SMK64–SMK512 версии 2.00 и выше нужно хотя бы один раз запустить
программу `SETMBR` для установки MBR винчестера [АльтПро].

---

## 17.15. Программирование: типовые приёмы

### Переключение страницы

Из демо `leCSIcon` (CSI, 2023),
`demos/leCSIcon_demo_2023_by_CSI/source/part1/PART1smk.mac` — заливка страниц 2 и 3
в линейном режиме ОЗУ10 и возврат в штатный режим БК-0011М:

```
        MOV     #177130, R5
        MOV     #6, (R5)
        MOV     #4 + 120, (R5)          ; страница 2 + режим ОЗУ10
        MOV     #0, (R5)
        MOV     #100000, R0             ; и заливаем 32 Кбайт подряд
        ...
        MOV     #6, (R5)
        MOV     #2004 + 120, (R5)       ; страница 3 + ОЗУ10
        MOV     #0, (R5)
        ...
        MOV     #6, (R5)
        MOV     #140, (R5)              ; возврат в Std11
        MOV     #0, (R5)
```

Демо занимает страницы 2…5, оставляя 0 и 1 системе.

### Определение машины и контроллера

Приём [Мнв]: сначала различить БК-0010 и БК-0011 по старшему байту `177717`, затем
проверить наличие и версию контроллера:

```
DetectSystem:
        CMPB    @#177717,#300           ; BK 0010 or BK 0011?
        BEQ     2                       ; на БК-0011 ничего не делаем
        CMP     @#167776,#176200        ; проверка версии СМК
        BHIS    1
            MOV #NoMem,MemPage          ; БК-0010 без СМК
            NOP
            RET
1:      MOV     #Switch10,MemPage       ; БК-0010 с СМК
2:      RET
MemPage: .WORD  Switch11
```

Константа `176200` соответствует «не менее 64 Кбайт»; для требования «не менее
128 Кбайт» её заменяют на `176600`, для «не менее 256 Кбайт» — на `177200`.

### Одна программа для БК-0010 и БК-0011М

Страницы дополнительной памяти на БК-0011М начинаются с адреса `100000`, а на
БК-0010 с СМК в штатных режимах — со `120000`. Поэтому удобно писать под БК-0011,
а на БК-0010 прибавлять к адресам `20000` [Мнв]:

```
Switch11:
        ASL     R0
        MOV     Mem11(R0),@#177716
        RET
Mem11:  .WORD   16000,14000,15000,15400,17400,16400

Switch10:
        ASL     R0
        MOV     #6,@#177130
        MOV     MemSMK(R0),@#177130
        MOV     #0,@#177130
NoMem:  RET
MemSMK: .WORD   60,2060,160,2160,64,2064
```

Здесь используются режимы SYS и Std10, то есть окно `120000–157777` по 16 Кбайт;
на СМК-64 доступны только первые две «страницы» такого вида. Если перейти на режим
ОЗУ10 (`120`) с ОЗУ вместо ПЗУ монитора, окно вырастает до 28 Кбайт, но расход
памяти и требования к модели контроллера выше. В режиме All (`20`) через
`SwitchSMK` перебираются все 16 страниц СМК-512.

---

## 17.16. Современные реализации

### Реплика СМК-512

Каталог `SMK-512/` содержит полный комплект для самостоятельного изготовления:
исходники прошивки ПЛИС (версии 1.0, 1.1, 1.3), схему, трассировку (v1.0, v2.1,
v3.0), герберы, дамп ПЗУ и перечень элементов. Логика 556РТ5 перенесена в
`RT5.vhd`; состав платы и её узлы разобраны в §17.2 по схеме [Сх512].

Реплика ведёт себя как **новая** версия контроллера в терминах Табл. 1 (§17.8) и
требует **точного** кода строба (§17.5). Разряд 03 «ПЗУ Бейсика» в ней не
реализован, и это видно по исходнику: выходы `BAS` и `BAS2` (контакты А14 и А29)
в версии 1.3 прошиты константой «неактивно», а строки, где они управлялись
разрядом 03, оставлены закомментированными [CPLD].

Три отличия реплики от оригинального СМК-64, каждое подтверждено исходником:

| Что | Оригинал | Реплика |
| --- | --- | --- |
| Две спорные ячейки Табл. 1 | «старая» версия (`7W`, `6R`) | «новая» (`—`, `6`) — §17.8 |
| Опознание строба | по маске разрядов 01 и 02 | точное значение `6` в младшей тетраде |
| Разряд 03 → ПЗУ Бейсика | работает | не реализован |

### Gryphon-MPI

Контроллер-супервизор на STM32 с ПЛИС, подключается в разъём МПИ. Заявлено, что он
**эмулирует контроллеры «АльтПро» СМК-512 и А-16** (доп. ОЗУ, дисковод, жёсткий
диск), а также ПЗУ 1801РЕ2, блок МСТД и блок ИРПС; в ремонтном режиме умеет
показывать «состояние блокировок, страниц памяти, режимов АльтПро». В веб-интерфейсе
прошивки СМК — отдельное устройство с двумя НГМД и одним НЖМД.

**Собственной побитовой документации на регистры Gryphon-MPI не публикует**, и
объём эмулируемого ДОЗУ явно не указан; предполагается функциональная совместимость
с СМК-512, то есть всё, что описано выше. Материалы — `~/Gryphon-MPI/`,
https://night-gryphon.ru/Gryphon-MPI/.

### AZ-БК

Контроллер с SD-картой, VGA, Ethernet и своим менеджером памяти. СМК в нём не
отдельный узел, а **режим трансляции**: запросы к `177130` (СМК) и к `177716`
(БК-0011М) переводятся в единый маппер AZ с гранулярностью окон 4 Кбайт,
совпадающей с сегментом СМК; страницы `200`–`377` маппера отведены под эмуляцию
СМК-512 [MAXIOL]. Чтение `177130` не поддерживается. Есть отдельные регистры
`177350` и `177352` — копии последней записи в `177130` и `177716` соответственно.

Для совместимости с «хаком» А22 (§17.3) на нижней плате AZ-БК предусмотрена
доработка: отпаять резистор `R14` и подвести проводок от ноги ПЛИС; наличие
доработки определяется автоматически. ⚠ Известное ограничение: в AZ-БК нет режима
обращения к ОЗУ «только запись» и не эмулируется участок `177000–177777`, что прямо
конфликтует со столбцами `7W`, `0R`, `6R` Табл. 1.

---

## 17.17. Поддержка в эмуляторах

| Эмулятор | Поддержка | Подробности |
| --- | --- | --- |
| **BKemu / GID** (`GID-src/`, `bkemu-QT/`) | полная: СМК-512 и A16M | Конфигурации `BK-0010-01_SMK512`, `BK-0011_SMK512`, `BK-0011M_SMK512`; отдельные функции `SMK512_MemManager_10()` и `_11M()`; в отладчике — 16 окон `SMK:00 (0000)` … `SMK:15 (2015)` по 32 Кбайт и дамп регистров режимов АльтПро |
| **BK2010** (Java) | только в конфигурации БК-0011М | ПЗУ `roms/smk512.rom` идентично `DISK_SMK512_v2.05.rom`; ⚠ иная перестановка разрядов номера страницы и переставленные имена регистров дорожки (§16.5) |
| **bkemu-android** | есть: доп. память и IDE-контроллер | `SmkMemoryManager.java`, `SmkIdeController.java` |
| **BKBTL** | нет | упоминаний СМК в проекте не найдено |

Имена ПЗУ в `bk.ini` эмулятора: `FDD SMK512 = DISK_SMK512_v2.05.rom`. Заметьте, что
в BKemu **нет отдельных типов СМК-64 и СМК-256** — есть один тип `SMK512`, а
различие целиком в подставленном образе ПЗУ: программа увидит объём, зашитый в
байт `167777`, но эмулятор физически всегда даст 16 страниц.

Тайминги: доступ к ОЗУ и ПЗУ СМК в BKemu считается без ожиданий, в отличие от
динамического ОЗУ самой БК (§15, арбитраж КР1801ВП1-037).

---

## 17.18. Что осталось за рамками

Не найдено и потому не описано:

* файл `СХЕМЫ.EDP`, на который ссылается [АльтПро] в части доработок БК для
  отключения ПЗУ монитора: на исследованном образе диска его нет;
* обещанные [АльтПро] «примечания специального характера… для тех, кто пишет
  некорректные программы» — в доступных перепечатках Приложения 1 текст
  обрывается на общих пояснениях;
* полное описание всех сорока с лишним запросов BIOS с форматами аргументов —
  здесь дан только обзор архитектуры и таблица точек входа; исходный документ
  (`ALTBIOS1..3.EDP`, `GID-src/BKemu/Docs/ALTBIOS.html`) содержит их целиком;
* побитовое описание регистров Gryphon-MPI (публичной документации нет).

Взаимодействия с экранным ОЗУ и с режимом расширения памяти БК («малый экран»,
§11, §8) у контроллера **нет по построению**: дешифратор включается только при
`A15 = 1`, то есть на адреса `0–77777` контроллер не отзывается вовсе [CPLD].
Экранное ОЗУ и разряд 09 регистра `177664` целиком остаются под управлением самой
БК. Единственная точка соприкосновения — попадание регистров `177662`/`177664` в
зеркало ПЗУ и в теневое ОЗУ (§17.9, §17.10).
