# 4. Система команд

> **Источники главы**
> * **[РСП, Прил. 1]** «Список команд микро-ЭВМ» (с. 55–58) — нормативный перечень:
>   мнемоника, код, влияние на признаки. **Это исчерпывающий список того, что реализовано
>   в БК-0010 аппаратно.**
> * **[З]** «Способы адресации», «Оператор прямого присваивания», «Операторы ассемблера»,
>   «Операторы управления машиной», «Псевдооператоры», «Временные характеристики команд
>   БК-0010» — семантика, практика, тайминги.
> * **[ВМ1]** «Однокристальный микропроцессор К1801ВМ1» § 3, 9, 10 —
>   независимая проверка кодов и флагов по реверс-инжинирингу кристалла,
>   зарезервированные коды, **обнаруженные аппаратные ошибки**.
> * **[ПЗУ]** — коды команд сверены с фактическими листингами ПЗУ МДС.

Процессор К1801ВМ1 реализует **подмножество системы команд PDP-11**. Ниже — полный список
того, что есть в БК-0010(-01), и явное указание того, чего в нём нет.

---

## 4.1. Способы адресации

Аппаратно способов адресации **восемь** (коды 0…7); все они так или иначе используют один
из РОН. Четыре дополнительных «способа», принятых в языке ассемблера, — это те же коды
2, 3, 6, 7 с регистром **PC (R7)** в роли указателя.

### Основные способы [З]

| Код | Наименование | Обозначение | Что адресует |
| --- | --- | --- | --- |
| 0 | Регистровая (прямая) | `Rn` | Сам регистр |
| 1 | Регистровая косвенная | `@Rn`, `(Rn)` | Ячейку, адрес которой в Rn |
| 2 | Автоинкрементная косвенная | `(Rn)+` | Ячейку по Rn, **после** — Rn += 1 или 2 |
| 3 | Автоинкрементная двойная косвенная | `@(Rn)+` | Ячейку по адресу, лежащему по Rn; **после** — Rn += 2 |
| 4 | Автодекрементная косвенная | `-(Rn)` | **Сначала** Rn −= 1 или 2, затем ячейку по Rn |
| 5 | Автодекрементная двойная косвенная | `@-(Rn)` | Сначала Rn −= 2, затем ячейку по адресу, лежащему по Rn |
| 6 | Индексная | `X(Rn)`, `MET(Rn)` | Ячейку по адресу Rn + X (X — второе/третье слово команды) |
| 7 | Индексная косвенная | `@X(Rn)`, `@MET(Rn)` | Ячейку по адресу, лежащему по Rn + X |

Модификация в кодах 2 и 4 — **на 1 для байтовых операций, на 2 для словных**.
Исключение: **`SP` и `PC` модифицируются всегда на 2**, независимо от формы оператора.

### Способы с использованием PC [З]

| Код | Наименование | Стандартная запись | Специальная запись |
| --- | --- | --- | --- |
| 2 | Непосредственная | `(PC)+` | `#X`, `#MET` |
| 3 | Абсолютная | `@(PC)+` | `@#X` |
| 6 | Относительная | `MET(PC)` | `MET` |
| 7 | Относительная косвенная | `@MET(PC)` | `@MET` |

* **Непосредственная** `#X` — операнд-константа записывается вторым/третьим словом команды.
* **Абсолютная** `@#X` — прямое указание абсолютного адреса. Применяется для обращения
  к системной области, системным регистрам и ПЗУ.
* **Относительная** `MET` — ассемблер вычисляет смещение (адрес метки − адрес команды),
  адрес операнда = PC + смещение. Смещение в дополнительном коде, поэтому адресация
  возможна и «вперёд», и «назад». Основной способ обращения к переменным внутри программы.
* **Относительная косвенная** `@MET` — по адресу метки лежит адрес операнда.

### Практические замечания [З]

* **Двойной косвенной адресации без модификации (`@(Rn)`) в системе команд нет.**
  Её эмулируют записью `@0(Rn)` — команда занимает два слова, второе из которых нуль.
* **Символьные константы.** Развитые ассемблеры (МИКРО.10К) допускают запись символов
  в апострофах: один символ → его код в младший байт, два символа → первый в младший байт,
  второй в старший. Пример: `MOV 'A=',R0`.
* ⚠ **Распространение знака при `MOVB` в регистр.** Если приёмником байтовой команды `MOVB`
  является РОН, все разряды его старшего байта принимают значение разряда 07 младшего.
  `MOVB #377,R0` даёт в R0 **177777**, а не 377. При байтовых операциях с ячейками ОЗУ
  этого не происходит.
* Метки в операндах должны быть определены в тексте программы либо заданы оператором
  прямого присваивания. **Локальные метки** (начинающиеся с цифры) пригодны только
  для указания адресов переходов в операторах ветвления и цикла — отличить локальную метку
  от числа-индекса ассемблер не может.
* Индексная адресация с меткой в роли индекса — основа **табличной перекодировки**:
  вся перекодировка сводится к одной команде `MOVB TAB(R2),R2` плюс таблице
  `TAB: .B:4,3,2,1,0,7,6,5`.

### Знаменитый «червяк» [З]

```asm
MOV -(PC),-(PC)         ; код 014747
```

Команда переписывает свой код по адресу на 2 меньше и туда же передаёт управление,
«размножаясь» в сторону младших адресов. Заполняет всю память примерно за **0,2 с**,
затирая системную область и стек; машина перестаёт реагировать даже на «СТОП»,
помогает только системный сброс. Проверить можно в МСД: `77776А14747И`, затем `77776G`.

Команды, «размножающейся вверх» по памяти, не существует.
«Бег на месте»: `MOV -(PC),@PC`.

---

## 4.2. Список команд микро-ЭВМ [РСП, Прил. 1]

Обозначения в кодах:

| Символ | Значение |
| --- | --- |
| `R` | Номер РОН (3 разряда) |
| `SS` | Поле адресации операнда-источника (6 разрядов: 3 — способ, 3 — регистр) |
| `DD` | Поле адресации операнда-приёмника (6 разрядов) |
| `NN` | Смещение (6 разрядов) |
| `*` | **1** для байтовой операции, **0** для словной |

Обозначения в колонке признаков (N Z V C):
`+` — устанавливается по результату, `0` — сбрасывается, `1` — устанавливается,
`−` — не изменяется.

> **О точности кодов.** Таблица снята со скана [РСП, Прил. 1]; в нескольких местах
> отдельные восьмеричные цифры читаются неуверенно. Такие коды приведены
> по каноническим значениям PDP-11, с которыми скан согласуется, и **дополнительно
> сверены с фактическими листингами ПЗУ МДС** [✓ ПЗУ]: `012706` = `MOV #1000,SP`,
> `016605` = `MOV 2(SP),R5`, `042705` = `BIC #…,R5`, `004715` = `JSR PC,@R5`,
> `106427` = `MTPS #0`, `000207` = `RTS PC`, `077203` = `SOB R2,…`, `104014` = `EMT 14`,
> `001413` = `BEQ`, `000002` = `RTI`.

### Одноадресные (однооперандные) команды

| Мнемоника | Команда | Код | N | Z | V | C |
| --- | --- | --- | --- | --- | --- | --- |
| `CLR(B)` | Очистка | `*050DD` | 0 | 1 | 0 | 0 |
| `COM(B)` | Инвертирование | `*051DD` | + | + | 0 | 1 |
| `INC(B)` | Прибавление единицы | `*052DD` | + | + | + | − |
| `DEC(B)` | Вычитание единицы | `*053DD` | + | + | + | − |
| `NEG(B)` | Изменение знака | `*054DD` | + | + | + | + |
| `ADC(B)` | Прибавление переноса | `*055DD` | + | + | + | + |
| `SBC(B)` | Вычитание переноса | `*056DD` | + | + | + | + |
| `TST(B)` | Проверка | `*057DD` | + | + | 0 | 0 |
| `ROR(B)` | Циклический сдвиг вправо | `*060DD` | + | + | + | + |
| `ROL(B)` | Циклический сдвиг влево | `*061DD` | + | + | + | + |
| `ASR(B)` | Арифметический сдвиг вправо | `*062DD` | + | + | + | + |
| `ASL(B)` | Арифметический сдвиг влево | `*063DD` | + | + | + | + |
| `SXT` | Расширение знака | `0067DD` | − | + | 0 | − |
| `SWAB` | Перестановка байтов | `0003DD` | + | + | 0 | 0 |
| `MFPS` | Чтение ССП | `1067DD` | + | + | 0 | − |
| `MTPS` | Запись ССП | `1064SS` | + | + | + | + |

### Двухадресные (двухоперандные) команды

| Мнемоника | Команда | Код | N | Z | V | C |
| --- | --- | --- | --- | --- | --- | --- |
| `MOV(B)` | Пересылка | `*1SSDD` | + | + | 0 | − |
| `CMP(B)` | Сравнение | `*2SSDD` | + | + | + | + |
| `BIT(B)` | Проверка разрядов | `*3SSDD` | + | + | 0 | − |
| `BIC(B)` | Очистка разрядов | `*4SSDD` | + | + | 0 | − |
| `BIS(B)` | Логическое сложение (ИЛИ) | `*5SSDD` | + | + | 0 | − |
| `ADD` | Сложение | `06SSDD` | + | + | + | + |
| `SUB` | Вычитание | `16SSDD` | + | + | + | + |
| `XOR` | Исключающее ИЛИ | `074RDD` | + | + | 0 | − |

> `XOR` — единственная двухоперандная команда, у которой **первый операнд может быть
> только РОН** (`074RDD`). Запись вида `XOR #1000,@#2400` ассемблером не транслируется.
> Байтовых форм `ADD`, `SUB`, `XOR` не существует.

### Команды управления программой

| Мнемоника | Команда | Код | Условие |
| --- | --- | --- | --- |
| `BR` | Ветвление безусловное | `000400` | — |
| `BNE` | Не равно (нулю) | `001000` | Z = 0 |
| `BEQ` | Равно (нулю) | `001400` | Z = 1 |
| `BGE` | Больше или равно (нулю) | `002000` | N ⊕ V = 0 |
| `BLT` | Меньше (нуля) | `002400` | N ⊕ V = 1 |
| `BGT` | Больше | `003000` | Z + (N ⊕ V) = 0 |
| `BLE` | Меньше или равно (нулю) | `003400` | (N ⊕ V) + Z = 1 |
| `BPL` | Плюс | `100000` | N = 0 |
| `BMI` | Минус | `100400` | N = 1 |
| `BHI` | Больше | `101000` | Z + C = 0 |
| `BLOS` | Меньше или равно | `101400` | Z + C = 1 |
| `BVC` | Нет арифметического переноса | `102000` | V = 0 |
| `BVS` | Арифметический перенос | `102400` | V = 1 |
| `BCC` / `BHIS` | Нет переноса / больше или равно | `103000` | C = 0 |
| `BCS` / `BLO` | Перенос / меньше | `103400` | C = 1 |
| `JMP` | Безусловный переход | `0001DD` | — |
| `JSR` | Обращение к подпрограмме | `004RDD` | — |
| `RTS` | Возврат из подпрограммы | `00020R` | — |
| `MARK` | Восстановление УС (указателя стека) | `0064NN` | — |
| `SOB` | Вычитание единицы и ветвление | `077RNN` | — |

Все команды ветвления однословные; смещение — 8 разрядов со знаком, в словах.
`BGE`/`BLT`/`BGT`/`BLE` учитывают **знак** (для чисел со знаком), `BHI`/`BLOS`/`BHIS`/`BLO`
— беззнаковое сравнение.

`CALL N` и `RET` в ассемблерах БК — синонимы `JSR PC,N` и `RTS PC`.

### Команды прерывания программы

| Мнемоника | Команда | Код | Вектор |
| --- | --- | --- | --- |
| `EMT` | Командное прерывание для системных программ | `104000`…`104377` | 30 |
| `TRAP` | Командное прерывание | `104400`…`104777` | 34 |
| `IOT` | Командное прерывание для ввода-вывода | `000004` | 20 |
| `BPT` | Командное прерывание для отладки | `000003` | 14 |
| `RTI` | Возврат из прерывания | `000002` | — |
| `RTT` | Возврат из прерывания (отладочного) | `000006` | — |

### Команды управления машиной

| Мнемоника | Команда | Код |
| --- | --- | --- |
| `HALT` | Останов (фактически — прерывание по вектору 4) | `000000` |
| `WAIT` | Ожидание прерывания | `000001` |
| `RESET` | Сброс внешних устройств | `000005` |

### Команды изменения признаков

| Мнемоника | Команда | Код |
| --- | --- | --- |
| `NOP` | Нет операции | `000240` |
| `CLC` | Очистка C | `000241` |
| `CLV` | Очистка V | `000242` |
| `CLZ` | Очистка Z | `000244` |
| `CLN` | Очистка N | `000250` |
| `CCC` | Очистка всех разрядов условий | `000257` |
| `SEC` | Установка C | `000261` |
| `SEV` | Установка V | `000262` |
| `SEZ` | Установка Z | `000264` |
| `SEN` | Установка N | `000270` |
| `SCC` | Установка всех разрядов условий | `000277` |

Младшие четыре разряда кода (3, 2, 1, 0) соответствуют разрядам N, Z, V, C. Поэтому
можно устанавливать и сбрасывать **произвольную комбинацию** признаков, а не только
по одному: код `000243` сбрасывает одновременно V и C (неофициальная мнемоника `CLVC`),
код `000275` устанавливает N, Z и C (`SENZC`).

---

## 4.3. Чего в БК-0010 нет

Список выше **исчерпывающий**. По сравнению с полной системой команд PDP-11 отсутствуют:

* **EIS** — расширенная арифметика: `MUL`, `DIV`, `ASH`, `ASHC`
  (но см. оговорку о К1801ВМ1Г ниже);
* **FIS/FPP** — плавающая арифметика: `FADD`, `FSUB`, `FMUL`, `FDIV` и весь сопроцессор;
* команды работы с раздельными адресными пространствами: `MFPI`, `MTPI`, `MFPD`, `MTPD`;
* `SPL` (установка приоритета).

Практические следствия:

* умножение, деление и вещественная арифметика реализуются **программно**; их эмуляторы
  перехватывают **вектор 10** (резервный код) — так устроен Фокал и ПЗУ КНГМД
  (см. [06-прерывания.md](06-прерывания.md), §6.5);
* разряд 07 регистра 177716 по заводской документации — константа «1», указывающая
  именно на **отсутствие в системе команд расширенной арифметики**
  (см. [05-системные-регистры.md](05-системные-регистры.md), §5.6).

### Практика: программная эмуляция EIS

Эмулятор расширенной арифметики — резидентный блок, перехватывающий **вектор 10**.
После его инициализации в программе можно писать мнемонику `MUL`, `DIV`, `ASH`, `ASHC`
как если бы они были аппаратными: встретив такой код, процессор уходит в прерывание
по резервному коду, а блок исполняет команду.

Типовая реализация — модуль `ARIFM.OBJ` из комплекта кросс-ассемблера **БКТурбо8**
(`GID-src/BKemu/Docs/BKTurbo8.html`):

```asm
        CALL    ARIFM           ; инициализация блока в начале программы
        ...
        MOV     #75.,R1
        CLR     R0
        DIV     #10.,R0         ; R0 — частное, R1 — остаток
        MOV     #7,R1
        MUL     #10.,R1         ; R1 = 70
        ASH     #5,R1           ; сдвиг R1 на 5 разрядов влево
```

Линковка: `BKTurbo8 LI NAME prog.obj arifm.obj` — полученная программа работает автономно.

Семантика эмулируемых команд (как в 1801ВМ2 / PDP-11 EIS):

| Команда | Действие |
| --- | --- |
| `MUL src,Rn` | `Rn:Rn|1 = Rn × src`. При **нечётном** номере регистра сохраняется только младшая часть результата |
| `DIV src,Rn` | Деление 32-разрядного `Rn:Rn|1` на `src`. **Номер регистра должен быть чётным.** Частное в `Rn`, остаток в `Rn|1` |
| `ASH src,Rn` | Арифметический сдвиг `Rn` на −32…+32 позиции. Направление задаёт разряд 5 аргумента: 1 — вправо, 0 — влево |
| `ASHC src,Rn` | То же для двойного слова `Rn:Rn|1` (чётный регистр — старшая часть, нечётный — младшая) |

На БК-0011М с КНГМД, прошивка ПЗУ 326, эмулятор уже присутствует в ПЗУ, и вместо
подключения модуля достаточно направить на него вектор:

```asm
        MOV     #164040,@#10
        MOV     #340,@#12
```

Такой же приём — эмуляция через вектор 10 — использован в интерпретаторе Фокала
и в ПЗУ КНГМД [З].

### Уточнения по [ВМ1]

Техническое описание процессора даёт три важных поправки к «наивной» картине
«ВМ1 = базовый PDP-11 без EIS»:

1. **`SOB` и `XOR` формально принадлежат набору EIS** и реализованы в ВМ1 аппаратно.
   То есть отсутствует не весь EIS, а только `MUL`, `DIV`, `ASH`, `ASHC`.
2. **`MUL` реализована в К1801ВМ1Г.** Ради неё и переписывалась микропрограмма
   (ВМ1Г выпускалась по другим фотошаблонам слоя диффузии). На машине с ВМ1Г
   `MUL` выполняется аппаратно; на ВМ1А/Б/В — нет.
   Определить вариант чипа можно по маркировке, но она менялась трижды —
   см. [03-процессор-К1801ВМ1.md](03-процессор-К1801ВМ1.md), §3.2.
   **Программы для БК на это рассчитывать не должны**: в машины ставились разные чипы.
3. **Коды `17xxxx` — зарезервированные, недопустимые**; вызывают исключение
   по вектору 10 [ВМ1 § 9.63]. При этом взводится флаг `UOP` в регистре ошибок
   процессора и сбрасывается микропрограммный автомат.

### ⚠ Аппаратная ошибка ВМ1: флаг C после `MOVB` и `MFPS`

**После команд `MOVB xx,Rd` и `MFPS Rd` (только с регистровым приёмником) команды
условного перехода видят флаг `C` сброшенным**, хотя в самом PSW он корректен.

```asm
        SEC                     ; установить флаг C
        MOVB    R1,R0           ; флаг C измениться не должен
        BCC     1$              ; переход БУДЕТ выполнен — неверно!
```

Обход — любая промежуточная команда, которая перезапишет промежуточный регистр флагов
из реального PSW:

```asm
        SEC
        MOVB    R1,R0
        NOP                     ; ← достаточно любой команды
        BCC     1$              ; переход не будет выполнен — верно
```

Причина и подробности — [03-процессор-К1801ВМ1.md](03-процессор-К1801ВМ1.md), §3.7.
Этой ловушки нет ни в [РСП], ни у [З], ни в [ПБК].

### Коды, вызывающие «ЗВ» вместо «НК» [З]

Не всякий нереализованный код даёт прерывание по вектору 10 («НК» — неправильная команда).
Часть кодов вызывает прерывание по вектору 4 («ЗВ» — зависание), потому что процессор
пытается обратиться к системному регистру, которого в БК нет:

* код `12` → **«ЗВ»** (обращение к отсутствующему регистру);
* код `30` → **«НК»** (действительно несуществующая команда).

Знание этой особенности помогает разбираться в загадочных сообщениях при отладке.

---

## 4.4. Формат и запись команды [З]

```
[метка:]  оператор  операнд1,операнд2   ; комментарий
```

* Метка отделяется двоеточием; обычные метки начинаются с буквы, локальные — с цифры.
* Байтовая форма оператора обозначается суффиксом `B`: `MOVB`, `CLRB`, `CMPB`.
* Числа по умолчанию **восьмеричные**.
* Для команд, занимающих больше одного слова, дополнительные слова (индексы, константы,
  абсолютные адреса) размещаются **сразу за словом команды**, в порядке следования
  операндов.

Длина команды: 1 слово при способах адресации 0…5 для обоих операндов; +1 слово за каждый
операнд со способом 6 или 7 (включая `#X`, `@#X`, `MET`, `@MET`). Максимум — 3 слова.

---

## 4.5. Псевдооператоры ассемблера

Псевдооператоры — директивы транслятора, а не команды процессора. Точный набор зависит
от ассемблер-системы; ниже — набор, общий для MACRO-11-совместимых ассемблеров БК
(МИКРО.10К, серия TURBO, кросс-ассемблер БКТурбо8).

| Псевдооператор | Действие |
| --- | --- |
| `.LA выражение` (синоним `.LINK`) | Адрес начала трансляции неперемещаемой программы. Должен быть первой командой (допустимо после начальных присвоений) |
| `.BLKB выражение` | Резервирование указанного числа **байт** |
| `.BLKW выражение` | Резервирование указанного числа **слов** |
| `.WORD N,N,…` | Запись слов. Смещение к метке записывается как `(MET-.)` или `@MET` |
| `.BYTE N,N,…` | Запись байтов. Значение должно умещаться в байт: `-1` = 177777 записать нельзя, `-177776` = 2 — можно |
| `.EVEN` | Выравнивание счётчика на чётный адрес (при нечётном добавляется нуль) |
| `.ASCII /строка/` | Запись строки символов. Ограничитель — любой знак пунктуации, кроме угловых скобок; символ в числовой форме задаётся в угловых скобках: `.ASCII /ТЕКСТ/<40>"Да/Нет:"<12>` |
| `.ASCIZ /строка/` | То же с добавлением нулевого байта в конце |
| `.RAD50 /строка/` | Запись строки в коде RADIX-50 |
| `.PRINT #TXT` | Раскрывается в `MOV #TXT,R1` / `CLR R2` / `EMT 20`. **Нельзя в перемещаемой программе** |
| `.ADDR Rn,MET` | Получение абсолютного адреса метки в `Rn`; раскрывается в `MOV PC,Rn` / `ADD (PC)+,Rn` / `.WORD @MET+2`. Пригоден в перемещаемых программах |

В МИКРО.10К (по [З]) используются варианты `.B:` (запись байтов), `.A:` (запись строки),
`.@` (вычисление адреса в перемещаемом формате), `.#` (запись слова), `.E` (конец данных).
Именно эта нотация встречается в листингах Зальцмана в настоящей документации.

Приёмы перемещаемого программирования опираются на `.@`/`.ADDR`:

```asm
        MOV     PC,R0               ; вычислить абсолютный адрес метки
        ADD     (PC)+,R0            ; в перемещаемом
        .@MET+2                     ; формате
```

## 4.6. Время исполнения

Полные таблицы — в [02-архитектура.md](02-архитектура.md), §2.5. Кратко:

* основное время команды (адресация `Rn` или безоперандная) — **4,0 мкс**;
* время **не зависит** от того, работает команда с байтом или со словом;
* «медленный» операнд выгоднее ставить **первым**: `CMP @MET,Rn` = 13,4 мкс,
  `CMP Rn,@MET` = 16,0 мкс;
* обращение к подпрограмме быстрее командного прерывания: `JSR`+`RTS` ≤ 26,8 мкс против
  `EMT`+`RTI` = 36,2 мкс плюс работа EMT-диспетчера.
