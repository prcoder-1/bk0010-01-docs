# 15. БК-0011М: чем отличается от БК-0010-01 и БК-0011

> **Источники главы**
> * **[РСП-11]** — **«Персональная микро-ЭВМ Электроника МС0513. Драйвер-мониторная
>   система. Руководство системного программиста» 00008-01.32.01**, 1989 →
>   `GID-src/BKemu/Docs/00008-01.32.01.html`.
> * **[РСП-11М]** — **«Персональная ЭВМ Электроника МС0513 (БК-0011М). Базовая
>   операционная система. Руководство системного программиста» 00015-01.32.01**, 1990 →
>   `GID-src/BKemu/Docs/00015-01.32.01.html`. **Определяющий источник главы.**
> * Каталоги документов проекта `gid.pdp-11.net` (`docstable.html`, `doclist1.html`) —
>   использованы для сверки состава заводской документации.

> **Место машины в ряду.** БК-0011М — **переработка БК-0011**, а не его исправленная
> редакция: системное ПО разное настолько, что программы не переносятся. Развёрнутое
> сопоставление двух машин — в [14-БК-0011.md](14-БК-0011.md), §14.9; здесь оно
> дано конспективно в §15.9.

> **Назначение главы.** Ядро справочника (главы 1–13) описывает БК-0010 и БК-0010-01.
> Эта глава — не самостоятельное описание БК-0011М, а **перечень отличий**: что в старшей машине устроено иначе, какие
> устройства, регистры и системные вызовы в БК-0010-01 отсутствуют вовсе. Всё, что
> у двух машин общее (система команд процессора, формат экранной строки, обмен
> с магнитофоном, цоколёвка МПИ), описано в предыдущих главах и здесь не повторяется.

## 15.1. Родословная и названия

**«Электроника МС0513»** — заводское обозначение; **БК-0011** и **БК-0011М** —
торговые названия двух ревизий. Процессор тот же — **К1801ВМ1**, поэтому
**система команд полностью совпадает** с БК-0010-01
(см. [04-система-команд.md](04-система-команд.md)): ни EIS, ни FIS, ни MMU
не добавилось.

Различия — в **обвязке**: объём памяти, страничная организация, цвет, системное ПО.

| | БК-0010-01 | БК-0011М |
| --- | --- | --- |
| Процессор | К1801ВМ1 | К1801ВМ1 (тот же) |
| ОЗУ | 32 Кбайт | **128 Кбайт**, страничная организация, К565РУ5 |
| **Системное** ПЗУ | 8 Кбайт (МДС) | **16 Кбайт** (БОС) |
| Всего масочного ПЗУ на плате | 32 Кбайт: МДС 8 + Бейсик 24 в трёх съёмных БИС. Блок МСТД добавляет ещё 16 Кбайт (Фокал 8 + тесты 8), перекрывая окна Бейсика | 16 Кбайт системного + страницы ПЗУ 0…3 |
| Системное ПО | МДС — драйвер-мониторная система | **БОС — базовая операционная система** |
| Адрес начального пуска | `100000` | **`140000`** |
| Буферов экрана | один | **два, с переключением** |
| Цвет | 4 фиксированных цвета | **16 палитр** |
| Контроллер клавиатуры | К1801ВП1-014 | тот же |
| Контроллер ОЗУ и дисплея | К1801ВП1-037 | тот же (но `nAD15` подан в «1», см. §13.1а) |

## 15.2. Адресное пространство и страницы

Виртуальное адресное пространство по-прежнему **64 Кбайт** — его задаёт процессор.
Физической памяти 128 Кбайт, и она разбита на **восемь страниц ОЗУ по 16 Кбайт**.

```
        0 ┌──────────────────────────────┐
          │  Страница ОЗУ 0 — постоянно  │
          │  отображена, не переключается│
    40000 ├──────────────────────────────┤
          │  Окно 1: страницы ОЗУ 0…7    │
   100000 ├──────────────────────────────┤
          │  Окно 2: страницы ОЗУ 0…7    │
          │  либо страницы ПЗУ 0…3       │
   140000 ├──────────────────────────────┤
          │  СИСТЕМНОЕ ПЗУ (БОС)         │
   160000 ├──────────────────────────────┤
          │  ПЗУ КНГМД / блока МСТД /    │
          │  сменного блока ПЗУ          │
   177600 ├──────────────────────────────┤
          │  Регистры внешних устройств  │
   177777 └──────────────────────────────┘
```

Существенное следствие, которое [РСП-11М] выделяет особо:

> Каждая страница ОЗУ может быть **одновременно подключена к двум окнам**
> виртуального адресного пространства, а статическая страница (адреса `0`…`40000`) —
> **даже к трём**.

То есть одни и те же физические ячейки видны по нескольким адресам сразу.

### Назначение страниц ОЗУ

| Страница | Назначение |
| :-: | --- |
| **0** | Постоянно отображена на `0`…`40000`; переключению не подлежит |
| 1…4 | Свободны для программ и данных |
| **5** | **Буфер экрана 0** |
| **6** | **Буфер экрана 1** |
| **7** | **Служебная страница БОС**, пользователю недоступна. Часть, не занятая БОС, доступна через запросы драйвера ОЗУ/ПЗУ |

**Два буфера экрана** — принципиально новая возможность: программа формирует
изображение в одном буфере, пока на экран выводится другой («скрытый» вывод).
В БК-0010-01 экранное ОЗУ одно и такой приём невозможен.

### Страницы ПЗУ

| Страница ПЗУ | Содержимое |
| :-: | --- |
| 0 и `100000`…`117777` страницы 1 | ПЗУ языка **Бейсик** |
| `120000`…`137777` страницы 1 | Программы **БОС** |
| `100000`…`117777` страницы 2 | ПЗУ **драйвера локальной сети** |
| **3** | Подключение **внешних блоков ПЗУ** к магистрали |

> Из пользовательской программы страницы **нельзя переключать напрямую** — только
> запросами базовой операционной системы.

## 15.3. Системный регистр `177716` — три регистра по одному адресу

В БК-0010-01 по адресу `177716` находятся два регистра — на чтение и на запись
(см. [05-системные-регистры.md](05-системные-регистры.md), §5.6). В БК-0011М их **три**:
один по чтению и **два по записи**, различаемые состоянием разряда 11.

### Регистр чтения

| Разряд | Назначение |
| :-: | --- |
| 15…08 | **Адрес начального пуска — `140000`** (в БК-0010-01 здесь `100000`) |
| 07 | Всегда «1» — признак отсутствия арифметического расширителя |
| 06 | «0», если нажата любая клавиша; «1» по отпускании |
| 05 | Данные с магнитофона |
| 02 | Признак записи в системный регистр; сбрасывается по окончании чтения регистра |

Разряды 02, 05, 06, 07 работают **так же, как в БК-0010-01**. Меняется только
адрес начального пуска.

### Регистр управления памятью (запись при разряде 11 = «1»)

| Разряд | Назначение |
| :-: | --- |
| 14…12 | Номер страницы ОЗУ на адреса **`40000`…`100000`** |
| **11** | **Должен содержать «1»** — иначе запись уйдёт в регистр управления магнитофоном |
| 10…08 | Номер страницы ОЗУ на адреса **`100000`…`140000`** |
| 04 | Подключить **страницу ПЗУ 3** на `100000`…`140000`, отключив страницу ОЗУ |
| 03 | То же для **страницы ПЗУ 2** |
| 01 | То же для **страницы ПЗУ 1** |
| 00 | То же для **страницы ПЗУ 0** |

**Кодировка номера страницы неочевидна** — это не двоичный номер:

| Разряды 14–12 (или 10–8) | Страница |
| :-: | --- |
| 110 | 0 — постоянно отображена на `0`…`40000` |
| 000 | 1 |
| 010 | 2 |
| 011 | 3 |
| 100 | 4 |
| 001 | **5 — буфер экрана 0** |
| 111 | **6 — буфер экрана 1** |
| 101 | **7 — системная страница** |

> ⚠ **Запись в регистр возможна только командами, работающими со словом целиком.**
> Байтовая запись не проходит. Это ровно то же ограничение, что у регистра смещения
> в БК-0010-01 (§5.3).

### Регистр управления магнитофоном (запись при разряде 11 = «0»)

| Разряд | Назначение |
| :-: | --- |
| 12 | Блокировка клавиши «СТОП» |
| 07 | Управление двигателем магнитофона |
| 06, 05 | Данные записи на магнитофон |
| **11** | **Должен быть «0»** |

> Разряд 11 работает как переключатель адресата записи. Программа, слепо
> переносящая из БК-0010-01 код вида `MOV #200,@#177716`, попадёт **не в тот
> регистр** — это первое, обо что спотыкается перенос.

**Копии обоих регистров записи** БОС хранит в ячейках **`114`** (управление памятью)
и **`116`** (управление магнитофоном) — прочитать сами регистры нельзя.

## 15.4. Регистр управления палитрами `177662`

Регистра с таким назначением в БК-0010-01 **нет вообще**. Он расположен по адресу
**`177662`** и доступен **только по записи**, тогда как по чтению тот же адрес —
регистр данных клавиатуры.

| Разряд | Назначение |
| :-: | --- |
| **15** | Отображаемый буфер экрана: 0 — буфер 0 (страница 5), 1 — буфер 1 (страница 6) |
| **14** | Системный таймер: **0 — включён, 1 — выключен** |
| **11…08** | **Код палитры** — кодировка цветов в режиме 256 точек в строке |

Четыре разряда дают **16 палитр**. Формат экранной строки при этом тот же, что
в БК-0010-01: два бита на точку в цветном режиме
(см. [09-экран-и-клавиатура.md](09-экран-и-клавиатура.md), §9.2) — меняется
только сопоставление кода цвету.

Текущее состояние регистра читается **запросами БОС**, а не с самого регистра.

> Обратите внимание: **включением системного таймера тоже управляет этот регистр**.
> В БК-0010-01 встроенный таймер процессора живёт своей жизнью и внешнего
> выключателя не имеет (§5.4).

## 15.5. Регистр смещения `177664` — без изменений

Работает **точно так же**, как в БК-0010-01: разряды 7…0 — смещение в строках,
разряд 9 — полный экран («1») или четверть («0»).

Совпадает и загадочная константа: **исходному состоянию соответствует смещение `330`**
(см. §5.3, где это число объяснено устройством БИС 1801ВП1-037). Экран так же
«замкнут в кольцо», 256 строк по 64 байта, строки начинаются с адресов, кратных `100`,
байты отображаются начиная с младшего бита.

Единственное уточнение [РСП-11М]: режим «малого» экрана (разряд 9 = 0) **базовой
операционной системой не используется**.

## 15.6. Векторы прерываний и ячейки БОС

Общая схема та же, но назначение ряда векторов и ячеек изменилось.

| Адрес | БК-0011М [РСП-11М] | Как в БК-0010-01 |
| --- | --- | --- |
| `4`, `6` | Ошибка обращения, `HALT`, клавиша «СТОП» | Так же |
| `10`, `12` | Несуществующая команда — **БОС не использует** | Используется Фокалом и эмуляторами EIS |
| `14`, `16` | `BPT` и T-разряд — **используется клавишей «ШАГ»** | Основа отладчиков |
| `30`, `32` | Команда `EMT` | Так же |
| **`40`** | **Адрес пуска по команде монитора `G`** (при работе с магнитофоном) | Нет такого назначения |
| **`52`** (мл. байт) | **Код возврата системных вызовов** | Нет |
| `60`, `62` | Клавиатура — алфавитно-цифровые и управляющие коды | Так же |
| **`100`, `102`** | **Вектор таймера** | Прерывание пользователя (вектор 100), см. §12.4 |
| **`110`, `112`** | **Программный вектор клавиатуры** — адрес пользовательской программы завершения | Нет |
| **`114`** | Копия регистра управления памятью | Нет |
| **`116`** | Копия регистра управления магнитофоном | Нет |
| `150`–`156`, `164`, `250`, `252` | Внутренние нужды БОС | Иное назначение |
| **`166`** | **Псевдовектор `EMT`** для перехвата необработанных БОС команд `EMT` | Нет |
| **`270`, `272`** | **Вектор прерывания параллельного порта** | Нет |
| `274`, `276` | Клавиатура — регистр АР2 и функциональные клавиши | Так же |

## 15.7. БОС вместо МДС: два способа вызова

Самое глубокое отличие для программиста. В БК-0010-01 системные функции вызываются
**только через `EMT`** (см. [08-монитор.md](08-монитор.md)). В БК-0011М их **два**.

### Таблица системных вызовов — основной способ

В ПЗУ с адреса **`140010`** расположен массив адресов программ-обработчиков.
Вызов делается так:

```
    JSR PC, @#140010        ; вызов функции, чей адрес лежит в 140010
```

Параметры передаются в **R0**, иногда и в **R1**. При возврате содержимое регистров
процессора **сохраняется**, кроме особо оговорённых случаев.

### Диспетчер `EMT` — альтернатива

Тот же набор функций доступен и командами `EMT`, способ передачи параметров совпадает.

| | Достоинства `EMT` | Недостатки `EMT` |
| --- | --- | --- |
| | Позиционная независимость | Программа не может использовать `EMT` с младшими номерами для своих нужд |
| | Меньший объём кода | Выполняются **медленнее** |
| | | При переходе в монитор (клавиша «СТОП», неверный `.BJMP`/`.BJSR`) выводится адрес **диспетчера**, а не программы |

Если номер `EMT` превышает допустимый, диспетчер смотрит ячейку **`166`**: если она
не нулевая, управление уходит по этому адресу, `PS` устанавливается по содержимому
ячейки `32`, а возврат из пользовательского обработчика делается **`RTI`** прямо
в вызвавшую программу.

### Состав БОС

Программа начального пуска, клавиатурный монитор, таблица системных вызовов,
обработчик `EMT`, драйверы: **ОЗУ/ПЗУ**, клавиатуры, экрана, кассетного магнитофона,
**принтера**, **накопителя на ГМД**.

> **Драйвера ОЗУ/ПЗУ и принтера в МДС БК-0010-01 нет** — первого потому, что нет
> страниц, второго потому, что печать в БК-0010-01 организуется прикладными
> средствами (§12.7).

## 15.8. Системные запросы, которых нет в БК-0010-01

Полные описания — в [РСП-11М], разд. 3. Здесь — перечень с назначением, чтобы было
видно, чего именно лишена младшая машина.

### Работа со страницами — драйвер ОЗУ/ПЗУ

| Запрос | Что делает |
| --- | --- |
| `.BMEM` | Чтение состояния памяти |
| `.BTEST` | Чтение статуса страницы ОЗУ/ПЗУ |
| `.BPAGE` | **Подключение страницы** ОЗУ/ПЗУ |
| `.BWORK` | Назначение **рабочей страницы** |
| `.BGETW` / `.BGETB` | Чтение слова / байта из рабочей страницы |
| `.BPUTW` / `.BPUTB` | Запись слова / байта в рабочую страницу |
| `.BMOVE` / `.BMOVB` | Пересылка массива слов / байтов между страницами |
| `.BJSR` | **Вызов подпрограммы** из рабочей страницы |
| `.BJMP` | Переход в рабочую страницу **без возврата** |

Это и есть та подсистема, ради которой существует БОС: процессор адресует 64 Кбайт,
а памяти 128 Кбайт, и весь доступ к «невидимой» её части идёт через эти вызовы.

### Общесистемные

| Запрос | Что делает |
| --- | --- |
| `.BINIT` | Инициализация БОС |
| `.BEXIT` | Выход в клавиатурный монитор |
| `.BTRPS` | Перехват вектора 4 |
| **`.BTIM`** | **Управление таймером** |
| `.BEMT` | Управление обработкой `EMT` |

### Клавиатура

`.BKINI`, `.BKSET`, `.BKGET`, `.BTTIN`, `.BINKEY`, `.BGLIN`, `.BEDIT`, **`.BKRES`**
(сброс буфера клавиатуры), `.BSFUN` (установка функциональных клавиш).

> Часть из них имеет прямые соответствия в БК-0010-01: `.BTTIN` ≈ `EMT 6`,
> `.BGLIN` ≈ `EMT 10`, `.BSFUN` ≈ `EMT 12`. Но **`.BINKEY` (ввод без ожидания),
> `.BEDIT` (редактирование существующей строки) и `.BKRES`** аналогов не имеют.

## 15.9. Различия между БК-0011 и БК-0011М

Полное сопоставление — в [14-БК-0011.md](14-БК-0011.md), §14.9. Коротко: совпадают
процессор, объём памяти, число страниц, формат экрана и **палитра** (4 цвета из 16
у обеих машин); расходится почти всё остальное — системное ПО (ДМС против БОС),
число регистров записи по `177716`, место управления магнитофоном и буфером экрана,
нумерация страниц ПЗУ (8…11 против 0…3), способ вызова системных функций и сами
их имена: **из 37 запросов
БК-0011 и примерно 75 запросов БК-0011М общих имён всего десять**.

Кроме того, у БК-0011 системное ПО называется **драйвер-мониторной системой**
(как в БК-0010), а в [РСП-11] описаны **регистры КНГМД и регистр КПУ**
(см. §14.10), которых в руководстве на БК-0011М нет.

**Бейсик — тоже разный, и не в пользу «новее значит дальше».** Микросхемы 327, 328,
329 содержат **редакцию БК-0010-01 с единственным добавленным оператором `MERGE`**,
числовыми сообщениями об ошибках и подписью **ОКБ «Экситон»**, тогда как на БК-0011
стоит отдельная вильнюсская ветвь 1987 года с `FILES`, `SCREEN`, `WIDTH`, `TRAP`,
`SYSTEM` и текстовыми сообщениями об ошибках. Проверено по образам ПЗУ [✓ ПЗУ];
разбор — в [16-бейсик.md](16-бейсик.md), §16.14.

## 15.10. Особенности выполнения команд

[РСП-11М] отмечает то же, что и документация на БК-0010-01, но с важными уточнениями
про клавишу «СТОП»:

* **`HALT` (код `000000`)** вызывает переход по вектору 4 и **устанавливает признак
  записи в системный регистр** — по нему БОС отличает останов от обычного прерывания
  по вектору 4. Пользовательская программа может перехватить **только обычное
  прерывание**, не останов.
* При останове происходит переход в монитор БОС с выводом содержимого счётчика команд.
* **Корректная обработка «СТОП» возможна только при правильном указателе стека.**
* Из-за особенностей аппаратуры **может выдаваться неправильное значение счётчика
  команд**.
* При нажатии «СТОП» во время выполнения запроса БОС **продолжение программы
  по команде `P` невозможно**.

> Вывод [РСП-11М] дословно: клавишей «СТОП» «следует пользоваться очень осторожно».
> Механизм разряда 02 системного регистра — тот же, что разобран для БК-0010-01
> в §5.6 и в реестре расхождений (В4).

## 15.11. Что переносится, а что нет

| Переносится без изменений | Требует переработки |
| --- | --- |
| Система команд процессора (§4) | Любое обращение к `177716` — теперь три регистра |
| Формат экранной строки, два бита на точку (§9.2) | Цвет: 16 палитр вместо фиксированных четырёх |
| Регистр смещения `177664` и рулон (§5.3, §9.3) | Запись в `177662` — теперь палитра, а не «ничего» |
| Регистры клавиатуры `177660`/`177662` по чтению (§5.1, §5.2) | Все системные вызовы: `EMT` МДС ≠ вызовы БОС |
| Параллельный порт `177714` (§5.5) | Адрес начального пуска `140000` вместо `100000` |
| Цоколёвка МПИ и УП (§12.1) | Прямая работа с памятью выше 32 Кбайт |
| Обмен с магнитофоном по существу (§10) | Управление двигателем — в другом регистре |

**Общий вывод:** программа в машинных кодах, не использующая ни системных вызовов,
ни системного регистра, ни цвета, переносится с БК-0010-01 на БК-0011М почти
дословно. Всё остальное — переписывается.
