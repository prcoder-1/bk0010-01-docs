# 18. Контроллер Gryphon-MPI

> **Источники главы**
> * **[GMPI]** — сайт проекта https://night-gryphon.ru/Gryphon-MPI/ (страницы
>   `index`, `docs`, `prepare`, `flash`, `case`, `assembly`, `api`, `firmware`;
>   кодировка CP1251) и демонстрационный веб-интерфейс
>   `https://night-gryphon.ru/Gryphon-MPI/interface/`. Автор — Night Gryphon.
>   Страница «Сетевое API» помечена самим автором как черновик.
> * **[GMPI-ПО]** — **разбор прошивок**, выполненный для этой главы:
>   `Gryphon-MPI/Gryphon_MPI_v2_2022_09_01-01.dfu` (основная плата, STM32,
>   формат DfuSe, дата сборки в образе — 1 сентября 2022) и
>   `Gryphon-MPI/firmware/2024.03.23/Gryphon_MPI_NET_2024_03_23-01.fw` +
>   `…-02.data.fw` (сетевая плата, ESP8266: образ кода и образ файловой системы
>   littlefs с веб-интерфейсом). Всё, что помечено ниже **[✓ ПО]**, получено
>   прямым разбором этих файлов, а не из документации.
> * **[GMPI-Сх]** — `Gryphon-MPI/STM32_Board_Connections.pdf`, схема соединения
>   модулей (KiCad, ревизия 1.0, 18.12.2022).
> * **[Каз]**, **[АльтПро]**, **[Рад]** — привлекаются для сверки: требования к
>   доработке БК (§14.2в, §17.3), ПЗУ контроллеров АльтПро (§17.1) и штатный
>   драйвер магнитофона (§12.6б).

**Gryphon-MPI** — современный контроллер для разъёма МПИ, который не столько
добавляет БК периферию, сколько **подменяет её**: он следит за работой машины,
отдаёт ей вместо отсутствующих микросхем ПЗУ свои образы, эмулирует контроллеры
АльтПро, блок МСТД и блок ИРПС, а управляется с экрана самой БК или по WiFi.

Дословно [GMPI]:

> Технически контроллер является системой-супервизором, которая отслеживает и
> контролирует работу БК, загружает и подготавливает БК к работе, эмулирует
> переферийные устройства.

Заявленный перечень эмулируемого [GMPI]: **ПЗУ 1801РЕ2, блок МСТД, контроллеры
АльтПро СМК-512 и А-16 (доп. ОЗУ, дисковод, жёсткий диск), блок ИРПС (сеть,
модем)**. Работает с БК-0010(-01), БК-0011М и новодельными машинами.

---

## 18.1. Состав и конструктив

Выпускается в двух вариантах с одинаковым основным функционалом [GMPI]:

| Вариант | Особенности |
| --- | --- |
| **Одноплатный** | Законченное устройство с корпусом (акриловая крышка со светящимся логотипом) |
| **Двухплатный (модульный)** | Адаптер и контроллер стыкуются буквой «Г», без корпуса; **можно подключить дисплей и энкодер** для ремонтных меню; легче модифицировать |

Электрически контроллер состоит из трёх частей:

* **основная плата** — микроконтроллер **STM32**; по прошивке видно
  **[✓ ПО]**: начальный указатель стека `0x20020000` (128 Кбайт ОЗУ),
  задействован контроллер внешней памяти **FSMC** (`0xA0000000`), блок
  **USB OTG** (`0x50000000`) и порты по базе `0x40020000` — то есть кристалл
  семейства **STM32F4** с FSMC (F405/F407-класса). Объём прошивки — около
  269 Кбайт флеш-памяти;
* **сетевая плата** — модуль на **ESP8266** (магическое число `0xE9` в начале
  образа, точка входа `0x4010F480`, сегменты по адресам IRAM `0x40100000` и
  DRAM `0x3FFE8000`) **[✓ ПО]**. На ней же расположен рабочий слот SD-карты;
* **адаптер МПИ** с программируемой логикой, стыкующийся с разъёмом БК.

Соединения модулей [GMPI-Сх]:

| Разъём | Сигналы |
| --- | --- |
| **J2** | к плате Gryphon-MPI (адаптеру МПИ) |
| **J3** (44 контакта) | питание `+3,3 В`/`GND`; SPI карты памяти `SD_SDI`, `SD_SDO`, `SD_SCL`, `SD_CS`; UART к сетевой плате `NET_TX`, `NET_RX`; энкодер `ENC_DT`, `ENC_CLK`, `ENC_SW`; дисплей `D_SDA`, `D_SCK`, `D_CS`, `D_A0`, `D_RESET`; **`AUDIO_BYTE` (конт. 42) и `AUDIO_WR` (конт. 44)** — стробы для звуковой платы Gryphon-Sound |
| **J4 «LED»** | второй комплект линий SPI карты памяти |

Дисплейный модуль несёт **собственный полноразмерный слот SD**, разделяющий шину
SPI с основной картой. **Слот SD на плате STM32 использовать нельзя** — рабочим
является слот на сетевой плате [GMPI].

---

## 18.2. Подключение к БК и необходимые доработки

Контроллер подключается в разъём МПИ на задней стенке БК; SD-карта ставится
вертикально в слот сверху на сетевой плате. При включении на экране БК должна
появиться заставка контроллера и главное меню [GMPI].

Для полноценной работы БК желательно доработать — ту же доработку требуют и
контроллеры АльтПро (§17.3), и здесь она описана подробнее:

### Вход отключения ПЗУ монитора

Подача сигнала на контакт **Б1** (БК-0010) или **Б6** (БК-0011М) разъёма МПИ
должна отключать микросхему ПЗУ монитора [GMPI]. Это ровно те же контакты, что
занимает СМК сигналами `MON10` и `M11` (§17.3), — то есть доработка одна на все
контроллеры.

| Машина | Микросхема ПЗУ монитора | Контакт МПИ |
| --- | :-: | :-: |
| БК-0010* | 1801РЕ2-**017** | **Б1** |
| БК-0011 | 1801РЕ2-**201** | Б6 |
| БК-0011М | 1801РЕ2-**324** | **Б6** |

Суть доработки для БК-0011М [GMPI]: на верхней стороне платы разрезать проводник
к **выводу 23** микросхемы 1801РЕ2-324, в разрез установить резистор **1 кОм**,
затем соединить вывод 23 с контактом **Б6** разъёма МПИ. Для БК-0010 —
аналогично, с контактом **Б1**: отсоединить контакт Б1 от схемы, отсоединить
переходное отверстие рядом с 1801РЕ2-017 от шины питания, впаять между ними
резистор 1 кОм и соединить нижний вывод резистора с Б1.

Это **та же самая доработка**, что описана в [АльтПро] для БК-0011М (§17.3), где
она формулируется через отвод вывода `DS17.23` на контакт B6. Доработка не мешает
работе БК без контроллера; в поздних ревизиях БК она сделана заводом.

Что будет, если её не сделать [GMPI]:

* у контроллеров СМК-512 и А-16 **возможны сбои у программ, работающих с
  дополнительной памятью** (причина разобрана в §17.3: доп. ОЗУ подключается
  параллельно ПЗУ, и при конфликте читается «1»);
* на БК-0010 **недоступна эмуляция магнитофона** (почему именно — см. §18.5).

Как альтернативу [GMPI] предлагает вовсе вынуть ПЗУ монитора из панельки:
контроллер подставит своё, но без контроллера машина загружаться не будет.

### Исправление сигнала СИП (RPLY)

На части БК-0010(-01) встречается дефект, при котором «оооочень медленно
очищается экран и медленно рисуется меню контроллера» [GMPI]. Признак виден в
стартовой диагностике как отказ «Ответ ОЗУ». Фотографии доработки —
на сайте проекта.

---

## 18.3. Стартовая диагностика БК

При включении контроллер прогоняет тест машины, результат виден на экране БК
(«Настр» → «Инфо»), в веб-интерфейсе («О Компьютере») и в ответе API
`/api/bkinfo`.

**[✓ ПО]** Слово состояния — третий элемент массива `bkinfo`; разряды
расшифровываются так (порядок восстановлен из веб-интерфейса, разряд 0 —
младший):

| Разряд | Имя | Что проверяется |
| :-: | :-: | --- |
| 00 | `mpi` | магистраль МПИ |
| 01 | `ram` | ОЗУ |
| 02 | `ram_rply` | ответ (СИП) от ОЗУ |
| 03 | `rom_rply` | ответ (СИП) от ПЗУ |
| 04 | `blk_mon` | наличие входа блокировки ПЗУ монитора |
| 05 | `blk_bas` | наличие входа блокировки ПЗУ Бейсика |
| 06 | `hw_palette` | аппаратная палитра |
| 07, 08 | `mon1`, `mon2` | микросхемы ПЗУ монитора |
| 09, 10, 11 | `bas1`, `bas2`, `bas3` | микросхемы ПЗУ Бейсика |

Установленный разряд = «Норма». Пункты «Блок. Мон» и «Блок. Бейсик» — это
разряды 04 и 05; «Ответ ОЗУ» — разряд 02. По [GMPI] проверка «Блок. Бейсик»
на момент написания документации ещё не была доведена.

Первые два элемента `bkinfo` — семейство и модель машины **[✓ ПО]**:

| Код | Семейство | Модель |
| :-: | --- | --- |
| 0 | не определена | не определена |
| 1 | БК-0010(01) | БК-0010(01) |
| 2 | БК-0011(М) | БК-0011М |
| 3 | БК-0010(01) | БК-0010-01 |
| 4 | БК-0011(М) | БК-0011 |

**[✓ ПО]** В прошивке видны и сами шаги диагностики: форматные строки
`177660: %04X`, `177662: %04X OK`, `177716: %04X` — контроллер читает регистры
клавиатуры и системный регистр (§6), а на экран выводит `Detect: 0010`,
`Detect: 0011x` либо `Detect: Unknown`. Стадии инициализации отображаются
строками `INITIALIZING`, `MPI Ok` / `MPI Fail`, `RAM OK` / `RAM Fail`,
`Network - Init` / `Network - OK` / `Network - X`, `SD Card - Init` /
`SD Card - OK` / `SD Card - MNT Err`, `SD SPI err %d`.

---

## 18.4. Образы ПЗУ внутри прошивки

Контроллер «автоматически эмулирует отсутствующие микросхемы ПЗУ как для БК-0010,
так и БК-0011М» [GMPI]. **[✓ ПО]** Образы лежат в прошивке основной платы
открытым текстом, и их удалось опознать побайтным сравнением с эталонными
образами ПЗУ из архива. Карта области:

| Адрес во флеш | Размер | Что это |
| --- | :-: | --- |
| `0x08020000` | 8 Кбайт | растровый знакогенератор контроллера (не ПЗУ БК) |
| `0x08022000` | 4 Кбайт | ПЗУ **АльтПро A16M v2.41**, изменено (§18.5) |
| `0x08023000` | 4 Кбайт | ПЗУ **СМК v2.05** (ROM-BIOS), изменено (§18.5) |
| `0x08024000` | 8 Кбайт | **1801РЕ2-017** — монитор БК-0010, **без изменений** |
| `0x08026000` | 8 Кбайт | **1801РЕ2-017** — монитор БК-0010, **изменённая копия** (§18.5) |
| `0x08028000` | 8 Кбайт | **1801РЕ2-325** — расширение БК-0011М |
| `0x0802A000` | 8 Кбайт | **1801РЕ2-324** — БОС БК-0011М |
| `0x0802C000` | 8 Кбайт | **1801РЕ2-018** — Фокал |
| `0x0802E000` | 8 Кбайт | **1801РЕ2-106** — Бейсик БК-0010, часть 1 |
| `0x08030000` | 8 Кбайт | **1801РЕ2-107** — Бейсик, часть 2 |
| `0x08032000` | 8064 байта | **1801РЕ2-108** — Бейсик, часть 3 |
| `0x08034000` | 8 Кбайт | **1801РЕ2-327** — Бейсик БК-0011М, часть 1 |
| `0x08036000` | 8 Кбайт | **1801РЕ2-328** — Бейсик БК-0011М, часть 2 |
| `0x08038000` | 8 Кбайт | **1801РЕ2-329** — Бейсик БК-0011М, часть 3 |
| `0x0803E800` | 8064 байта | **1801РЕ2-019** — МСТД БК-0010 |
| `0x08040800` | 8 Кбайт | **1801РЕ2-330** — МСТД БК-0011М |

Отсюда прямо следуют возможности из перечня [GMPI]: «Запуск блока МСТД как для
БК-0010, так и 0011М; для 0010 — МСТД с Фокалом» (образы 019 и 018 лежат рядом),
и подмена любой отсутствующей микросхемы Бейсика.

На экране БК контроллер показывает наличие микросхем строками вида `2 017:`,
`2 201:`, `2 202:`, `2 324:`, `2 325:` (то есть «РЕ2-017» и т. д.) и отдельно
` 1:`, ` 2:`, ` 3:` для трёх БИС Бейсика **[✓ ПО]**.

---

## 18.5. Как контроллер перехватывает обращения БК

Это самая интересная часть устройства, и в документации проекта она не описана.
**[✓ ПО]** В прошивке лежат **три изменённых образа ПЗУ**, и во все три врезан
один и тот же короткий фрагмент кода.

### Общая схема врезки

Во всех трёх случаях врезка устроена одинаково:

1. в один из адресов страницы ввода-вывода записывается **указатель на блок
   параметров** (тот самый, который БК подготовила для штатного драйвера);
2. выполняется запись `MOV #177741, @#177700` — обращение, по которому
   контроллер опознаёт запрос;
3. две команды `NOP` — пауза, за которую контроллер успевает отработать;
4. управление возвращается через штатную подпрограмму восстановления регистров.

Запись по `177700` смысла для самой машины не имеет: это внутренний регистр
периферийного блока К1801ВМ1, у которого при записи задействованы лишь разряды
0–2 (§4.3). Судя по всему, врезка использует не результат записи, а
**сам факт цикла на магистрали**, который контроллер-супервизор наблюдает.
Это объяснение — реконструкция по коду, прямого подтверждения в источниках нет.

### Перехват драйвера магнитофона

Изменённая копия ПЗУ монитора отличается от штатного 1801РЕ2-017 ровно на
**27 байт в диапазоне `116102`–`116135`** — это начало драйвера магнитофона,
разобранное в §12.6б. Штатный пролог заменён целиком:

```
        было (штатный монитор, [Рад])          стало (Gryphon-MPI)
116102  MOV   R1, 306                          MOV   R1, @#177734
116106  MOV   #177716, R3                      MTPS  #340
116112  CLR   300                              MOV   #177741, @#177700
116116  CLR   302                              NOP
116122  MOV   @#4, -(SP)                       NOP
116126  MOV   #116214, @#4                     MTPS  #0
116134  MOV   SP, 310                          JSR   R4, @#110362
                                               RTS   PC
```

Логика врезки: адрес блока параметров из `R1` отдаётся контроллеру через
`177734`, прерывания запрещаются (`MTPS #340`), подаётся сигнал запроса, затем
приоритет восстанавливается и управление возвращается по штатной подпрограмме
`110362` — восстановлению R0–R4 (§9.6). Обратите внимание, что **вход в драйвер
по `116076` не тронут**: сохранение регистров `JSR R4,110346` остаётся штатным,
подменяется только тело.

Практически это и означает «перенаправление чтения с кассеты на SD-карту»,
о котором говорит [GMPI]. Понятно и почему функция требует доработки БК: чтобы
подставить изменённый монитор, контроллер обязан уметь **отключить штатное ПЗУ**
(§18.2) — без входа блокировки Б1 эмуляция магнитофона на БК-0010 недоступна.

### Перехват дисковых драйверов

Образы ПЗУ **A16M v2.41** и **СМК v2.05** изменены **одинаково** — по 18 байт
в диапазоне `160372`–`160415`, в теле подпрограммы драйвера:

```
160372  MOV   R3, @#177132
160376  MOV   #177741, @#177700
160404  NOP
160406  NOP
160410  MTPS  #0
160414  RTS   PC
```

Здесь указатель передаётся через `177132` — **штатный регистр данных контроллера
НГМД** (§14.2г), что естественно: машина и так адресует туда дисковый обмен.

Отсюда следует важное для понимания: **эмуляция АльтПро в Gryphon-MPI — не
потактовое повторение схемы СМК**, а подмена ПЗУ контроллера на образ, драйвер
которого переадресован супервизору. Регистры и режимы памяти при этом должны
вести себя как у настоящего СМК (§17.5–17.7), иначе программы, работающие с
`177130` напрямую, мимо драйвера, работать не будут.

---

## 18.6. Эмуляция контроллеров АльтПро

Контроллер эмулирует **СМК-512 либо А-16** — расширенную память вместе с
дисководом и жёстким диском [GMPI]. Полное описание самих контроллеров, их
регистра `177130`, восьми режимов памяти и разметки винчестера — в
[17-контроллер-СМК.md](17-контроллер-СМК.md); здесь только то, что относится
к Gryphon-MPI.

В интерфейсе контроллера это три «устройства», запускаемые по отдельности:
`basic` (ПЗУ Бейсика), `a16` (АльтПро А-16), `smk` (АльтПро СМК-512), плюс
`mstd` (блок МСТД) **[✓ ПО]**. Для СМК и А-16 доступны три «привода»:

| Идентификатор | Что это |
| :-: | --- |
| `F0`, `F1` | дисководы (образы `.img`, `.dsk`, `.bkd`) |
| `H0` | жёсткий диск (образы `.hdi`, `.img`) |
| `T` | «лента» — каталог на SD-карте, из которого читает эмулируемый магнитофон |

При запуске файла можно явно указать, через какой контроллер его подставлять
(параметр `pref`) **[✓ ПО]**:

| Код | Устройство |
| :-: | --- |
| 0 | Авто |
| 1 | ПЗУ Бейсика |
| 2 | А-16 |
| 3 | СМК-512 |

Ремонтное меню умеет **принудительно включать блокировки, переключать страницы
памяти БК-0011(М) и показывать режимы АльтПро** [GMPI]; в прошивке этому
соответствуют пункты `Blk ROM`, `Blk RAM 11`, `Blk Monitor`, `Prepare Mon`,
`Set RAM pages` **[✓ ПО]**.

---

## 18.7. Блок ИРПС и IP-модем

Контроллер эмулирует **блок ИРПС** (§13) с выходом на последовательный порт и
поверх него — **IP-модем** для выхода в сеть: Telnet, IP-BBS и т. п. [GMPI].

**[✓ ПО]** Набор адресов и векторов совпадает в веб-интерфейсе и в прошивке
основной платы:

| Код | Регистры / вектор |
| :-: | --- |
| 0 | отключён |
| 1 | `177560` / `60` |
| 2 | `176560` / `360` |
| 3 | `176570` / `370` |
| 4 | `177560` / `360` |

Вариант 2 — это штатный блок ИРПС БК (§6.5а, §13.5; регистры `176560`…`176566`);
остальные соответствуют распространённым переделкам.

Скорости: **300, 600, 1200, 2400, 4800, 7200, 9600, 12000, 14400 бод** и режим
«без ограничения» (в интерфейсе — «Неогр.»), выбираются индексом 0…9 **[✓ ПО]**.

Модем управляется AT-командами; справка выводится по `AT?` **[✓ ПО]**:

```
AT               - attention reply
AT?              - Help
ATE              - local echo
AT+V,  AT+GMR    - firmware version
AT+IP, AT+CIPSTA - show local IP
ATD<URL>, AT+CONNECT<URL> - connect <URL>
ATD              - connect last URL
ATD?             - show last URL
```

Ответы модема — `CONNECTED` и `ERROR`. В веб-интерфейсе есть **терминал ИРПС**,
работающий через WebSocket на порту **81** **[✓ ПО]**.

---

## 18.8. Носители, форматы и автоподстановка ОС

Поддерживаются карты **до 32 Гбайт**, файловые системы **FAT и FAT32**; SDXC не
поддерживаются [GMPI]. Форматы файлов: **`.BIN`** (программы, в том числе
многофайловые, и картинки), **`.IMG`, `.DSK`, `.BKD`** (дискеты), **`.HDI`,
`.IMG`** (жёсткий диск), **`.MSF`** (снимок состояния БК в формате эмулятора
GID). В прошивке эти наборы расширений хранятся строками
`bin`, `img,dsk,bkd`, `hdi,img` **[✓ ПО]**.

Рекомендуемая структура карты [GMPI]:

| Каталог | Назначение |
| --- | --- |
| `BK_OS` | загрузочные дискеты для разных ОС — **подставляются автоматически** |
| `BK_MSF_TPL` | шаблоны для сохранения состояния БК в формате `.MSF` |
| `BK_Test` | тесты БК |
| `BK_Tools` | утилиты |
| `BK_IRPS` | терминалы для работы с ИРПС |

**[✓ ПО]** Автоподстановка устроена так: контроллер определяет файловую систему
внутри образа дискеты и подставляет загрузочный образ по шаблону пути
**`\BK_OS\<ИМЯ_ФС>.IMG`**. Список опознаваемых систем зашит в прошивку:

`ANDOS`, `MKDOS`, `AODOS`, `NORD`, `MIKRODOS`, `CSIDOS3`, `RT11`, `NCDOS`,
`DXDOS`, `MSDOS`, `FAT12`

плюс варианты подписей `DX-DOS`, `AO-DOS`, `BY VOLAND`, `?BOOT-`, `?QUBOOT-`.
Если систему опознать не удалось, выводится ` UNKNOWN FS, NON BOOTABLE `.

Утилита `Mirage` в меню запускает `\BK_Tools\MIRAGM.bin` с карты **[✓ ПО]**.

---

## 18.9. Меню на экране БК и ремонтное меню

Меню вызывается **в любой момент нажатием кнопки на контроллере** [GMPI]; для
вывода на экран БК контроллер использует собственный знакогенератор, лежащий в
прошивке (§18.4).

**[✓ ПО]** Состав меню восстановлен по строкам прошивки:

| Раздел | Пункты |
| --- | --- |
| Верхний уровень | `MAIN`, `Devices`, `Diagnostic`, `Browse SD`, `BK Menu`, `Debug` |
| Устройства | `Run A16M`, `Run SMK-512`, `Run MSTD`, `IRPS`, `FDD Select`, `Drive images`, `MPI Dev` |
| Диагностика | `Detect`, `MEM Scan`, `MEM Test`, `Keyboard Test`, `Char map`, `Tests`, `Mirage` |
| Управление машиной | `Reset`, `Reset+HLT`, `DCLO Stop`, `Unhalt`, `Blk ROM`, `Blk RAM 11`, `Blk Monitor`, `Prepare Mon`, `Set RAM pages` |
| Журнал | `LOG Start`, `LOG View`, `Clear` |
| Сеть | `WiFi`, `SSID`, `Password`, `IP`, `NET Firmware`, `Update` |

Отдельно стоит **монитор магистрали** (`MPI Monitor`): на экран выводятся
`ADDR: %06o`, `DATA: %06o`, счётчики `IN/s` и `OUT/s`, а также состояния линий,
обозначенные `Syn`, `Out`, `Rpl`, `Ppl`, `Byt` — то есть СИА, ВЫВОД, СИП и
БАЙТ (§14.2б); что именно обозначено `Ppl`, по строкам установить не удалось.
Есть и журнал обращений к шине.

Ремонтные функции доступны **только при подключённых дисплее и энкодере** —
то есть на двухплатном варианте [GMPI].

---

## 18.10. Сетевые функции

* **WiFi.** При включении контроллер пытается подключиться к сохранённой сети;
  если не удалось — через 15–30 секунд поднимает точку доступа **`GryphonMPI`**
  с паролем **`BK0010ap`**, веб-интерфейс на `http://192.168.4.1/` [GMPI].
  Параметры можно задать и из меню на экране БК. После подключения адрес
  запрашивается по DHCP.
* **mDNS** — имя **`GryphonMPI.local`** **[✓ ПО]**.
* **Веб-интерфейс** — одностраничное приложение, лежащее в файловой системе
  littlefs сетевой платы. Разделы: «О Компьютере» (включая «ПЗУ 1801РЕ2»),
  «Файлы SD карты», «Быстрый запуск файла», «Запуск файла с SD карты»,
  «ПЗУ Бейсик „Вильнюс“», «АльтПро5 А-16», «АльтПро СМК-512», «ПЗУ МСТД»,
  «Блок „ИРПС“» с терминалом и настройками модема, «Настройка WiFi», «SD карта»,
  «Отладка» (состояние, процессор), «О Контроллере» **[✓ ПО]**.
* **WebDAV** — заливка и чтение файлов на карте по сети **[✓ ПО]**: в прошивке
  сетевой платы присутствует реализация DAV с блокировками.
* **WebSocket** на порту 81 — терминал ИРПС **[✓ ПО]**.
* **REST API** — см. §18.11.

---

## 18.11. Сетевое API

Официальное описание [GMPI] помечено автором как черновик; ниже — сводка,
**сверенная с обеими сторонами реализации [✓ ПО]**: с клиентским сценарием
веб-интерфейса и со списком маршрутов в прошивке сетевой платы. Все ответы —
JSON; в ответе всегда есть либо `"success":1`, либо `"error":"…"`.

| Запрос | Параметры | Ответ / действие |
| --- | --- | --- |
| `GET /api/version` | — | `{"version-mpi":…, "version-net":…, "serial-mpi":…}` |
| `GET /api/bkinfo` | — | `{"bkinfo":[семейство, модель, слово состояния, разряды ошибок]}` — расшифровка в §18.3 |
| `GET /api/status` | — | `{"status":…}` |
| `GET /api/dir` | `path` | `{"charset":"utf8", "dir":[{"type","size","name"}, …]}` |
| `GET /api/mkdir` | `path` | создать каталог |
| `GET /api/delete` | `path` | удалить |
| `GET /api/rename` | `from`, `to` | переименовать/переместить |
| `GET /api/getdrv` | `drv` | `{"drive":…, "path":…}` |
| `GET /api/setdrv` | `drv`, `path` | смонтировать образ в привод `F0`/`F1`/`H0`/`T` |
| `GET /api/run` | `dev` (`basic`\|`a16`\|`smk`\|`mstd`\|`file`), `emu10`, `fname`, `pref` | запустить устройство или файл |
| `GET /api/irps_mode` | `mode` (0…4) | режим ИРПС, см. §18.7 |
| `GET /api/irps_speed` | `speed` | скорость обмена |
| `GET /api/net_cfg` | `sta_ssid`, `sta_psk`, `save` | настройка WiFi |
| `GET /api/client_ctl` | `connect` \| `disconnect` | управление соединением модема |
| `GET /api/sd_format` | — | **форматирование SD-карты** (добавлено в прошивке 2024_03_23) |
| `POST /api/upload` | форма `multipart`: `storeas`, `size`, `file` | загрузка файла |

Типовой сценарий «залить и запустить» из [GMPI] — две команды `curl`: загрузка
файла в `/BK_Uploads/` и затем `run?dev=file&emu10=no&fname=…`. Готовые
`gryphon_run.sh` и `gryphon_run.bat` есть на сайте; локальная копия скрипта —
`Gryphon-MPI/gryphon_run.sh`.

---

## 18.12. Протокол «основная плата ↔ сетевая плата»

**[✓ ПО]** Платы связаны последовательным каналом (`NET_TX`/`NET_RX`, §18.1) и
обмениваются текстовыми командами, начинающимися с `>`. Список восстановлен из
обеих прошивок и почти буквально повторяет структуру REST API — сетевая плата
служит шлюзом:

| Команда | Назначение |
| --- | --- |
| `>ID`, `>VERSION` | опознание и версии |
| `>BK.INFO`, `>BK.STATUS` | сведения о машине и её состояние |
| `>BK.MENU.MAIN`, `>BK.MENU.SHARED`, `MENU.SHOW` | управление меню на экране БК |
| `>BK.RUN.BASIC`, `>BK.RUN.A16`, `>BK.RUN.SMK`, `>BK.RUN.MSTD`, `>BK.RUN.FILE%i` | запуск устройств и файлов |
| `>DIR.OPEN`, `>DIR.NEXT`, `>DIR.MK`, `>DIR.RM`, `>DIR.MV`, `>DIR.DAT` | работа с каталогом; данные отдаются JSON-фрагментами `{"type":"%c","size":%i,"name":"…"}` |
| `>DRV.GET`, `>DRV.SET`, `>DRV.DAT` | приводы; ошибки — `Wrong FDD Num`, `Wrong HDD Num`, `Wrong drive ID` |
| `>M.CONN`, `>M.DISC`, `>M.DATA`, `>M.RX`, `>M.MODE`, `>M.SPEED`, `>M.AV` | модем и ИРПС |
| `>NET.IP`, `>NET.DAV …` | состояние сети и ход передачи по WebDAV (с процентами) |
| `>SSID`, `>PASS`, `RELEASE` | настройка WiFi |
| `>SD.`, `>PARAM`, `>STATUS.MENU.`, `>STATUS.ACTIVE.` | карта, параметры, признаки активности |

---

## 18.13. Обновление прошивки

Обе платы прошиваются с SD-карты через меню на экране БК [GMPI]:

* **основная плата** — «Настр» → «Настр» → «Версия ПО», выбрать файл прошивки;
* **сетевая плата** — «Настр» → «WiFi» → «Версия ПО»; **прошить нужно оба
  файла** комплекта (код и `data`), порядок не важен, затем выключить и включить
  питание. Комплект 2024_03_23 добавил форматирование SD-карты, скачивание
  файлов из браузера помимо WebDAV и ускорил загрузку интерфейса;
* **аварийное обновление** основной платы: записать прошивку на карту под именем
  **`Rescue.fw`**, выключить БК, нажать и удерживать кнопку на контроллере,
  включить БК, дождаться начала прошивки и отпустить кнопку.

**[✓ ПО]** Файл `.dfu` основной платы — стандартный контейнер **DfuSe** с одной
целью и тремя элементами: `0x08000000` (664 байта), `0x080002A0` (275 128 байт —
собственно программа) и `0x20000000` (24 452 байта, практически полностью нули —
образ инициализируемых данных). Прошивка сетевой платы — образ ESP8266 (файл 1)
и образ файловой системы **littlefs** размером 1 024 000 байт — 125 блоков по
8192 байта (файл 2, `*.data.fw`), где лежит веб-интерфейс.

---

## 18.14. Известные ограничения и границы разбора

Со слов автора [GMPI], на момент публикации не работали демонстрационные
программы «Good/Bad Apple» и некорректно отображался палитровый эффект в
демо «Однажды».

Чего **не** удалось установить и что поэтому здесь не описано:

* побитовой карты собственных регистров контроллера на магистрали: публичной
  документации нет, а в коде STM32 адреса вычисляются, а не хранятся
  константами. Достоверно известны только адреса из врезок в ПЗУ —
  `177734`, `177132` и `177700` (§18.5);
* объёма ОЗУ, отдаваемого в режиме эмуляции СМК-512: явно нигде не указан;
  заявлена функциональная совместимость, то есть предполагаются 512 Кбайт
  (§17.1);
* точного типа кристалла STM32 — по косвенным признакам это F4 с FSMC (§18.1);
* смысла индикатора `Ppl` в мониторе магистрали (§18.9).

Разбор выполнен по прошивкам **2022_09_01** (основная плата) и **2024_03_23**
(сетевая плата); в других версиях состав образов ПЗУ и набор команд API могут
отличаться.
