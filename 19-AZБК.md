# 19. Контроллер AZБК (MAXIOL AZ)

> **Источники главы**
> * **[AZ]** — сайт проекта https://azbk.maxiol.com/ (перечень возможностей).
> * **[AZ-Форум]** — **основной источник по технике.** Разработчик (`SuperMax`)
>   ведёт на https://forum.maxiol.com подробное техническое описание, которое
>   повторяется и дополняется в каждом сообщении о новой прошивке. Использованы
>   темы: **5553** «AZ BK обновления прошивки» (кумулятивное описание регистров,
>   видеоконтроллера, звука, маппера, блиттера), **5593** «AZБК в режиме эмуляции
>   SMK-512», **5617** «Программирование под AZБК», а также тема о контроллере
>   псевдодиска на MicroSD — там описан **программный интерфейс AZ** (CSR/DR и
>   блок команд) с примерами на ассемблере.
> * **[AZ-ПО]** — **разбор прошивок**, выполненный для этой главы: `AZ БК/Прошивки/00018/`
>   и `00019/` — прошивка микроконтроллера `AZBK_000NN.hex` (Intel HEX), прошивка
>   ПЛИС `AZBK_000NN.pof` / `.sof`, а также **ПЗУ для самой БК**: `azboot.ROM`,
>   `AZLIB00.ROM`…`AZLIB03.ROM`, `SETUP.ROM`. Пометка **[✓ ПО]** здесь означает
>   ровно то же, что и в главе 18 (методика — в §16.4).
> * **[AZ-Док]** — `AZ БК/AZ.docx`, `AZ БК/AZ_2.docx` (краткая инструкция),
>   `AZ БК/AZ.INI` (рабочий файл конфигурации), образы карты
>   `MicroSD_image_*_v18_*` и архив `AZBK.7z`.

**AZБК** — контроллер-расширитель для разъёма МПИ, самое «тяжёлое» из
современных устройств для БК. В отличие от СМК (§17), который добавляет к
машине память и диски, и от Gryphon-MPI (§18), который подменяет её ПЗУ, AZБК
**замещает почти всю обвязку БК**: собственный менеджер памяти на 32 Мбайт,
собственный видеоконтроллер с VGA-выходом, звуковую подсистему, сеть и
BIOS-подобную систему настройки. От исходной машины остаются процессор,
клавиатура и корпус.

---

## 19.1. Состав

**[✓ ПО]** Электрически контроллер построен на двух кристаллах:

| Часть | Что известно |
| --- | --- |
| **ПЛИС** | **Altera Cyclone II EP2C8Q208C8** (8 тыс. логических элементов, корпус PQFP-208). Опознано по заголовку файла `.sof`; проект собран Quartus II 13.0.1. Прошивки поставляются в двух видах: `.pof` (для загрузочной ПЗУ конфигурации, 524 509 байт) и `.sof` (загрузка в ОЗУ ПЛИС, 240 800 байт) |
| **Микроконтроллер** | **STM32**; прошивка `AZBK_000NN.hex` занимает 98 552 байта в диапазоне `0x08000000`…`0x080180F7`, начальный указатель стека `0x200118B8`, точка входа `0x08000189` |

Такое разделение видно и по сообщениям об обновлениях: часть выпусков помечена
«новое только прошивка STM32», часть требует перепрошивки ПЛИС [AZ-Форум]. ПЛИС
занимается магистралью, памятью, видео и звуком; микроконтроллер — картой
памяти, файловыми системами, сетью и часами.

Кроме того, в комплект прошивки входят **ПЗУ, исполняемые самой БК** (§19.11), и
**сетевой модуль** Ethernet 10 Мбит [AZ].

Заявленные возможности [AZ]: до 32 эмулируемых дисков на одной MicroSD;
совместимость с RT-11, ANDOS, MKDOS, AODOS, NORD и другими; расширение ОЗУ до
**32 Мбайт**; VGA-выход 1024×768 60 Гц; кадровый таймер; генератор псевдослучайных
чисел; звук AY, 2×AY, OPL2, ковокс, речевой DAC TDA1387; DMA-контроллер звука;
RTC и энергонезависимая память; открытое API; Setup; эмуляция БК-0011М на
БК-0010; палитра 32768 цветов; блиттер.

---

## 19.2. Адресное пространство памяти AZ

Внутренняя память контроллера — **32 Мбайт**, адресуемые 24-разрядным **словным**
адресом. Нижний мегабайт поделён на четыре служебных области, всё остальное —
обычное поле памяти [AZ-Форум]:

| Диапазон | Назначение |
| --- | --- |
| **0…128 Кбайт** | «теневая» память: полная копия ОЗУ БК-0011М (или эмулируемого ОЗУ на БК-0010). Из неё идёт вывод «старых» видеорежимов на VGA |
| **128…256 Кбайт** | служебная память контроллера, всегда R/W и всегда даёт СИП |
| **256…512 Кбайт** | **только для чтения** — сюда загружаются образы эмулируемых ПЗУ; записать их со стороны БК нельзя, только средствами AZ |
| **512…1024 Кбайт** | область для эмуляции СМК-512 |
| **с 1 Мбайт и далее** | обычная память без ограничений; при обращении всегда выдаётся СИП, если для окна не включён режим «только чтение» |

---

## 19.3. Маппер памяти: 16 окон

Ключевое отличие AZБК от всех прежних расширителей — **полный маппер**: любую
страницу внутренней памяти можно подставить на **любой** адрес БК. Дословно
[AZ-Форум]:

> маппер V2 — это чисто маппер AZ без костылей и **все** запросы к мапперам
> `177716` (БК-0011М) или `177130` (СМК) транслируются в маппер AZ, который
> является единым устройством управления памятью… Это означает что на любой
> адрес в адресном пространстве БК10/БК11М можно подставить любую страницу
> памяти!

64-килобайтное адресное пространство БК разбито на **16 окон по 4 Кбайт** —
та же дискретность, что у СМК (§17.4). Каждому окну соответствует свой регистр,
в который пишется **13-разрядный номер страницы**; младшие 11 разрядов адреса
приходят с магистрали, и вместе получается 24-разрядный адрес слова. Старшие
3 разряда регистра зарезервированы — маппер рассчитан на расширение до 256 Мбайт.

| Окно | Адреса БК | Регистр | | Окно | Адреса БК | Регистр |
| :-: | --- | :-: | --- | :-: | --- | :-: |
| 0 | `000000–007777` | `177300` | | 8 | `100000–107777` | `177320` |
| 1 | `010000–017777` | `177302` | | 9 | `110000–117777` | `177322` |
| 2 | `020000–027777` | `177304` | | 10 | `120000–127777` | `177324` |
| 3 | `030000–037777` | `177306` | | 11 | `130000–137777` | `177326` |
| 4 | `040000–047777` | `177310` | | 12 | `140000–147777` | `177330` |
| 5 | `050000–057777` | `177312` | | 13 | `150000–157777` | `177332` |
| 6 | `060000–067777` | `177314` | | 14 | `160000–167777` | `177334` |
| 7 | `070000–077777` | `177316` | | 15 | `170000–177777` | `177336` |

Три регистра-маски, в каждом младший разряд соответствует нулевому окну:

| Адрес | Регистр |
| :-: | --- |
| `177340` | **активация окон** — какие окна подключены к магистрали |
| `177342` | **«только чтение»** — в каких окнах запрещена запись (то есть окно ведёт себя как ПЗУ) |
| `177344` | **shadow** — в каких окнах всякая запись дублируется в «теневую» память |

Режим shadow нужен, чтобы на БК-0011М содержимое экранных страниц попадало в
первые 128 Кбайт памяти AZ, откуда видеоконтроллер формирует изображение для
VGA; при работе с маппером БК-0011М он устанавливается автоматически.

Пример из документации разработчика — подключить обычную память в окно 14
(`160000–167777`):

```
177334 <- 400      ; указатель на первый мегабайт
177340 <- 40000    ; разрешение окна 14
177342 <- сброс разряда 14   ; хотим ОЗУ, а не ПЗУ
```

Если в `177342` разряд 14 **установить**, то же окно станет «ПЗУ» — доступным
только на чтение.

Ещё два регистра доступны **только на чтение** и служат для того, чтобы
программа могла узнать, что было записано в маппер БК или СМК (штатными
средствами это невозможно):

| Адрес | Что содержит |
| :-: | --- |
| `177350` | копия последней записи в `177130` в режиме управления памятью СМК |
| `177352` | копия последней записи в `177716` в режиме управления памятью БК-0011М |

**[✓ ПО]** Все перечисленные адреса независимо подтверждаются разбором ПЗУ
самого контроллера: сканирование `azboot.ROM`, `AZLIB0x.ROM` и `SETUP.ROM` на
слова из страницы ввода-вывода даёт ровно этот набор — `177300`…`177336`
(по 2–3 обращения на регистр, как и положено таблице окон), `177340`, `177342`,
`177344`, `177346`, `177352`, а также блок `177220`…`177236`.

---

## 19.4. Регистр управления маппером `177346`

Дословно по [AZ-Форум], разряд за разрядом:

| Разряд | Дост. | Назначение |
| :-: | :-: | --- |
| 00, 01 | R/O | версия аппаратной части платы ПЛИС (сейчас `0`) |
| 02 | R/W | кадровый таймер: `1` — 50 Гц, `0` — 60 Гц (60 Гц берётся от VGA-модуля, 48 Гц генерируется из 65 МГц) |
| 03 | R/W | `1` — включить таймер по вектору `100` безусловно |
| 04 | — | резерв, `0` |
| 05 | R/W | управление эмуляцией ПЗУ в БК-0011М: подключение расширенных ПЗУ Бейсика (325, 327 и 328, 329) средствами AZБК. Разряд независим от эмуляции БК-0011М — возможен случай БК-0011М вообще без ПЗУ |
| 06–08 | R/W | тактовая частота процессора: `3`, `4`, `5`, `6` МГц; `0` — не определена. Заполняется программно стартовым ПЗУ |
| 09 | R/W | отключить прохождение СИА на ВП1-037 для адресов `000000–077777`: 037-я не отвечает и не принимает запись, СИП выдаёт маппер, и из маппера становится возможно читать. Работает только при разряде 14 = `1` |
| 10 | R/W | отключить ответ 037-й на `177660–177663` (эмуляцию 014-й), СИП по этим адресам выдаёт AZ. Работает только при разряде 14 = `1` |
| 11 | R/W | **включение эмуляции БК-0011М**; активируется только при разряде 12 = `1`. Разрешает запись в `177662`, отчего начинают работать палитры и кадровый таймер |
| 12 | R/W | тип машины: `0` — БК-0011М (состояние по умолчанию), `1` — БК-0010. Проверяется и устанавливается программно стартовым ПЗУ; значение `1` обязательно для эмуляции БК-0011М на БК-0010 |
| 13 | R/W | наличие работоспособной доработки «окна 1»: `1` — отключение окна 1 традиционным способом (оттягивание выхода TM9 на землю). Сбрасывается в `0` при установке разряда 14 |
| 14 | **R/O** | тип доработки, определяется ПЛИС автоматически: `0` — доработка окна 1 либо её отсутствие, `1` — доработка с управлением 037-й |
| 15 | R/W | `1` — отключение окна 1 в БК-0011М; транслируется из эмуляции СМК и подаётся либо сигналом на контакт А22, либо переключением окон 11М — в зависимости от типа доработки |

Разряды 09, 10 и 14 — самое любопытное в этом регистре: они позволяют
**отключить от магистрали штатный контроллер ОЗУ и видео КР1801ВП1-037** (§14.2г,
§15.1) и полностью заменить его маппером AZ. Именно это и делает возможной
«эмуляцию БК-0011М на БК-0010» и подстановку произвольных страниц на любые
адреса.

---

## 19.5. Совместимость с СМК и «хак» контакта А22

AZБК умеет прикидываться СМК-512 (опция `SMK-512 Emulation` в Setup, §19.12):
запись в `177130` транслируется в маппер AZ. Отдельная сложность — приём,
которым СМК отключает окно 1 БК-0011М. Разбор [AZ-Форум], тема 5593:

> …это выход D36 — 12-я нога, см. схему БК-0011М. Этот выход обозначен в шине
> номером 66 и он выведен на разъём МПИ на пин **A22**… это выход, который
> **разрешает работу ПЗУ ЛВС**… Но, к сожалению, разработчики решили сделать
> хитрость и зацепили выход своего регистра на А22 МПИ с целью манипулировать
> им напрямую!

Физический ноль, подаваемый контроллером, «перетягивает» линию, и схема
отключения ОЗУ считает, что через `177716` включено ПЗУ. Соединять два выхода
без открытого коллектора или третьего состояния некорректно: микросхемы, скорее
всего, не сгорают, но греются — ток ограничивается резистором. Правильный
порядок — сначала записать код отключения ПЗУ в `177716`, и только потом
подключать страницы контроллером. Тот же приём и те же последствия описаны с
другой стороны в §17.3.

Чтобы поддержать программы, эксплуатирующие этот приём, в AZБК предусмотрена
доработка платы (отпаять резистор `R14` и подвести проводник от вывода ПЛИС),
наличие которой контроллер определяет автоматически и отражает в разряде 14
регистра `177346`.

---

## 19.6. Видеоподсистема

VGA-выход 1024×768 60 Гц; палитра **32768 цветов** (по 5 разрядов на канал);
режимы 2/4/8/16/256 цветов с независимыми палитрами; три слоя с аппаратным
наложением; вертикальный и горизонтальный скроллинг [AZ].

Регистры [AZ-Форум]:

| Адрес | Назначение |
| :-: | --- |
| `177230` | регистр управления видеорежимом |
| `177232` | номер страницы начала отображения — **верхняя** страница (слой 0) |
| `177240` | то же — **средняя** страница (слой 1), «под спрайты» |
| `177242` | то же — **нижняя** страница (слой 2), «под фон» |
| `177244`, `177246`, `177250` | вертикальный скроллинг слоёв 2, 1, 0 |
| `177252`, `177254`, `177256` | горизонтальный скроллинг слоёв 0, 1, 2 |
| `177234` | адрес ячейки палитры (9 разрядов) |
| `177236` | значение палитры (15 разрядов) |

Регистр `177230` — «конструктор видеорежимов»:

| Разряды | Поле |
| :-: | --- |
| 00–02 | режим цветовой интерпретации |
| 03–04 | длина строки в словах: `0` — 32, `1` — 64, `2` — 128, `3` — 256 |
| 06–07 | растяжение по горизонтали: ×1 (1024 точки), ×2 (512), ×3 (256), ×4 (128) |
| 09–10 | количество повторов строк: ×1 (768 строк) и далее |
| 12–15 | длина рулона в словах |

Режимы цветовой интерпретации: `0` — 1 разряд на точку (2 цвета), `1` — 2 разряда
(4 цвета), `2` — 4 разряда (16 цветов), `3` — 8 разрядов (256 цветов); значения
`4`…`7` — те же глубины, но **со включёнными слоями**.

Длину строки нужно согласовывать с режимом вручную, иначе будут повторы или
пропуски данных; расчёт простой — например, для 512×384 при 16 цветах:
512 точек × 4 разряда = 2048 разрядов ÷ 16 = **128 слов**.

### Блиттер

Начиная с версии 15 в контроллере есть **блиттер** — он читает пакет команд и
выполняет их по окончании отображения кадра [AZ-Форум]. Ограничения: работает
только словами (минимальная дискретность по горизонтали — 2 точки в
256-цветном режиме); команда — 8 слов; максимум **128 команд** в пакете (на
сайте указано «до 511» — расхождение между источниками); адресация плоская,
24-разрядная, то есть блиттеру видны сразу все 32 Мбайт; исходные спрайты
хранятся линейно с выравниванием на 32 слова (адрес кратен `100`); приёмник
прямоугольный (строка + пропуск); максимум 256 слов ширины, 256 строк.

Операции: заполнение константой; копирование; наложение с прозрачным цветом
(попиксельное, с точностью до байта); наложение с проверкой цвета приёмника;
сохранение фона (копирование из прямоугольной области в линейную).

Чтение пакета происходит по концу 767-й строки, запуск — с началом 769-й, то
есть одновременно с кадровым прерыванием: команды можно безопасно менять в
обработчике прерывания.

---

## 19.7. Звук

| Адреса | Узел |
| :-: | --- |
| `177160`–`177176` | **DMA-контроллер звука** и OPL2 |
| `177200`–`177212` | ковокс / микшер |
| `177714` | «legacy» AY — с автоопределением, ковокс это или AY |

DMA-контроллер [AZ-Форум]: `177160` — регистр управления (пуск, однократный или
циклический режим, принудительный останов, признак завершения, потоковое
воспроизведение с подкачкой для IMA ADPCM, скорость — поддерживается 44100,
режим интерпретации данных); `177162` — номер страницы, с которой читаются
данные (дискретность 4 Кбайт); `177164` и `177166` — старшая и младшая части
длины; `177170` — номер текущей страницы воспроизведения. Поддерживаются
PCM 16 моно, IMA ADPCM моно и стерео. Данные идут на микшер отдельным каналом.

**OPL2** (экспериментально с версии 15b4): `177176` — адрес (младший байт),
`177177` — данные (старший байт); допускается и словная запись.

Ковокс: `177200` — 16 разрядов левый канал, `177202` — правый, `177204` — моно
(запись дублируется в оба), `177206` — 8-разрядный стерео/моно (запись слова
трактуется как стерео, запись младшего байта — как моно). `177212` — регистр
управления звуком.

---

## 19.8. Прочие узлы

| Адрес | Узел |
| :-: | --- |
| `177550` | **генератор псевдослучайных чисел** — LFSR длиной 128 разрядов, младшие 16 доступны программно; новое число на каждую выборку |
| `177560`–`177566` | **UART** со стандартными адресами последовательного канала (§13): `177560` — состояние приёмника (разряд 07 — есть данные), `177562` — управление и принятые данные, `177564` — состояние передатчика, `177566` — данные на передачу |
| `177220`–`177226` | **интерфейс AZ** (§19.9) |
| `177100`–`177106` | отладочные регистры |
| `177000` | загрузчик |

Есть также **RTC** и блок **энергонезависимой памяти** — 255 слов, доступных
через блок команд AZ; время можно получать с NTP-серверов [AZ], [AZ-Форум].

---

## 19.9. Программный интерфейс AZ

Основной способ, которым программа БК разговаривает с контроллером, — пара
регистров [AZ-Форум]:

| Адрес | Регистр |
| :-: | --- |
| `177220` | **РКС** — регистр команд и состояния (CSR) |
| `177222` | **РД** — регистр данных (DR) |
| `177224`, `177226` | однословные ПЗУ начальных загрузчиков |

Обращение **только словное, байтовый доступ запрещён**. Типовой цикл: записать
код команды в `177220`, дождаться разряда готовности, обменяться данными через
`177222`. Запись нуля в `177220` — сброс контроллера. Для обмена блоками
используется буфер на **256 слов**.

Если из этой четвёрки читается только `177220`, значит не вставлена карта с
годным набором образов [AZ-Форум].

Загрузчики: команда пультового режима `177226G` запускает ОС с диска `AZ0`;
альтернативный вход — `177224G`.

### Блок команд

Команды восьмеричные; ниже — сводка по [AZ-Форум]. Каждая по завершении
устанавливает разряд готовности.

| Код | Действие |
| :-: | --- |
| `021` | считать блок энергонезависимой памяти в буфер |
| `022` | отдать содержимое буфера на магистраль |
| `023` | принять с магистрали данные в буфер |
| `024` | записать буфер в энергонезависимую память |
| `025` | инициализация таблицы рекордов (Hall of Fame) |
| `026` | обмен с таблицей рекордов |
| `027` | версия прошивки STM32 |
| `031` | получить время из часов микроконтроллера в буфер |
| `032` | выдать время из буфера на магистраль |
| `033` | записать дату и время в буфер (ручная установка часов) |
| `034` | установить автономные часы по данным из буфера |
| `035` | запросить время с NTP-сервера и установить часы |
| `036` | установить часы по времени из стека TCP/IP |
| `037` | перезапуск AZБК и всей БК |
| `040` | получить IP-адреса в буфер |
| `041` | прочитать буфер IP-адресов (8 слов) |
| `042` | получить время из часов стека TCP/IP |
| `043` | чтение MAC-адреса в буфер |
| `044` | сохранить снимок экрана в файл |
| `047` | загрузить файл прямо в память AZБК |
| `050` | задать имя файла для чтения |
| `051` | размер открытого файла либо статус открытия |
| `052` | читать блок файла в буфер |
| `053` | задать имя файла для записи |
| `054` | задать длину создаваемого файла |
| `055` | записать в файл данные из буфера |
| `056` | получить сведения о размере карты в буфер |
| `057` | прочитать буфер размера карты (2 слова) |

Отдельная группа команд (`003`, `004`, `011`, `014`) обслуживает монтирование
дисков, в том числе сетевых. Для работы с ними выпущены утилиты `AZSMNT`
(просмотр назначений дисков), `AZDIR`, `AZMNT`, `AZUMNT`; исходные тексты
драйверов для RT-11 открыты [AZ].

---

## 19.10. ПЗУ, исполняемые самой БК

**[✓ ПО]** В комплект прошивки входят программы для К1801ВМ1, которые контроллер
подставляет машине как ПЗУ:

| Файл | Размер (v19) | Что это |
| --- | ---: | --- |
| `azboot.ROM` | 3500 | стартовое ПЗУ: определяет тип машины и частоту, заполняет разряды `177346`, ведёт загрузку |
| `AZLIB00.ROM` | 4046 | библиотека процедур; начинается с пролога сохранения `R0`–`R5` |
| `AZLIB01.ROM` | 2098 | то же, второй том |
| `AZLIB02.ROM` | 3284 | библиотека, активно работающая с `177716`, таймером и палитрой |
| `AZLIB03.ROM` | 1542 | небольшой модуль-копировщик |
| `SETUP.ROM` | 17550 | **AZBK BIOS Setup Utility v1.05** (§19.11) |

Между версиями 00018 (март 2025) и 00019 (май 2025) изменились только
`azboot.ROM` и `AZLIB03.ROM`; `AZLIB00`–`AZLIB02` и `SETUP.ROM` совпадают
побайтно.

### Слоты ПЗУ

Образы ПЗУ перечисляются в `AZ.INI` под номерами `R00`, `R01`, … Наблюдаемое
распределение [AZ-Док]:

| Слот | Файл | | Слот | Файл |
| :-: | --- | --- | :-: | --- |
| `R00` | `AZBOOT.ROM` | | `R28`, `R30`, `R32` | `10_017`, `10_018`, `10_019` |
| `R01`…`R04` | `AZLIB00`…`AZLIB03` | | `R34`, `R36`, `R38` | `10_106`, `10_107`, `10_108` |
| `R08` | `AZ337.rom` | | `R56` | `SETUP.rom` |
| `R16`…`R26` | `11m_324`, `325`, `327`, `328`, `329`, `330` | | | |

Восьмикилобайтные ПЗУ занимают слоты через один, четырёхкилобайтные — подряд.
Отсюда видно, что **слот — это страница в 4 Кбайт**, то есть та же дискретность,
что у маппера; 64 слота по 4 Кбайт как раз дают 256 Кбайт области ПЗУ из §19.2.

---

## 19.11. BIOS Setup

**[✓ ПО]** `SETUP.ROM` — полноценная программа настройки в стиле BIOS
персонального компьютера, с заголовком **«AZBK BIOS Setup Utility v1.05»** и
шестью вкладками. Ниже — полный состав пунктов, восстановленный из строк ПЗУ.

**Main:** системное время, дата, часовой пояс (`UTC±ЧЧ:ММ`), «Set Time from
NTP»; `Legacy Diskette A:` (например `1.44 3.5"`) и `B:`; `IDE Master` и
`IDE Slave` (образы `.hdi`); справочные поля — `Motherboard` (`0011M`),
`CPU Clock` (`4MHz`), `System Memory` (`128KB`), `Extended Memory` (`32768KB`),
`AZBK Hardware Rev`, `BIOS build`, `Firmware HW`, `Firmware SW`.

**Advanced** — переключатели возможностей:
`BK-0011M Emulation`, `SMK-512 Emulation`, `ROM 11M Emulation`, `Fast RAM`,
`V-Sync Timer`, `Ext. PS/2 keyboard`, `Legacy Speaker`, `AY Emulation`,
`Legacy Covox`. Возможные значения — `Disabled`, `Enabled`, `Hardware`,
`SD:image`; для таймера — `48Hz`, `50Hz`, `60Hz`.

**Network:** `DHCP`, `IP Address`, `Netmask`, `Default Gateway`, `DNS1`, `DNS2`,
`NTP Source`, `MAC Address`, `Hostname`.

**Boot:** `FastBoot` и восемь позиций `1st`…`8th Boot Device`.

**ROM:** список слотов `R00`… с назначенными файлами.

**Exit:** `Save and Exit`, `Discard and Exit`, `Reset Main Config`,
`Reset Net Config`, с подтверждениями («Save configuration and exit?»,
«Load default settings?», «Reset net config?», «Quit without saving?»).

Управление: **АР2+1** — помощь, **АР2+9** — установки по умолчанию,
**АР2+0** — сохранить и выйти.

Пояснения разработчика [AZ-Форум]: для дискового Бейсика БК-0011М нужно включить
`ROM 11M emulation` — эта же опция позволяет работать на БК-0011М вообще без
каких-либо ПЗУ; `V-Sync Timer` — это расширенный таймер через `177346`;
на БК-0010 в зависимости от доработок доступны `FastRAM` (режим БК-0010 с
быстрой нижней памятью), `BK11 emulation` и внешняя клавиатура PS/2.

---

## 19.12. Файл конфигурации `AZ.INI`

Конфигурация лежит на карте в корне, в файле `0:/AZ.INI` **[✓ ПО]**. Разделы:
`[ROM]`, `[DISKS]`, `[BOOT]`, `[ALTBOOT]`, `[NETWORK]`, `[LOGO]`.

* `[ROM]` — строки `Rnn=путь` (§19.10);
* `[DISKS]` — `Dn=путь`; номер `n` соответствует логическому диску `AZn`;
* `[BOOT]` — диск, с которого грузиться по умолчанию;
* `[NETWORK]` — `DHCP`, `TIMEZONE` (в минутах), `MAC`, `DHCPNAME`, `IP`, `MASK`,
  `GW`, `NTP`, `DNS1`, `DNS2`;
* `[LOGO]` — файл заставки (`.RAW`).

Пути с префиксом `0:` — локальные, на карте. Начиная с версии 17 поддерживаются
**сетевые пространства имён** [AZ-Форум]: `N:` — образы из сетевого архива
`mirrors.pdp-11.ru`, `R:` — репозиторий избранного ПО. Оба монтируются только на
чтение; максимальная длина пути — 384 символа; регистр букв имеет значение,
поддерживаются русские имена в КОИ-7 и КОИ-8. Пример:
`D4=N:/_bk0010-11-11m/soft/…/OVERKIL.IMG`.

**[✓ ПО]** Разбор `AZ.INI` в прошивке снабжён внятными сообщениями об ошибках;
их полный перечень:

| Код | Сообщение |
| :-: | --- |
| `#1` | физическая ошибка при чтении блока INI-файла |
| `#2` | слишком длинная строка в `AZ.INI` |
| `#3` | диск 0 не смонтирован |
| `#4` | `AZ.INI` не найден |
| `#5` | ошибка в логике программы |
| `#6` | ошибка позиционирования при чтении |
| `#7` | физическая ошибка при чтении блока данных |
| `#8` | неверная последовательность команд при записи |
| `#9` | ошибка позиционирования при записи |
| `#10` | физическая ошибка при записи |
| `#11` | ошибка сброса буферов при записи |
| `#12` | неверная секция в INI-файле |
| `#13` | неверная строка: в секции дисков не начинается с `Dn=`, либо в секции загрузки не `Dn` |
| `#14` | слишком длинная строка в секции дисков |
| `#15` | файл образа не найден или не открывается |

---

## 19.13. Загрузка и горячие клавиши

После включения на экране появляется приглашение `Press KT to run Setup`, и
доступны горячие клавиши [AZ-Форум]:

| Клавиша | Действие |
| :-: | --- |
| **КТ** | запуск Setup (не мгновенно) |
| **Пробел** | выход в монитор — БК-0010 или БК-0011М, смотря по машине и режиму |
| **0**…**9** | загрузка с выбранного диска |

Если номер загрузочного диска не задан, выводится
`AZLDR-W-NO BOOT DISK NUMBER SPECIFIED` и запрос
`ENTER THE BOOT DISK NUMBER [0..7]` **[✓ ПО]**. При остановленном процессоре БК
контроллер показывает логотип, а при невозможности старта — сообщение об ошибке
с подсказками [AZ-Форум].

---

## 19.14. Сетевые функции

**[✓ ПО]** В прошивке микроконтроллера реализованы:

* **WebDAV-сервер** — доступ к карте по сети (ответы `207 Multi-Status`,
  свойства `D:prop`), с корректными датами создания файлов, в том числе внутри
  образов RT-11, с учётом часового пояса;
* **HTTP-команды** — `/CMD.REBOOT` (перезапуск AZБК) и `/CMD.SCREENSHOT`
  (снимок экрана). Снимки сохраняются на карту как `0:/SCREENS/ччммссNN.SCR`;
  распаковываются онлайн на сайте разработчика. Командный режим работает только
  при включённой сети;
* **Landisk** — фирменная технология сетевых дисков (`landisk.maxiol.com`,
  `landisk2.maxiol.com`);
* **Hall of Fame** — сетевая таблица рекордов (`back.hof.maxiol.com`,
  `back2.hof.maxiol.com`); обмен шифруется AES-256 и кодируется base64;
* **NTP** — получение точного времени;
* поддержка файловых систем RT-11/F11 при работе с образами (`.DSK.F11`).

Сетевой стек опознаётся строкой
`Hardware: AMIKO-1 v0.07 Software: GADO-5 v1.00 TCP/IP: v2.00.0` **[✓ ПО]**.

---

## 19.15. Прошивки, версии и границы разбора

Прошивка поставляется комплектом: `AZBK_000NN.hex` (микроконтроллер),
`AZBK_000NN.pof`/`.sof` (ПЛИС) и набор ПЗУ для БК. Часть обновлений затрагивает
только микроконтроллер, часть требует и перепрошивки ПЛИС [AZ-Форум]. В архиве
имеются версии **00018** и **00019**; лежащие рядом файлы `AZBK_2 (2).pof/.sof`
не совпадают ни с одной из них — это отдельная сборка **[✓ ПО]**.

Чего установить не удалось:

* **содержимого прошивки ПЛИС.** `.sof`/`.pof` — упакованный поток конфигурации
  Cyclone II; из него достоверно читается только заголовок (тип кристалла и
  версия Quartus). Всё, что сказано выше о поведении ПЛИС, взято из описаний
  разработчика, а не из бинарного файла;
* **точной модели STM32** — по образу видны лишь адреса и размер;
* **полного списка команд AZ**: коды `001`–`020` (дисковые операции) описаны в
  более ранней теме форума, ссылка на которую в разобранных материалах не
  раскрывается; здесь приведены только те, что удалось прочитать дословно;
* **разрядной раскладки регистров `177230`, `177160` и `177212`** сверх
  приведённой — источник описывает их выборочно.

**Расхождение источников.** Максимальное число команд в пакете блиттера: сайт
[AZ] говорит «до 511», техническое описание [AZ-Форум] — «макс количество
команд — 128». Приоритет отдан техническому описанию как более подробному, но
вопрос остаётся открытым (§16.5, группа Т).

**Важная оговорка.** Адреса `177200`–`177206` в старой документации на
предшественника — контроллер псевдодиска на MicroSD — описаны как
**альтернативный набор адресов самого контроллера**, включаемый DIP-переключателем
№1 (основной набор — `177220`–`177226`). В AZБК эти же адреса заняты регистрами
звука (§19.7). При чтении старых материалов это легко перепутать.
