# 2. Архитектура БК-0010-01

> **Источники главы**
> * **[РСП § 2, 3, 4]** «Состав микро-ЭВМ», «Архитектура микро-ЭВМ», «Распределение
>   адресного пространства» (с. 6–13) — нормативная карта памяти, состав ПЗУ, границы стека.
> * **[З]** «Архитектура БК-0010», «Адресное пространство БК-0010», «Центральный процессор»,
>   «Временные характеристики команд БК-0010» — режим РП, ССП, тайминги.
> * **[МПСС]** (вторичный) — типы БИС, конструктив.
> * **[ВМ1]** «Однокристальный микропроцессор К1801ВМ1» § 3 — характеристики кристалла.
>   Подробно процессор описан в отдельной главе
>   [03-процессор-К1801ВМ1.md](03-процессор-К1801ВМ1.md).
> * **[ЭМ]** `bkemu-QT/devemu/Config.h` — тактовая частота.

## 2.0. Модельный ряд и комплект поставки

**[РСП § 2]** описывает три машины одного семейства:

| Модель | Отличия |
| --- | --- |
| **БК-0010** | Исходная модель. Клавиатура кнопочная, регистровая клавиша «НР» |
| **БК-0010-01** | Новая бездребезговая клавиатура («АР2» вместо «НР»), переработанная плата, встроенное ПЗУ Бейсика |
| **БК-0010Ш** | Школьный вариант. **Содержит устройство последовательного обмена — блок ИРПС, работающий по протоколу ИРПС на скорости 9600 бод** (регистры 176560…176566, векторы 360/364) |

В состав БК-0010(-01) входят:

* информационно-вычислительное устройство;
* **блок МСТД**;
* блок питания;
* кабели связи с внешними устройствами.

В качестве устройства отображения используется бытовой ТВ-приёмник, в качестве внешнего
запоминающего устройства — кассетный монофонический магнитофон типа «Электроника 302»
с компакт-кассетами **МК-60**.

> **Замечание [РСП § 1].** Функции работы с последовательным каналом (§ 5.2.4 и гл. 10
> руководства) и **работа с цветным изображением** (§ 8.3.2) «поддержаны математическим
> обеспечением, однако возможность их использования решается самим пользователем путём
> **доработки Микро-ЭВМ**». То есть цветной вывод с заводской точки зрения — такая же
> опция, требующая согласующего устройства, как и ТЛГ-канал.

## 2.1. Состав машины

БК-0010-01 конструктивно состоит из двух частей: системного блока, совмещённого с
клавиатурой, и внешнего блока питания. В качестве дисплея используется бытовой телевизор
или видеомонитор, в качестве внешней памяти — бытовой магнитофон.

Внутренние устройства:

* **центральный процессор (ЦП)** К1801ВМ1 — однокристальный микропроцессор с системой
  команд PDP-11;
* **ОЗУ** — 32 Кбайт на 16 БИС КР565РУ6Г (динамическое, организация 1 × 16 Кбит);
* **ПЗУ** — **всего 32 Кбайт** на 4 БИС на плате машины (масочное, по 8 Кбайт = 4 Кслова).
  Состав по [РСП § 2]: **системное ПЗУ** с управляющими программами (МДС) — **8 Кбайт**;
  компилятор **Бейсика — 24 Кбайт в трёх микросхемах съёмного ПЗУ**. Интерпретатор
  **Фокала (8 Кбайт) и тест-ПЗУ (8 Кбайт) на плате не стоят** — они в **блоке МСТД**
  и перекрывают окна Бейсика, поэтому Фокал и требует подключённого МСТД
  (адресные коды микросхем — в [13-схемотехника-и-доработки.md](13-схемотехника-и-доработки.md), §13.1б).
  ⚠ Не путать: **32 Кбайт — это весь объём ПЗУ машины, а не системное ПЗУ**;
  системного — 8 Кбайт. Из отведённых Бейсику 24 Кбайт код занимает
  `120000`…`177527`, дальше до начала регистров (`177600`) остаётся незанятый
  хвост; устройство этих трёх микросхем разобрано в [16-бейсик.md](16-бейсик.md);
* **контроллер ОЗУ** — наряду с управлением и регенерацией динамического ОЗУ выполняет
  функции контроллера бытового телевизора. Независимая схема регенерации читает слова
  из ОЗУ и выдаёт их на экран; арбитр синхронизирует регенерацию и обращения процессора
  к ОЗУ;
* **контроллер дисплея** — БИС **К1801ВП1-037**;
* **контроллер клавиатуры** — БИС **К1801ВП1-014**, формирует коды КОИ-7 и параллельно
  вводит их в системную магистраль. Клавиатура — печатная плата с 92 переключателями
  ПКН-150;
* **контроллер кассетного накопителя** — программно-аппаратный: аппаратная часть состоит
  из регистра временного хранения и схемы преобразователей уровня сигналов; всё остальное
  реализовано программно;
* **системные регистры** — интерфейс к клавиатуре, магнитофону, звуку, ТЛГ-каналу,
  порту УВВ и таймеру.

Конструктивно информационно-вычислительное устройство смонтировано на **двух печатных
платах** — вычислителя и клавиатуры; габариты корпуса 370 × 180 × 70 мм. В отсеке
пользователя ПЗУ установлены в **двух розетках типа РС-24**, что позволяет заменять их
для ориентации микро-ЭВМ на специальные задачи.

> **Тип БИС ПЗУ.** Статья разработчиков 1985 г. называет **К1801РЕ1**; обе популярные книги
> (для более поздних БК-0010-01) — **К1801РЕ2 / КР1801РЕ2**. Речь о разных ревизиях машины.
> Существуют ППЗУ-аналоги (К573РФ, КМ1801РР1), устанавливаемые взамен масочных.

Машина построена по схеме **общей шины адрес-данные** (конфигурация DEC, «Q-Bus»,
в отечественной терминологии — МПИ, магистральный параллельный интерфейс). Все
устройства — процессор, память, клавиатура — соединены одними и теми же линиями,
по которым передаются и адрес, и данные. Обмен асинхронный, по принципу «запрос-ответ»
(сигналы СИА/SYNC и СИП/RPLY).

Отличия БК-0010-01 от исходного БК-0010:

* полностью переработанная печатная плата и новая бездребезговая клавиатура;
* регистровая клавиша **«АР2»** вместо «НР» (функция та же);
* встроенное ПЗУ Бейсика (три БИС) — на БК-0010 в этих адресах мог быть Фокал;
* контакт **А14 (БЛОК)** разъёма МПИ, отключающий ПЗУ Бейсика при подключении блока МСТД
  или контроллера НГМД;
* **монитор-драйверная система (МДС) не менялась с 1985 г.** — все модели БК-0010(.01)
  полностью программно совместимы между собой. (В отличие от БК-0011 → БК-0011М, где
  МДС была изменена и совместимость потеряна.)

## 2.2. Представление данных

* **Бит** — минимальная единица; **байт** — 8 бит (0…377); **слово** — 16 бит (2 байта).
* Байты адресуются подряд; **чётный байт — младший**, нечётный — старший байт слова.
  Адрес слова совпадает с адресом его младшего (чётного) байта.
* Обращение к слову требует чётного адреса. Нечётный адрес команды или операнда-слова
  вызывает прерывание по вектору 4 («зависание»).
* Разряды нумеруются справа налево десятичными числами: 00 — младший, 15 — старший.
* Отрицательные числа представляются в **дополнительном коде**; знаковый разряд —
  старший (15 для слова, 07 для байта).
* 1 Кбайт = 1024д байт.

Все системные данные и вся документация по БК традиционно записываются в **восьмеричной**
системе: перевод в двоичную выполняется потриадно, что удобно при работе с масками разрядов.

## 2.3. Центральный процессор

> Здесь — только то, что нужно программисту БК. Устройство самого кристалла
> (характеристики, варианты А/Б/В/Г, периферийный блок, встроенный таймер,
> обработка прерываний, аппаратные ошибки) — в отдельной главе
> [03-процессор-К1801ВМ1.md](03-процессор-К1801ВМ1.md).

### Регистры общего назначения (РОН)

Восемь 16-разрядных регистров, **не входящих в адресное пространство**:

| Регистр | Назначение |
| --- | --- |
| R0…R5 | Универсальные |
| R6 = **SP** | Stack Pointer — указатель вершины стека. Автоинкремент/автодекремент всегда на 2, независимо от байтовой или словной операции |
| R7 = **PC** | Program Counter — счётчик команд |

Операции в регистрах выполняются существенно быстрее, чем в памяти, и ряд команд
(SOB, XOR, JSR, RTS, MARK) может работать только через регистры.

### Слово состояния процессора (ССП, PSW, PS)

В БК-0010 монитор использует **только младший байт** PS:

| Разряд | Обозн. | Назначение |
| --- | --- | --- |
| 00 | **C** | Перенос (Carry) — за пределы слова/байта |
| 01 | **V** | Переполнение (oVerflow) — перенос в знаковый разряд |
| 02 | **Z** | Ноль (Zero) — результат равен нулю |
| 03 | **N** | Отрицательность (Negative) — результат отрицателен |
| 04 | **T** | Ловушечный разряд. При T=1 после каждой команды возникает прерывание по вектору 14 (пошаговый режим) |
| 05, 06 | — | **Резервные.** Читаются и записываются, но **ни на что не влияют** [ВМ1] |
| 07 | **I** | **Маска прерываний** — единственный разряд приоритета |

Записать «1» в T-разряд командой `MTPS` **нельзя** — процессор игнорирует эту попытку.
Единственный способ — подменить сохранённое в стеке ССП перед возвратом из прерывания
(см. [06-прерывания.md](06-прерывания.md), пример отладчика).

> ⚠ **«Приоритет 0…7» — фикция, унаследованная от PDP-11.** В полном PDP-11 приоритет
> задают три разряда 07…05. В **К1801ВМ1 приоритет однобитный**: маскирует только
> разряд **07** (`I`), а разряды 06 и 05 — резервные и на работу процессора **не влияют**
> [ВМ1]. Привычное `MTPS #340` работает, но реально значим в нём лишь разряд 07;
> `MTPS #200` даёт тот же результат.

Значения PS для типовых приоритетов (остальные разряды нулевые):
приоритет 0 → `000000`, приоритет 4 → `000200`, приоритет 7 (максимальный) → `000340`.
Все значения с установленным разрядом 07 эквивалентны.

### Полная карта разрядов PSW [ВМ1]

Монитор БК пользуется только младшим байтом, но у процессора значим и старший.
Полное описание — в [03-процессор-К1801ВМ1.md](03-процессор-К1801ВМ1.md), §3.5;
сводка:

| Разряд | Имя | Назначение |
| :-: | --- | --- |
| 00…04 | C, V, Z, N, T | Как выше |
| 05, 06 | — | Резервные, влияния не оказывают |
| 07 | **I** | Маскирует прерывания от ВЕ-таймера (только ВМ1Г), `nIRQ2`, `nIRQ3`, векторное `nVIRQ` |
| **08** | **PA0** | **Младший разряд номера процессора**, инверсия входа `nPA0`. **Только чтение**, при записи не меняется |
| **09** | **PA1** | Старший разряд номера процессора, инверсия `nPA1`. Только чтение |
| 10 | PSW10 | Зависание шины обрабатывается в пультовом режиме по вектору `160002`; дополнительно маскирует пропадание питания, `nIRQ1`, таймер, `nIRQ2`, `nIRQ3`, `nVIRQ` |
| 11 | PSW11 | То же для зависания шины; запрещает пультовое прерывание `nIRQ1` |
| 12…15 | — | Резервные, влияния не оказывают |

Разряды 10 и 11 **всегда сбрасываются** при входе в обработчик пользовательского
прерывания и при выполнении `RTI`/`RTT` — значение из стека или вектора игнорируется.
Установить их можно только при входе в пультовый режим (например, командой `HALT`)
и при возврате командами `STEP`/`START`. **`MTPS` не изменяет старший байт PSW**,
а также разряд T.

При аппаратном сбросе PSW обнуляется, но микропрограмма начального старта
записывает в него **`000340`**.

Клавиша **«СТОП»** — внеприоритетное устройство: её прерывание нельзя запретить
установкой разрядов приоритета.

### Что процессор БК не умеет

Аппаратно **не реализованы** команды расширенной арифметики (EIS: MUL, DIV, ASH, ASHC —
кроме `MUL` в варианте К1801ВМ1Г, см. [03-процессор-К1801ВМ1.md](03-процессор-К1801ВМ1.md), §3.2)
и плавающей арифметики (FIS). Их коды вызывают прерывание по вектору 10 (резервный код),
чем и пользуются программные эмуляторы — например, интерпретатор Фокала обрабатывает
через вектор 10 свои псевдокоманды с кодами 7000…7377.

Кроме того, часть кодов, отсутствующих в БК, даёт не «НК» (неправильная команда, вектор 10),
а «ЗВ» (зависание, вектор 4) — например, код `12`, по которому процессор пытается
обратиться к несуществующему системному регистру. Пример действительно несуществующей
команды — код `30`.

## 2.4. Адресное пространство

Полное адресное пространство — 65536д байт (0…177777).

### Обычный режим

```
000000 ┬──────────────────────────────────────────┐
       │ Системная область: векторы прерываний,   │  0,5 К
       │ системные переменные монитора, стек      │
001000 ┼──────────────────────────────────────────┤
       │ ОЗУ пользователя                         │ 15,5 К
040000 ┼──────────────────────────────────────────┤
       │ Экранное ОЗУ (видеопамять)               │   16 К
100000 ┼──────────────────────────────────────────┤
       │ ПЗУ: монитор-драйверная система (МДС)    │    8 К
120000 ┼──────────────────────────────────────────┤
       │ ПЗУ: Бейсик-1 (или Фокал при МСТД)       │    8 К
140000 ┼──────────────────────────────────────────┤
       │ ПЗУ: Бейсик-2 (или резерв)               │    8 К
160000 ┼──────────────────────────────────────────┤
       │ ПЗУ: Бейсик-3 (или МСТД — тесты и МСД)   │ 7,875 К
177600 ┼──────────────────────────────────────────┤
       │ Область системных регистров (СР)         │ 0,125 К
177777 ┴──────────────────────────────────────────┘
```

### Режим расширенной памяти (РП)

Включается клавишами **АР2 + СБР** (код 214). Экран сжимается до 4 строк текста,
освобождая 12 К под программу:

```
000000 … 000777   системная область и стек          0,5 К
001000 … 067777   ОЗУ пользователя                 27,5 К
070000 … 077777   экранное ОЗУ                        4 К
100000 …          далее — как в обычном режиме
```

Особенности перехода:

* при переходе **в** РП часть экранного ОЗУ, отходящая пользователю, **сохраняет** прежнее
  содержимое (просто перестаёт отображаться);
* при обратном переходе экран очищается и информация по адресам 40000…67777 **теряется**;
* через служебные ячейки системной области распределение памяти между ОЗУ пользователя и
  экраном можно менять произвольно, но «отрезанная» область будет отображаться на экране
  как «пестрота» (приём использован, например, в программе VorteX!).

### Замечания по ПЗУ

* Одна БИС ПЗУ содержит 20000 байт (8 К). Все области ПЗУ, кроме последней,
  имеют такую длину; последняя — 17600 байт (7,875 К), так как заканчивается на 177577.
* **Важно при подготовке ПЗУ для адресов 160000:** область системных регистров
  177600…177777 попадает в адресное пространство четвёртой микросхемы ПЗУ и **блокирует
  соответствующие 128 байт** — использовать их нельзя [РСП § 4].
* Подключение микросхем ПЗУ производится через колодки, размещённые на плате микро-ЭВМ;
  адреса 120000…157777 занимают **съёмные** ПЗУ с управляющей системой (при отключённом
  блоке МСТД — Бейсик, при подключённом — Фокал и тест-мониторная система).
* ПЗУ — **масочные**: информация записывается при изготовлении кристалла и не может быть
  изменена ни программно, ни программатором. Существуют ППЗУ-аналоги (например, серии
  К573РФ, КМ1801РР1), которые могут быть установлены взамен масочных.
* Запись по адресам ПЗУ запрещена аппаратно и приводит к прерыванию по вектору 4.
* Фокал и Бейсик не могут работать одновременно, как и Бейсик и МСТД, — они занимают
  одни и те же адреса. Блок МСТД подключается к разъёму МПИ и сигналом БЛОК (контакт А14)
  отключает встроенное ПЗУ Бейсика.

**Подключение и отключение блока МСТД производится только при выключенном питании.**

## 2.5. Временные характеристики команд

Основное время команды (все безоперандные и все команды при способе адресации `Rn`,
код 0) — **4,0 мкс**. При 3 МГц это 12 тактов.

Время исполнения команды с байтом и со словом **одинаково**.

### Таблица 1. Добавочное время адресации операнда, мкс

Обозначения: `A`/`A1` — источник, `AB` — источник-приёмник однооперандной команды,
`B` — приёмник, `A2`/`Nj` — неизменяемый приёмник и адрес перехода JMP,
`Ns` — адрес перехода JSR.

| Способ адресации | Код | A, A1 | AB | B | A2, Nj | Ns |
| --- | --- | --- | --- | --- | --- | --- |
| `Rn` | 0 | 0 | 0 | 0 | 0 | — |
| `@Rn`, `(Rn)+`, `-(Rn)`, `#X` | 1, 2, 4 | 4,0 | 5,4 | 6,8 | 6,8 | 10,8 |
| `@(Rn)+`, `@-(Rn)`, `X(Rn)`, `@#X`, `MET` | 3, 5, 6 | 6,8 | 8,0 | 10,8 | 9,4 | 13,4 |
| `@X(Rn)`, `@MET` | 7 | 9,4 | 10,8 | 13,4 | 12,0 | 16,0 |

Правила для двухоперандных команд:

* для операнда-источника время берётся из таблицы как есть (тип `A` или `A1`);
* для операнда-приёмника тип `B` берётся **только если источник — `Rn`**, иначе тип `AB`;
* для `CMP` и `BIT` (не меняющих операнды): если источник `Rn` — приёмник типа `A2`,
  иначе типа `A1`;
* для `XOR` второй операнд — типа `A2`.

Практический вывод: **«медленный» операнд выгоднее ставить первым.**
`CMP @MET,Rn` = 13,4 мкс, `CMP Rn,@MET` = 16,0 мкс.

### Таблица 2. Время особых операторов

| Оператор | Время |
| --- | --- |
| `JMP N` | по табл. 1, столбец `Nj` |
| `JSR Rn,N` (`CALL N`) | по табл. 1, столбец `Ns` |
| `RTS Rn` (`RET`) | 10,8 мкс |
| `BR`, `BEQ` и все ветвления (независимо от того, выполнен ли переход) | 5,4 мкс |
| `SOB Rn,MET` (независимо от ветвления) | 6,8 мкс |
| `EMT`, `TRAP`, `BPT`, `IOT` | 22,8 мкс |
| `HALT` | 48,0 мкс |
| `RTI`, `RTT` | 13,4 мкс |
| `RESET` | ≈380 мкс |
| Прерывание от внешнего устройства | ≈20 мкс + время завершения текущей команды |

Обращение к подпрограмме заметно быстрее командного прерывания:
`JSR` + `RTS` даже в худшем случае — 26,8 мкс против 36,2 мкс у `EMT` + `RTI`,
не считая работы EMT-диспетчера.

### Таблица 3. Время исполнения EMT-функций (приближённо)

| Прерывание | Время |
| --- | --- |
| `EMT 4` | 0,2 мс |
| `EMT 14` | **240 мс** |
| `EMT 16` | 0,4 мс (R0 = 0) … 1,7 мс (вывод символа); с погашенным курсором — 1,1 мс; режим «ИНВ.С» +100 мкс; «ПОДЧ.» +20 мкс; в режиме «32 символа» всё примерно вдвое больше; сброс экрана — 200 мс; перевод строки — 1,8 мс (с рулонным сдвигом — 18 мс) |
| `EMT 20` | (1,6 × N) + 0,5 мс, N — число символов; поправки на спецрежимы как у EMT 16 |
| `EMT 22` | 1,1 мс (вывод символа) … 34 мс (сброс служебной строки) |
| `EMT 24` | 1,8 мс (символьный режим) / 2,4 мс (графический) |
| `EMT 26` | 0,25 мс / 0,27 мс |
| `EMT 30` | 0,5 мс независимо от кода операции |
| `EMT 32` | (0,5 × N) + 0,5 мс, N — число точек вектора; быстрее всего горизонтальные и вертикальные векторы |

Практическое следствие: если выводится 4 и более символов, выгоднее погасить курсор
перед выводом и восстановить после (уравнение 1,7·N = 2·1,1 + 1,1·N даёт N ≈ 3,67).

## 2.6. Расхождение по быстродействию в источниках

* Ю. А. Зальцман: «реальное быстродействие примерно **250 тыс. операций в секунду**»,
  основное время команды 4,0 мкс.
* «Программирование на БК-0010-01» (Чебоксары, 1993): «для самых простых операций
  быстродействие БК составляет около **400 тысяч операций в секунду**».

Оценка Зальцмана согласуется с приведённым им же основным временем команды
(1 / 4,0 мкс = 250 000) и с тактовой частотой 3 МГц, принятой в эмуляторе
(`devemu/Config.h`: `CPU_SPEED_BK10 = 3000000`). Оценка в 400 тыс. оп./с получается,
если считать команду за 7–8 тактов, что для ВМ1 нереалистично. **Следует пользоваться
цифрой 250 тыс. оп./с.**
