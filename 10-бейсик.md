# 10. Бейсик-Вильнюс: устройство ПЗУ

> **Источники главы**
> * **[БейсикИсх]** — **исходный текст ПЗУ Бейсика на языке МАКРО-11**, ВЦКП ВГУ
>   (Вычислительный центр коллективного пользования Вильнюсского госуниверситета),
>   1985; пакет графических программ — ИПИ АН СССР совместно с ВЦКП ВГУ.
>   Семь файлов, ≈12 000 строк → `Бейсик-Вильнюс/BASIC - VVU - 1985/`; та же редакция
>   с Makefile и полным листингом — `github.com/andpp/BK0010_Basic_Rom_Src`.
>   **Определяющий источник главы.**
> * **[ПЗУ]** — образы `bkemu/ROM/bk10_106_basic1.rom`, `bk10_107_basic2.rom`,
>   `bk10_108_basic3.rom` (БК-0010-01), `bk11_198…200` (БК-0011),
>   `bk11m_327…329` (БК-0011М). **Арбитр:** каждое утверждение главы,
>   помеченное **[✓ ПЗУ]**, сверено с содержимым реальной микросхемы.

Глава описывает **не язык, а его реализацию**: что именно лежит в трёх съёмных БИС
ПЗУ, как Бейсик размещается в ОЗУ, какими системными ячейками и функциями `EMT` он
пользуется и как из него дотянуться до программ в кодах. Описание самого языка —
предмет заводского руководства по Бейсику и в справочник не входит.

Материал важен и за пределами Бейсика. Из штатного ПО БК исходный текст сохранился
**только у Бейсика**: монитор и Фокал известны лишь по дизассемблерам. Поэтому
[БейсикИсх] — единственный случай, когда видно не «что делает система», а **как ею
пользовались**: какие ячейки считались общедоступными, какие функции `EMT` — рабочими,
а какие приёмы применялись, чтобы уместить 24 Кбайт кода в три микросхемы. Это делает
его независимой проверкой глав 6, 8, 9 и 11.

## 10.1. Что это такое

| Параметр | Значение |
| --- | --- |
| Полное имя в исходниках | `BASIC - VVU` (Vilniaus Valstybinis Universitetas) |
| Разработчик | **ВЦКП ВГУ, Вильнюс**; графический пакет — **ИПИ АН СССР** совместно с ВЦКП ВГУ |
| Год в заголовке исходников | 1985 |
| Дата сборки в ПЗУ | **1986.07.24** — выводится в начальном сообщении [✓ ПЗУ] |
| Микросхемы | 106, 107, 108 (три БИС по 8 К, последняя — 8064 байт) |
| Занимаемые адреса | `120000`…`177527`, дальше до `177577` — незанятый хвост, с `177600` начинаются регистры |
| Начальное сообщение | `БЕЙСИК     (Вильнюс,1986.07.24)`, по адресу `132666` [✓ ПЗУ] |
| Точка входа | `120000` |
| Вход монитора Бейсика | `133750` (метка `MONIT`) |
| Обработчик «СТОП» | **`120234`** [✓ ПЗУ] — см. §10.9 |

Ключевая особенность, определяющая всё устройство: **это компилятор, а не
интерпретатор**. Строка транслируется в объектный код в момент ввода, а `RUN`
исполняет уже готовый код. Отсюда и деление ОЗУ на область текста и область
объектного кода (§10.4), и то, что синтаксическая ошибка сообщается сразу при
наборе строки, а не при её исполнении.

> **Терминология.** В [РСП] и в главе 1 Бейсик назван «компилятором» — это не
> вольность формулировки, а точное описание: [БейсикИсх] делит свои модули на
> «программы компиляции» (`AOPSUB`) и «программы непосредственного выполнения»
> (`AOPMON`).

## 10.2. Одни исходники на пять машин

Первый же блок `BK.mac` — «блок настройки», задающий целевую машину:

```asm
        BK0010=0
        EL60=1          ; Электроника-60
        NC80=2
        SM4=3           ; СМ-4, ОС РАФОС
        MDS=4           ; MDS РАФОС
        RAM=10000       ; сборка для загрузки в ОЗУ
        ROM=120000      ; сборка для прошивки в ПЗУ
        NORM=0
        KLAS=1          ; вариант для класса

        MASINA=BK0010
        MEMTYP=ROM
        KONTYP=NORM
```

Условная трансляция по `MASINA` разводит работу с экраном, клавиатурой и внешней
памятью: у БК-0010 всё делается через `EMT` пускового монитора, у остальных машин —
через собственный драйвер клавиатуры с перехватом вектора `60` и через РАФОС.

Это **прямое подтверждение практики кросс-разработки**, описанной в
[05-система-команд.md](05-система-команд.md), §5.5а: программу для БК писали и
отлаживали на СМ-4 или «Электронике-60», а в БК она попадала уже в виде прошивки.
Здесь видно и обратное направление — один и тот же текст собирался и для БК, и для
старших машин.

Различие `MEMTYP` (`RAM` = `10000` против `ROM` = `120000`) объясняет, почему в
обращении встречаются «Бейсики, грузящиеся с ленты»: это тот же текст, собранный
с базой `10000`, у которого верхняя граница памяти берётся не `40000`, а сама
`MEMTYP`.

## 10.3. Самопроверка ПЗУ при запуске

Первое, что делает Бейсик по адресу `120000` [✓ ПЗУ]:

```asm
120000  012701  100000          START:  MOV     #100000,R1      ; начало проверки
120004  012703  000003                  MOV     #3,R3           ; три блока
120010  012702  010000          4$:     MOV     #10000,R2       ; 4096 слов = 8 Кбайт
120014  005000                          CLR     R0
120016  062100                  3$:     ADD     (R1)+,R0        ; сумма с циклическим
120020  005500                          ADC     R0              ;   переносом
120022  077203                          SOB     R2,3$
120024  020027  177777                  CMP     R0,#177777
120030  001401                          BEQ     2$
120032  000207                          RETURN                  ; сумма не сошлась
120034  077310                  2$:     SOB     R3,4$
```

Проверяются **три блока по 8 Кбайт начиная с `100000`**: `100000`…`117777`,
`120000`…`137777` и `140000`…`157777` — то есть системное ПЗУ МДС и две первые
микросхемы Бейсика. Ожидаемая сумма — `177777`.

Сумма посчитана по образам ПЗУ:

| Микросхема | Адреса | Циклическая сумма слов | Проверяется |
| --- | --- | --- | :-: |
| **017** (МДС) | `100000`…`117777` | `177777` ✓ | да |
| **106** (Бейсик-1) | `120000`…`137777` | `177777` ✓ | да |
| **107** (Бейсик-2) | `140000`…`157777` | `177777` ✓ | да |
| **108** (Бейсик-3) | `160000`…`177577` | не сходится | **нет** |
| 019 (МСТД) | — | `177777` ✓ | не участвует |
| 018 (Фокал) | — | не сходится | не участвует |

Видно, что **контрольная сумма — заводское соглашение**, а не выдумка авторов
Бейсика: ему подчиняются и монитор, и МСТД. Третья микросхема Бейсика из проверки
исключена по объективной причине: она короче остальных (8064 байта), её последние
128 байт — адреса `177600`…`177777`, занятые регистрами. Просуммировать её как
полный блок в 8 Кбайт невозможно — чтение упёрлось бы в регистры устройств.

**Что происходит при несовпадении.** Команда `RETURN` (`RTS PC`) выглядит странно
для точки начального пуска, но она уместна: монитор передаёт управление Бейсику
командой **`004737 120000` = `JSR PC,@#120000`** (адрес `100270`) [✓ ПЗУ], а не
`JMP`. Поэтому неисправное ПЗУ Бейсика не подвешивает машину — управление
возвращается в пусковой монитор, и на экране остаётся приглашение ПМ.
См. [09-монитор.md](09-монитор.md), §9.3.

## 10.4. Рабочая область в ОЗУ: адреса `2000`…`3051`

Бейсик размещает свои переменные сразу за системной областью
(см. [08-память-и-системная-область.md](08-память-и-системная-область.md)), начиная с адреса `2000`,
который одновременно служит вершиной его стека.

Таблица восстановлена по [БейсикИсх] и **сверена с реальным ПЗУ** — см.
предупреждение после неё.

| Адрес | Имя | Назначение |
| --- | --- | --- |
| `2000` | `STACK` / `LYGIS` | Вершина стека Бейсика; она же — режим работы системы (0 — монитор, ≠0 — исполнение) |
| `2002` | `TXEND` | Конец текста программы и начало объектного кода |
| `2004` | `STRREG` | Начало области строк |
| `2006` | `STRSIZ` | Размер области строк (по умолчанию `200`д байт) |
| `2010` | `FREBEG` | Начало свободной части области строк |
| `2012` | `FRELEN` | Длина свободной части области строк |
| `2014` | `ENDCOD` | Конец объектного кода, начало свободной области |
| `2016` | `CIKL` | Начало стека циклов `FOR`…`NEXT` |
| `2020` | `LIMIT` | Вершина стека подпрограмм `GOSUB` |
| `2022` | `LENT` | Начало таблицы имён |
| `2024` | `TABTOP` | Конец таблиц связи и имён |
| `2026` | `FCB` | Начало области файлов и таблицы связи |
| `2030` | `HIMEM` | Верхняя граница памяти, доступной Бейсику. При старте — **`40000`** |
| `2032` | `ERRS` | Признак запрета выполнения программы |
| `2034` | `TYPE` | Тип компилируемого выражения |
| `2036` | `DATBEG` | Начало буфера `DATA` |
| `2040` | `DATPTR` | Текущий указатель буфера `DATA` |
| `2042` | `DATINP` | Флаг `READ` / `INPUT` |
| `2044` | `NUMBER` | Номер компилируемой строки |
| `2046` | `ADRHLT` | Адрес останова программы (для `CONT`) |
| `2050` | `TRFLAG` | Флаг режима трассировки (`TRON` / `TROFF`) |
| `2052` | `AUTOON` | Флаг режима `AUTO` |
| `2054` | `AUINC` | Приращение номеров строк в режиме `AUTO` |
| `2056` | `CURLIN` | Текущая строка |
| `2060`…`2066` | `III` | Рабочие ячейки функции `RND` |
| `2070`…`2076` | `RNDSAV` | Текущее значение `RND` (4 слова — вещественное) |
| `2100`…`2123` | `USRTAB` | **Таблица адресов `USR0`…`USR9`** — 10 слов; см. §10.10 |
| `2124` | `PNTSAV` | Рабочая ячейка шага |
| `2126`, `2130` | `PFACT` | Указатель блока параметров для `FN` |
| `2132` | `TMP` | Указатель временных дескрипторов |
| `2134` | `SAVJMP` | Адрес исполняемого кода программы |
| `2136` | `LPTPOS` | Счётчик позиции головки принтера (байт) |
| `2140`…`2411` | `FNKSTR` | **Буфер текстов функциональных клавиш** — 10 записей по 17д байт; см. §10.11 |
| `2412` | `$SCRT` | Рабочая переменная оператора `DRAW` |
| `2414` | `WRITE` | Адрес драйвера устройства внешней памяти |
| `2416` | `IODEV` | Номер текущего файла |
| `2420` | `INK` | Буфер функции `INKEY$` |
| `2422`…`3021` | `BUF` | **Буфер вводимой строки**, `400` байт (256д) |
| `3022`…`3051` | `BUFOUT` | Буфер вывода, `30` байт (24д) |
| **`3052`** | `TEXT` | **Начало текста программы пользователя** |

> ⚠ **Расхождение восстановленного текста с ПЗУ.** Четыре ячейки —
> `WRITE`, `IODEV`, `INK` и `BUF` — в опубликованных исходниках описаны **не по тем
> адресам**: они помещены за буфер ввода (`3014`, `3016`, `3020`, `3022`), тогда как
> в реальном ПЗУ стоят перед ним (`2414`, `2416`, `2420`, `2422`). Кроме того,
> `BUF` там объединён с `BUFOUT` в одну ячейку, из-за чего буфер ввода на 256
> символов пришёлся бы на начало текста программы — рабочая машина так вести себя
> не может.
>
> В таблице выше даны **адреса из ПЗУ**. Они подтверждены двадцатью пятью
> обращениями в трёх микросхемах: например, `121066: 012701 002422` = `MOV #BUF,R1`
> и `120564`/`120572` — очистка области от `BUF` до `TEXT` при инициализации.
> Разбор расхождения — в [16-источники-и-соглашения.md](16-источники-и-соглашения.md),
> §16.5, К1.

**Практический смысл таблицы.** Всё это доступно из самой программы оператором
`PEEK` / `POKE`. Например, `PRINT PEEK(&O2030)` покажет верхнюю границу памяти,
`POKE &O2030,...` — сдвинет её (штатно это делает оператор `CLEAR`), а запись
адреса подпрограммы в `USRTAB` заменяет собой `DEF USR`.

## 10.5. Как Бейсик распределяет память

При старте `HIMEM` := `40000`, то есть Бейсику отводится **`2000`…`37777`, около
15 Кбайт**; всё, что выше, — экран (см. [11-экран-и-клавиатура.md](11-экран-и-клавиатура.md)).
Область между `TEXT` и `HIMEM` делится динамически:

```
   2000  ┌──────────────────────────────────┐
         │ рабочие переменные Бейсика       │  §10.4
         │ (стек растёт вниз от 2000)       │
   2140  │ тексты функциональных клавиш     │
   2422  │ буфер ввода строки (256 байт)    │
   3022  │ буфер вывода                     │
   3052  ├──────────────────────────────────┤  TEXT
         │ текст программы пользователя     │  растёт вверх
         ├──────────────────────────────────┤  TXEND
         │ объектный код (шитый, §10.6)     │  растёт вверх
         ├──────────────────────────────────┤  ENDCOD
         │ область строк (STRSIZ)           │  STRREG / FREBEG / FRELEN
         ├──────────────────────────────────┤
         │                                  │
         │       свободная память           │
         │                                  │
         ├──────────────────────────────────┤  LENT / LIMIT / CIKL
         │ таблица имён, стек GOSUB,        │  растут вниз
         │ стек циклов FOR                  │
         ├──────────────────────────────────┤  TABTOP
         │ область файлов и таблица связи   │
  40000  ├──────────────────────────────────┤  HIMEM (по умолчанию)
         │ экран                            │
```

Существенно, что **таблицы и стеки растут навстречу коду**: `MEMFUL` («нет больше
памяти») возникает, когда `TABTOP` опускается до `TXEND`. Оператор `CLEAR`
одновременно задаёт размер области строк и, вторым аргументом, поднимает `HIMEM` —
именно так под программу в кодах отводят место в верхней памяти, вплоть до занятия
части экрана в узком режиме.

## 10.6. Шитый код

Компилятор порождает не команды процессора, а **список адресов подпрограмм** —
классический шитый код (threaded code). Компиляция сводится к
`MOV #ПОДПРОГРАММА,(R1)+`, где `R1` — указатель записи объектного кода:

```asm
LPRINT::MOV     #DEVLP,(R1)     ; занести адрес устройства
        MOV     (R1)+,-(SP)
```

Исполнение ведёт **`R4` — счётчик шитого кода**. Каждая runtime-подпрограмма
завершается одной и той же командой:

```asm
        JMP     @(R4)+          ; перейти по следующему адресу списка
```

Отсюда несколько следствий, заметных пользователю:

* **`R4` занят системой.** Программа в кодах, вызванная через `USR` или `CALL`,
  обязана сохранить `R4`, иначе возврата в Бейсик не будет. Бейсик со своей стороны
  прячет `R4` в ячейку `SAVJMP` (`2134`) на время внешнего вызова.
* **Аргументы передаются прямо в потоке кода.** Константы и адреса лежат следующими
  словами за адресом подпрограммы, и подпрограмма забирает их через `(R4)+`.
* **Скорость.** Один шаг диспетчеризации — это одна команда `JMP @(R4)+`, поэтому
  накладные расходы на «интерпретацию» практически отсутствуют; вся цена сосредоточена
  в самих подпрограммах.

## 10.7. `TRAP` как внутренний вызов

Самый нестандартный приём в реализации — использование команды `TRAP` вместо
`JSR`. Диспетчер (`TRAP.mac`) устроен так:

```asm
TRAP::  MOV     R0,-(SP)
        MOV     R2,-(SP)
        MOV     4(SP),R0        ; R0 := адрес возврата (слово за TRAP)
        MOV     -2(R0),R2       ; сама команда TRAP
        BIC     #177400,R2      ; младший байт = номер
        SUB     #100,R2
        BLT     T_ERROR         ; номер < 100 — это код ошибки
        ASL     R2
        CALL    @ATRAP(R2)      ; номер >= 100 — вызов подпрограммы
        MOV     R0,4(SP)        ; R0 может сдвинуть точку возврата
        MOV     (SP)+,R2
        MOV     (SP)+,R0
        RTI
```

Номер `TRAP` делится на два диапазона:

| Диапазон | Значение |
| --- | --- |
| `0`…`77` (0…63д) | **Код ошибки.** Управление уходит в `T_ERROR`, который печатает `ошибка` и номер |
| `100`…`130` (64…88д) | **Вызов внутренней подпрограммы** по таблице `ATRAP` |

Выигрыш ровно тот же, что у `EMT` в мониторе (см. [09-монитор.md](09-монитор.md),
§9.4): `TRAP n` занимает **одно слово вместо двух** у `JSR PC,@#адрес`. При
нескольких сотнях обращений экономия составляет сотни слов ПЗУ. Наиболее частый
вызов, `TRAP 112` (пропуск пробелов), встречается в исходниках **42 раза** —
на нём одном экономится 42 слова.

Две дополнительные особенности этого механизма:

* **Параметры в потоке команд.** `R0` указывает на слово за командой `TRAP`,
  и подпрограмма читает аргументы через `(R0)+`. Так, `TRAP 113` (поиск слова
  в таблице) получает адрес таблицы и границы:

  ```asm
        TRAP    113
        .WORD   RWPRIN
        .WORD   3003            ; параметры лежат в коде
  ```
* **Возврат в разные точки.** Подпрограмма может продвинуть `R0`, и тогда возврат
  произойдёт мимо следующих команд. Поэтому за `TRAP` часто стоит цепочка `BR`,
  играющая роль переключателя:

  ```asm
        TRAP    102             ; классифицировать символ
        BR      1$              ; цифра
        BR      4$              ; буква
        ...                     ; прочее — управление приходит сюда
  ```

Назначение основных внутренних `TRAP` (по [БейсикИсх]):

| `TRAP` | Что делает |
| --- | --- |
| `100` | Компиляция выражения (верхний уровень, логические операции) |
| `101` | Поиск символа в списке-строке |
| `102` | Классификация символа: цифра / буква / прочее |
| `104` | Поиск в таблице имён (`LENT`…`TABTOP`) |
| `105` | Распознавание имени функции |
| `106` | Проверка конца оператора |
| `110` | Чтение десятичного числа из текста |
| `111` | Поиск в таблице связи (`FCB`) |
| **`112`** | **Пропуск пробелов** — самый частый |
| `113` | Поиск слова в таблице ключевых слов |
| `114` | Вывод десятичного числа |
| `115` | Компиляция первичного выражения |
| `116` | Разбор списка через запятую |
| `120` | Компиляция строковой константы |
| `121` | Параметры функции `FN` |
| `122` | Компиляция отношения |
| `123`, `124` | Пара целых координат через запятую |
| `125` | Выражение с приведением к целому (`CINT`) |
| `126` | Проверка обязательной запятой |
| `127` | Проверка открывающей скобки `(` |
| `130` | Проверка закрывающей скобки `)` |

> **Почему это важно за пределами Бейсика.** Вектор `TRAP` — это адрес `34`
> (см. [07-прерывания.md](07-прерывания.md) и
> [08-память-и-системная-область.md](08-память-и-системная-область.md)). Бейсик занимает его целиком
> под себя, устанавливая при каждом запуске: `MOV #TRAP,TRAPV`. Программа в кодах,
> вызванная из Бейсика через `USR` или `CALL`, **не может пользоваться командой
> `TRAP`** — она попадёт в диспетчер Бейсика и в лучшем случае получит сообщение об
> ошибке.

## 10.8. Коды ошибок

Бейсик БК-0010-01 печатает только `ошибка NN` — таблицы текстов в его ПЗУ нет.
Расшифровка ниже взята из **ПЗУ Бейсика БК-0011** (`bk11_198_basic1.rom`, адрес
`136247`), где такая таблица есть [✓ ПЗУ]; совпадение нумерации проверено по
[БейсикИсх] для одиннадцати кодов — они отмечены знаком ✓.

| № | Сообщение | Сверено |
| --: | --- | :-: |
| 1 | `NEXT` без `FOR` | |
| **2** | Синтаксическая ошибка | ✓ |
| **3** | `RETURN` без `GOSUB` | ✓ |
| **4** | Кончились `DATA` | ✓ |
| **5** | Неправильный вызов функции | ✓ |
| **6** | Переполнение | ✓ |
| **7** | Нет больше памяти | ✓ |
| **8** | Неопределённый номер строки | ✓ |
| 9 | Несоответствие индекса | |
| 10 | Повторное определение массива | |
| 11 | Деление на ноль | |
| **12** | Невозможно выполнить | ✓ |
| **13** | Ошибка типов | ✓ |
| **14** | Кончилась память строк | ✓ |
| 15 | Слишком длинная строка | |
| 16 | Слишком сложное выражение | |
| 17 | Продолжение невозможно | |
| **18** | Неопределённая функция | ✓ |
| 19 | Ошибка ввода-вывода | |
| 20…23 | *(в таблице пусто — зарезервировано)* | |
| **24** | Отсутствует операнд | ✓ |
| **25** | Переполнение буфера ввода | ✓ |
| 26 | Недопустимое обращение к файлу | |
| 27 | Файл не найден | |
| 28 | Файл уже открыт | |
| 29 | Конец файла | |
| 30 | Неправильное имя файла | |
| 31 | Строка без номера | |
| 33 | Файл не открыт | |

Коды **выше 33** в таблице БК-0011 отсутствуют, но в [БейсикИсх] используются:
`51` — недопустимый код операции (выдаётся по вектору `10`, см.
[07-прерывания.md](07-прерывания.md)), а также `52`, `54`…`57`, `59` в операциях
с внешними устройствами и печатью. Их расшифровка не установлена.

Сообщение об ошибке предваряется словом `ошибка` и звонком: в ПЗУ это байты
`357 373 351 342 353 341 040 007`.

## 10.9. Что Бейсик берёт из системы

### Системные ячейки

| Ячейка | Имя в [БейсикИсх] | Как используется |
| --- | --- | --- |
| `4` | `BREAKW` | Вектор «СТОП». Бейсик ставит сюда **`120234`** и переставляет на `125070` (`IRPS2-10`) на время критических участков — так «СТОП» на них не срабатывает |
| `10` | `ILCODV` | Вектор недопустимой команды → `SYSERR`, выдающий ошибку 51 |
| `34` | `TRAPV` | Вектор `TRAP` — целиком занят диспетчером §10.7 |
| `100` | `TIMER` | Вектор таймера; Бейсик ставит туда заглушку `125070` («работа с принтером») |
| `105` | `TSTKEY` | Признак нажатия клавиши — основа функции `INKEY$` |
| `176`, `200` | `$DRVX`, `$DRVY` | Текущие координаты графического курсора; читаются функциями `POS`/`CSRLIN` и оператором `DRAW` |
| `212`, `214` | — | **Маска цвета фона** (`FON`) и **маска цвета символа** (`MASCW`). Операторы `COLOR`, `PSET`, `PRESET`, `LINE`, `CIRCLE`, `PAINT` работают **непосредственно через эти ячейки** |
| `264`, `266` | `MSTART`, `MLEN` | Стартовый адрес и длина массива — через них передаются параметры при обмене с лентой и при `CALL` |
| `320`…`371` | `MAG` | Блок параметров магнитофона (см. [12-магнитофон.md](12-магнитофон.md)) |
| `360` | `IRPSV` | Вектор ИРПС |

Ячейки `212` и `214` заслуживают отдельного упоминания: соответствие с
[08-память-и-системная-область.md](08-память-и-системная-область.md), §8.1 подтверждается тем, что
оператор `COLOR` в буквальном смысле сводится к их сохранению, изменению и
восстановлению:

```asm
COLR::  MOV     @#214,R3        ; сохранить текущий цвет
        MOV     (SP)+,R0        ; новый цвет из выражения
        CALL    TSTCOL          ; проверить 0..4 и превратить в код 220..224
        EMT     16              ; отдать драйверу дисплея
        JMP     @(R4)+
```

### Функции `EMT`

Бейсик пользуется только штатными функциями пускового монитора, без единого
обращения напрямую к контроллерам экрана или клавиатуры:

| `EMT` | Где применяется в Бейсике |
| --- | --- |
| `6` | Ввод символа — редактор строки, `INKEY$`, пауза |
| `12` | Установка ключей клавиатуры — при инициализации функциональных клавиш |
| `14` | Полная инициализация драйверов при холодном старте |
| `16` | Основной вывод: печать, управляющие коды, цвет, графические примитивы |
| `20` | Вывод строки |
| `22`, `24`, `26` | Служебные операции с экраном и позицией курсора |
| `30`, `32` | Графика: рисование отрезков (`LINE`, `CIRCLE`, `DRAW`, `PAINT`) |
| `34` | Чтение слова состояния дисплея (`CURON`, `CUROFF`, определение режима) |
| `36` | **Обмен с магнитофоном** — `CSAVE`, `CLOAD`, `BSAVE`, `BLOAD`, `FIND` |

Полное описание каждой — в [09-монитор.md](09-монитор.md), §9.4 и далее. Тот факт,
что большая прикладная программа обходится этим набором, служит независимым
подтверждением полноты таблицы `EMT`.

Печать на принтер идёт мимо `EMT`: `LPRINT` и `LLIST` работают **непосредственно
с регистром `177714`** (`ARPORT` в исходниках), что согласуется с замечанием
[06-системные-регистры.md](06-системные-регистры.md), §6.5 — EMT-функций для порта
УВВ не предусмотрено.

## 10.10. Выход в машинные коды: `USR`, `CALL` и пользовательское ПЗУ

### `USR0`…`USR9`

Таблица `USRTAB` (`2100`…`2123`, десять слов) хранит адреса пользовательских
подпрограмм. При старте все десять заполняются адресом заглушки `NOUSR`, которая
выдаёт ошибку 5. Оператор `DEF USRn = адрес` сводится к записи слова в таблицу:

```asm
$DEFUS::MOV     (SP)+,@(R4)+    ; значение выражения -> USRTAB[n]
        JMP     @(R4)+
```

Вызов:

```asm
$USR::  MOV     SP,R5           ; R5 -> аргумент на стеке
        MOV     (R4)+,R0        ; номер USR, уже умноженный на 2
        MOV     (R4)+,R3        ; тип аргумента
        MOV     R4,SAVJMP       ; спрятать счётчик шитого кода
        CALL    @USRTAB(R0)     ; ---> подпрограмма пользователя
        MOV     SAVJMP,R4
        CLR     SAVJMP
        JMP     @(R4)+
```

Отсюда соглашение о связи для подпрограммы в кодах:

* **`R5` указывает на аргумент**, лежащий на стеке;
* **`R3` содержит код типа** аргумента;
* возврат — обычным `RETURN` (`RTS PC`);
* `R4` сохранён системой в `SAVJMP`, но **портить `SAVJMP` нельзя**;
* пользоваться командой `TRAP` нельзя (§10.7).

Именно на этот механизм рассчитаны драйверы серийной периферии — например,
драйвер графопостроителя, который «вызывается как USR-функция вильнюсского
Бейсика» (см. [14-разъёмы-и-периферия.md](14-разъёмы-и-периферия.md), §14.8).

### `CALL` и `_`: обращение к пользовательскому ПЗУ

Операторы **`CALL <текст>`** и **`_ <текст>`** — синонимы; компилируются они
не в вызов по адресу, а в обращение к подпрограмме `USEROM`, которой передаётся
указатель на текст:

```asm
CALL::  MOV     #USEROM,(R1)+
        MOV     R3,(R1)+        ; R3 -> текст аргумента
        RETURN
```

`USEROM` перебирает до четырёх пользовательских ПЗУ, выбирая их **старшим байтом
системного регистра `177716`** и вызывая фиксированный адрес `160000`:

```asm
        AUSROM=160000           ; адрес перехода на пользовательское БИС ПЗУ
        ASPORT=177716           ; системный регистр

USEROM::MOV     (R4)+,R3        ; текст аргумента
        MOV     R4,SAVJMP
        MOV     #1,R0           ; маска выбора ПЗУ: 1, 2, 4, 10
        MOV     #NOROM,@#BREAKW
NEXROM: MOVB    R0,@#ASPORT+1   ; выбрать ПЗУ  <-- запись в 177717
        ASL     R0
        CALL    @#AUSROM        ; вызвать по адресу 160000
        BCS     ERROM           ; C=1 — это ПЗУ отказалось, пробуем следующее
        JSR     R0,STATUSQ
        JMP     @(R4)+
...
ERROM:  CMP     R0,#10
        BNE     NEXROM
```

Соглашение получается такое: пользовательское ПЗУ начинается с точки входа
`160000`, получает в `R3` адрес текста аргумента и **возвращает `C = 1`, если
команда ему не адресована**. Адрес `160000` не случаен: с него же начинаются
резервные входы `EMT 52`…`EMT 110` пускового монитора
([09-монитор.md](09-монитор.md), §9.4) — то есть это общее для машины место
подключения стороннего ПЗУ.

> ⚠ **Запись в старший байт `177716`.** В
> [06-системные-регистры.md](06-системные-регистры.md), §6.6 разряды `08`…`15`
> регистра `177716` описаны как **доступные только по чтению** — это жёстко заданный
> адрес начального пуска `100000`. Бейсик тем не менее записывает в байт `177717`
> значения `1`, `2`, `4`, `10` и `200`. Противоречия здесь нет ровно до тех пор,
> пока речь идёт о **штатной машине**: на ней такая запись ни на что не влияет
> и вся ветка `USEROM` заканчивается ошибкой 5. Механизм рассчитан на **внешнюю
> плату ПЗУ**, которая сама дешифрует запись по этому адресу. Ни одного описания
> такой платы в источниках справочника нет, поэтому вопрос занесён в реестр как
> открытый — §16.5, К2.

## 10.11. Функциональные клавиши

Десять клавиш `1`…`9`, `0` в регистре «АР2» подставляют заранее заданный текст
(см. [11-экран-и-клавиатура.md](11-экран-и-клавиатура.md), §11.4). Буфер — `FNKSTR`
(`2140`, десять записей по 17д байт: длина + текст). Значения по умолчанию
задаёт таблица `KEYTXT` [✓ ПЗУ]:

| Клавиша | Подставляемый текст |
| :-: | --- |
| 1 | `COLOR ` |
| 2 | `AUTO ` |
| 3 | `GOTO ` |
| 4 | `LIST ` |
| 5 | `RUN` + ВК |
| 6 | `COLOR 1,0` + ВК |
| 7 | `CLOAD"` |
| 8 | `CONT` + ВК |
| 9 | `.` + ВК |
| 0 | очистка экрана + `RUN` + ВК |

Клавиша `9` подставляет точку не случайно: **строка, начинающаяся с точки**, —
это команда редактирования текущей строки (`ANALIZ` проверяет первый символ
буфера на `'.'` и уходит в `EDIT`).

Переопределяются они оператором `KEY`, а инициализация (`KINIT`) заодно сообщает
новые тексты драйверу клавиатуры через `EMT 12` и выводит подсказку — строку
служебных обозначений внизу экрана (`ISVSS`).

## 10.12. Числа и типы

Бейсик оперирует четырьмя типами; суффикс в имени переменной задаёт тип:

| Суффикс | Тип | Размер |
| :-: | --- | --- |
| `%` | целое | 1 слово |
| `!` | вещественное обычной точности | 2 слова |
| `#` | вещественное двойной точности | **4 слова** |
| `$` | строка | дескриптор |

Вещественные числа хранятся в **формате с плавающей запятой DEC** (F- и D-формат
PDP-11): знак, 8-разрядный порядок со смещением `200`, мантисса со скрытой старшей
единицей. Проверяется по константе `1.0`, которая в ПЗУ записана как `040200 0 0 0`
[✓ ПЗУ] — каноническое представление единицы в этом формате.

Работа с ними ведётся через стек макросами `FPUSH` / `FPOP`, разворачивающимися
в четыре команды `MOV`:

```asm
        FPUSH   10(SP)          ; -> MOV 6+10(SP),-(SP)  x4
        JSR     R4,STAND
        .WORD   $DMUL$          ; имя операции — в потоке кода
        .WORD   17$             ; адрес возврата
```

Собственной арифметики у К1801ВМ1 для этого нет (см.
[05-система-команд.md](05-система-команд.md), §5.3): все четыре действия,
а также `SIN`, `COS`, `EXP`, `LOG`, `SQR`, `POW` реализованы программно
в `FLOAT.mac` — это **2800 строк, около четверти всего Бейсика**.

## 10.13. Словарь языка по данным ПЗУ

Все таблицы ниже прочитаны непосредственно из микросхем 106 и 107 [✓ ПЗУ].

**Операторы, доступные в программе** (компилируются, таблица `AOPSUB`):

```
'  DIM  LINE  CIRCLE  DEF  TROFF  FOR  GOSUB  GOTO  IF  INPUT  LET  OPEN  ?
CLOSE  NEXT  PAINT  PRINT  DRAW  REM  RETURN  _  CALL  READ  POKE  OUT  STOP
LPRINT  ON  RESTORE  END  DATA  LOCATE  CLEAR  TRON  KEY  BEEP  CLS  COLOR
PSET  PRESET
```

**Команды непосредственного режима** (таблица `AOPMON`):

```
CLOAD  BLOAD  FIND  CSAVE  BSAVE  LOAD  SAVE  NEW  AUTO  LIST  MONIT  LLIST
RENUM  DELETE  CONT  RUN
```

**Числовые функции:**

```
PI  POINT  INP  PEEK  RND  SQR  SIN  COS  TAN  ATN  EXP  LOG  INT  FIX  ABS
SGN  CINT  CDBL  CSNG  EOF  CSRLIN  POS  LPOS  FRE  LEN  ASC  VAL  USR  FN
```

**Строковые функции:** `INKEY$  MID$  STRING$  STR$  CHR$  BIN$  OCT$  HEX$`

**Операции:** `MOD  NOT  IMP  EQV  XOR  OR  AND`, отношения `=`, `>`, `<`

**Прочие служебные слова:**

```
REM  RETURN  NEXT  RESTORE  GOSUB  GOTO  THEN  ELSE  EL  ATN  AT  TAB  TO
STEP  OUTPUT  INPUT  AS  USR  FN  B  BF
```

`B` и `BF` — модификаторы оператора `LINE` (рамка и закрашенный прямоугольник).

**Ключи оператора `DRAW`** (строка `MNBCSAXRULDRULEHGFEHG` в ПЗУ):
`M` — перемещение в точку, `N` — с возвратом, `B` — без вычерчивания,
`C` — цвет, `S` — масштаб, `A` — поворот, `X` — исполнить подстроку;
направления `R`, `L`, `U`, `D` и диагонали `E`, `F`, `G`, `H`.

**Типы файлов на ленте:** `COD` и `BIN` — см.
[12-магнитофон.md](12-магнитофон.md).

## 10.14. Три разных Бейсика семейства

Сравнение сделано по строкам таблиц ключевых слов в самих ПЗУ [✓ ПЗУ].

| | БК-0010-01 | БК-0011 | БК-0011М |
| --- | --- | --- | --- |
| Микросхемы | 106, 107, 108 | 198, 199, 200 | 327, 328, 329 |
| Происхождение | ВЦКП ВГУ, Вильнюс, сборка **1986.07.24** | ВЦКП ВГУ, Вильнюс, **1987** | подпись **ОКБ «Экситон»** |
| Строка в ПЗУ | `БЕЙСИК (Вильнюс,1986.07.24)` | `БЕЙСИК БК0011` / `Вильнюс, 1987` | `БЕЙСИК БК11/10 (с) ОКБ "Экситон"` |
| Тексты сообщений об ошибках | **нет**, только номер | **есть**, 33 сообщения | нет |
| Совместимость исходников | базовая редакция | **отдельная ветвь 1987 г.** | базовая редакция + `MERGE` |

Различия в наборе операторов:

| Оператор | 0010-01 | 0011 | 0011М |
| --- | :-: | :-: | :-: |
| `FIND` | есть | **нет** | есть |
| `MONIT` | есть | **нет** | есть |
| `MERGE` | нет | **есть** | **есть** |
| `FILES` | нет | **есть** | нет |
| `SCREEN` | нет | **есть** | нет |
| `WIDTH` | нет | **есть** | нет |
| `TRAP` | нет | **есть** | нет |
| `SYSTEM` | нет | **есть** | нет |

> **Неожиданный вывод.** Бейсик **БК-0011М — это не развитие Бейсика БК-0011**,
> а возврат к более ранней, «десятой» редакции с добавленным `MERGE`. Ветвь 1987
> года с операторами `SCREEN`, `WIDTH`, `FILES` и текстовыми сообщениями об ошибках
> осталась только на БК-0011.
>
> Это согласуется с общей картиной несовместимости двух старших машин
> (см. [02-БК-0011.md](02-БК-0011.md), §2.9): у БК-0011 и БК-0011М разное не только
> системное ПО, но и язык высокого уровня, причём **не по линии «старше — новее»**.

Исходный текст редакции 1987 года сохранился отдельно — семь файлов в
`Бейсик-Вильнюс/BASIC - VVU - 1987/`, с целевыми машинами `BK0011`, `BK0010`,
`ДВК-2`, `ДВК-3`, `УКНЦ`, ВУМС «Силелис», MDS РАФОС и с сопроводительным описанием
совместимости с MSX-Бейсиком (`BASMSX.TXT`, 6000 строк). Постранично он не
разбирался — см. [16-источники-и-соглашения.md](16-источники-и-соглашения.md), §16.6.

## 10.15. Достоверность восстановленного текста

Опубликованные исходники — это **восстановленный, а не заводской** текст, поэтому
их согласие с ПЗУ проверялось напрямую. Из полного листинга извлечены все
машинные слова и сопоставлены с образами трёх микросхем:

| Показатель | Значение |
| --- | --- |
| Сопоставлено слов | 11 686 |
| Совпали точно | 10 506 |
| Совпали с учётом настройки адресов при компоновке | 1 098 |
| **Итого совпадение** | **99,3 %** |
| Действительных расхождений | 82 слова |

Расхождения не рассеяны по тексту, а сводятся к трём местам:

1. **Четыре ячейки рабочей области** размещены не по тем адресам — 63 слова из 82.
   Разбор — в примечании к §10.4;
2. **Девять вещественных констант** в математических подпрограммах отличаются
   ровно вдвое (`040100` вместо `040200`, `037700` вместо `040000`) — то есть
   на единицу порядка;
3. **Две команды** в области работы с внешними устройствами.

Сопоставление с Бейсиками старших машин даёт 0,6 % и 0,8 % совпадения — то есть
восстановленный текст относится именно к **редакции БК-0010-01** и ни к какой другой.

Вывод: как описание **логики** Бейсика источник надёжен, и все структурные
утверждения этой главы на него опираются. Но **конкретные адреса** из него следует
брать только после сверки с ПЗУ — что и сделано для каждой таблицы главы.
